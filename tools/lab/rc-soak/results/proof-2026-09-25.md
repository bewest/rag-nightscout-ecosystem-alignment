# rc-soak proof runs, 2026-09-25

*Contributor-facing. Snapshot: every number below was measured on 2026-09-25 between 19:03Z and
20:28Z with the harness in `tools/lab/rc-soak/`. Synthetic data only. Raw run directories
(requests, ledgers, samples, server logs, generated secrets) stay outside version control.*

**Current candidate: e9dbb1fb** (`wip/object-id-crud-fixes-2`, open (branch)): ab7b22d6 plus six fix
commits, among them BF-131, the fix for CANDIDATE-1 below. Its runs are in the section
[Candidate e9dbb1fb](#candidate-e9dbb1fb). The first sections describe ab7b22d6.

## Builds and environment

| arm | build | head | tree | package version |
|---|---|---|---|---|
| A | tag 15.0.8 = origin/master | 92d08342 | ab458e98 | 15.0.8 |
| B | candidate: origin/dev 4f705217 + #8758 head | ab7b22d6 | 25ab7afc | 15.0.9 |
| dev (attribution only) | origin/dev | 4f705217 | | 15.0.9 |

- Worktrees `externals/work/crm-6a-soak-{a,b,dev}`, `npm ci` under `n exec 22.23.2`.
- Node v22.23.2 for every run except R6 (v24.20.0). MongoDB 7.0.43 except R6 (4.4.24); the
  server version is read from each server into `run.json`.
- `AUTH_DEFAULT_ROLES=denied` except R6 (`readable`).
- Compressed mode: one CGM tick (5 simulated minutes) every 2.1 to 3.5 real seconds.

## Runs

| run | arms | real min | simulated | compared ops | differing | ledger keys (missing) | parity | verdict |
|---|---|---|---|---|---|---|---|---|
| R1 A/A control | A, A | 10 | 24 h (288 ticks) | 3078 | 0 | 966 (0 / 0) | equal: entries 288, treatments 318, devicestatus 342, profile 2 | PASS |
| R2 A/A, drop 1% devicestatus on b | A, A | 10 | 24 h | 3078 | 4 | a 966 (0), b 966 (2) | devicestatus 342 / 340, 2 only in A | FAIL (6 findings) |
| R3 A/B, mongo restart + server restart | A, B | 50 | 72 h (864 ticks) | 9232 | 1405 | 2896 (0 / 0) | equal: entries 864, treatments 954, devicestatus 1025, profile 4 | FAIL (14 findings) |
| R4 A/A control | A, A | 25 | 48 h (576 ticks) | 6155 | 0 | 1931 (0 / 0) | equal | PASS on every check except memory; see below |
| R5 A/A, b keeps 16 KB heap per request | A, A | 25 | 48 h | 6155 | 0 | 1931 (0 / 0) | equal | FAIL (memory) |
| R6 A/B, node 24, mongo 4.4, readable, 45 s mongo outage | A, B | 15 | 24 h | 3078 | 396 (61 inside the outage window) | 966 (0 / 0) | equal | FAIL (3 findings) |
| R7 A/A control, harness 2 (page loads) | A, A | 12 | 24 h | 3370 | 0 | 966 (0 / 0) | equal | PASS |
| R8 A/B, harness 2 | A, B | 25 | 48 h | 6739 | 1076 | 1931 (0 / 0) | equal | FAIL (6 findings) |

Every run: 0 liveness samples not ok for either arm; every acknowledged sgv inside the 48 h window
reached the arm's socket follower (R3: 492/492 each arm; R8: 542/542); no 5xx other than the
allowed Loop remote-command 500 on both arms.

Commands (with `SOAK_STATE` set; runs in parallel used their own `PFX` and ports):
`lab.sh run --aa --minutes 10 --sim-hours 24` (R1), `... --fault-drop 0.01` (R2),
`lab.sh up` + `lab.sh traffic <run> --minutes 50 --sim-hours 72` + `lab.sh disturb <run>
mongo-restart both` at +20 min and `server-restart both` at +35 min (R3),
`lab.sh run --aa --minutes 25 --sim-hours 48` (R4), `... --fault-leak 16` (R5),
`MONGO_IMAGE=mongo:4.4 NODE_VER=24.20.0 lab.sh up --config readable` + `traffic --minutes 15
--sim-hours 24` + `OUTAGE_SEC=45 disturb mongo-outage both` at +6 min (R6),
`lab.sh run --aa --minutes 12 --sim-hours 24` (R7), `lab.sh run --minutes 25 --sim-hours 48` (R8).

## The checks can fail

| check | break | control | result |
|---|---|---|---|
| ledger + parity | R2: `proxy.js` answered 200 to 2 of arm b's 342 devicestatus uploads and never forwarded them | R1, same build and traffic without the proxy fault | R2: `ledger arm b: 2 acknowledged writes are not in its database` (ds-38, ds-197) and `parity devicestatus: soakKey only in A (x2)`; the same two keys; proxy log counts 2 drops. R1: zero |
| memory | R5: arm b's server retains 16 KB of heap per request (`preload.js`) | R4, identical without the fault | heap-minima growth difference b-a 79.6 MB (R5) and -3.1 MB (R4); threshold 25 MB |
| page load | none needed: R8's arm B shows the defect below | R7 (A/A) | R8 b: 36 deleted records sent to newly opened pages; R7: 0 |
| responses | R2's proxy echo has no `_id` | R1 | R2: 4 response signatures; R1: 0 of 3078 |

The memory threshold is calibrated on R4. With the first rule (5 MB per 10 000 requests and 10 MB/h)
R4, an A/A control, reported a leak on arm a (8.72 vs -1.29 MB/h over 25 min), so that rule was
replaced by the growth difference over the evaluated span; R4 and R5 were re-analysed with it.

## Differences between A and B, classified

Against `releases/cgm-remote-monitor-15.0.9/release-notes.md` (R3 counts; R6 and R8 show the same
signatures):

| signature | count (R3) | classification |
|---|---|---|
| `oref0 count=1?<hash>` / `count=1?token=…`: header `Deprecation: true` on B | 287 / 288 | expected: Corrections > count, "a whole number followed by ?" |
| GluPredKit `count=0` windows (treatments, sgv, profile): `Deprecation: true` | 36 each | expected: Corrections > count, "count=0 and a date range" |
| GluPredKit `count=0` devicestatus window: `Deprecation` and a longer list on B | 36 | expected: same section, "Device status reports gave only the last 10 here on 15.0.8" |
| `entries.json?count=0` without a window: `Deprecation` and a different length | 18 | expected: same section, "count=0 and no date range" |
| Loop remote command: B's short message instead of A's HTML error page naming a TypeError | 6 | expected: Other fixes, "clearer error messages when a Loop remote command fails" |
| `/api/v2/properties`: `cob.treatmentCOB` object present on A, absent on B | 240 | expected by commit 34e9b2da ("On the properties API cob.treatmentCOB is a number now"); the release notes' COB section describes the value, not this field change: **release-notes gap** |
| entries re-send without `_id`: B's reply carries `_id`, A's does not | 144 | expected by #8758 decision D3 (`tools/lab/object-id/results/object-id-2026-09-24.md`, P-ID-3); not in the release notes: **release-notes gap** |
| `entries.json?count=288`: B returns different readings from A, and adds `mills` to each | 268 | **not expected**: CANDIDATE-1 below. `mills` marks a reply built from the in-memory copy (`lib/api/entries/index.js` adds it on that path, in 15.0.8 too); A's replies to the same request carry no `mills` |
| `/api/v2/properties`: `cob.lastCarbs.notes` on B only, once per run (R3 tick 195, R6 and R8 tick 57); in R8 (which asks again) still different 3 s later | 1 | **not explained**: CANDIDATE-2 below |
| requests during the server restart: one arm answered, the other not yet | 9 | disturbance window; not counted from harness 2 on |

## Candidates

**CANDIDATE-1 (BF-131, fixed (branch) e9dbb1fb): a deleted record keeps being served from memory (B only).** On ab7b22d6,
`DELETE /api/v1/entries/<id>`, `/api/v1/treatments/<id>` and `/api/v1/devicestatus/<id>` answer
200 and remove the document from MongoDB, but the server's in-memory copy keeps it:

- `GET /api/v1/entries.json?count=10` (and `count=100`, `count=288`) keeps returning the deleted
  reading after 35 s and after a new reading arrives; the same read with a filter
  (`find[sgv][$gte]=0`), which goes to MongoDB, does not.
- `treatments.json?count=5` and `devicestatus.json?count=5` keep returning the deleted record.
- A newly opened web page is sent the deleted treatment in its first `dataUpdate` (R8: 36
  deleted records, some 24 minutes after the delete).
- Websocket `dbRemove` of a treatment does not show it (control, same probe).
- A (15.0.8) and dev 4f705217 do not show it, with the same probes on fresh databases.

Mechanism: `lib/server/entries.js` `remove()` emits `data-update` with `changes: opts.find._id`
after `query_for(opts)`; on B, `idForms.matchEitherForm` rewrites `opts.find._id` in place to
`{ $in: [<hex>, ObjectId] }` (confirmed with node against B's `lib/server/query.js` and
`object-id-forms.js`), so the cache's `removeFromArray` (`o._id == id`) never matches. On A the
same field is left as a single id. Ablation: a copy of ab7b22d6 whose `remove()` passes the id as
asked (branch `lab/6a-soak-ablate`, f76e6fd5, one file, +3/-1) passes `probe-deleted-entry.js`;
unmodified ab7b22d6 fails it in the same session. Only entries was ablated; treatments and
devicestatus were reproduced, not ablated.

Reproduce: `lab.sh up`, then `node tools/lab/rc-soak/probe-deleted-entry.js
http://127.0.0.1:17532 <run>/secrets/api_secret` (exit 1 on B, 0 on A).

Why it matters: a treatment deleted because it was entered by mistake (a bolus or carbs) is
still sent to newly opened pages and to readers of the unfiltered API, so insulin and carbs on
board shown there can include it until the server restarts or the record leaves the in-memory
window.

**CANDIDATE-2: `/api/v2/properties` `cob.lastCarbs` differs at one tick.** Resolved below
(section [Candidate e9dbb1fb](#candidate-e9dbb1fb)): the "last carbs" entry can be an older carb
record on every build, 15.0.8 included; ab7b22d6 differs from 15.0.8 only in the tick at which it
switches.

## Observations that are not findings

- R6 (node 24.20.0, mongo 4.4): both arms' RSS rose to about 250 MB under load; after traffic
  stopped, A's last sample was 136 MB and B's 280 MB (one sample each). The heap check passed
  (10.8 MB, threshold 25). Watch RSS in the long soak.
- A socket.io `authorize` with a readable access token in the `token` field (instead of the JWT the
  web client sends) fails with "All validation failed" on both builds, and the failure adds the
  client address to the authentication delay list, after which every HTTP request from that
  address waits about 5 s. Seen while building the harness; by design on both arms.
- 45 s MongoDB outage (R6): 3 of 3079 requests per arm without a 2xx, every write retried and
  stored afterwards on both arms.
- `treatments.json?count=50&token=…` (harness 2, R8: 576 reads) did not differ between the arms,
  although the direct probe with `count=5` shows CANDIDATE-1 on treatments. Explained below.

## Candidate e9dbb1fb

Measured 2026-09-25 20:42Z to 21:52Z. B = e9dbb1fb (tree ea4c4852, package 15.0.9), worktree
`externals/work/crm-6a-soak-b2`, `npm ci` under node 22.23.2. `git log ab7b22d6..e9dbb1fb` lists six
commits (17add44b, c3a34bac, 63dd716c, cb7d4110, 6c3ccce6, e9dbb1fb). Node 22.23.2, MongoDB
7.0.43, `AUTH_DEFAULT_ROLES=denied` for every run here.

Harness versions: 3 records values for key-set differences (`diffs-values.jsonl`); 4 adds the
header-authenticated reads `uploader.treatments.count50` and `uploader.devicestatus.count10`.

| run | arms | shape | harness | compared ops | differing | ledger (missing) | parity | findings | verdict |
|---|---|---|---|---|---|---|---|---|---|
| R9 | 15.0.8 / e9dbb1fb | as R8: 25 min = 48 h | 3 | 6739 | 783 | 1931 (0 / 0) | equal | 0 | PASS |
| R13 | 15.0.8 / e9dbb1fb | as R8 | 4 | 7891 | 783 | 1931 (0 / 0) | equal | 0 | PASS |
| R10 | 15.0.8 / e9dbb1fb | as R3: 50 min = 72 h; mongo restart at +20 min, server restart at +35 min | 3 | 10108 | 1130 (75 inside disturbance windows) | 2896 (0 / 0) | devicestatus 1013 / 1014 | 2 at the run; 1 after re-analysis, explained below | FAIL, see below |
| R16 | 15.0.8 / e9dbb1fb | 15 min = 24 h; server restart twice, 30 s mongo outage | 4 | 3946 | 412 (166 inside disturbance windows) | 966 (0 / 0) | equal | 0 | PASS |
| R11 | 15.0.8 / ab7b22d6 (control) | 12 min = 24 h | 4 | 3946 | 655 | 966 (0 / 0) | equal | 9 | FAIL |
| R12 | 15.0.8 / dev 4f705217 | 12 min = 24 h | 4 | 3946 | 343 | 966 (0 / 0) | equal | 0 | PASS |

Every run above: 0 liveness samples not ok on either arm; every eligible reading delivered to the
socket follower (R10: 494/494 each arm); memory check evaluated on R9, R10 and R13 with heap growth
difference b−a of −2.1, −1.0 and 0.9 MB (threshold 25).

**CANDIDATE-1 is gone on e9dbb1fb.** In R9, R10, R13 and R16: no `loopfollow.entries288`
difference, 0 deleted records sent to a newly opened page on either arm, and no page-load
difference between the arms. The R11 control (ab7b22d6) on the same harness shows all of them:
20 deleted records sent to page loads, 47 page-load differences, and 259 differences on the
new header reads (237 + 2 + 1 treatments, 19 devicestatus). `probe-deleted-entry.js` on fresh databases, run twice each: ab7b22d6 exit 1
(the plain read still returns the reading), e9dbb1fb exit 0. The treatments and devicestatus
`count=5` probe and the page-load probe after a v1 DELETE: ab7b22d6 still returns or sends the
record, e9dbb1fb does not.

**Differences between 15.0.8 and e9dbb1fb** (R13, identical signatures in R9, R10 and R16), all
expected:

| signature | R13 count | classification |
|---|---|---|
| oref0 `count=1?<hash>` / `?token`: `Deprecation: true` | 192 / 192 | Corrections > count, "a whole number followed by ?" |
| `/api/v2/properties` `cob.treatmentCOB` absent on B | 188 + 3 | commit 34e9b2da; **release-notes gap** |
| entries re-send reply carries `_id` on B | 96 | #8758 decision D3; **release-notes gap** |
| GluPredKit `count=0` windows (treatments, sgv, profile): `Deprecation` | 24 each | Corrections > count, "count=0 and a date range" |
| GluPredKit `count=0` devicestatus: `Deprecation` and a longer list | 23 + 1 | same, device status |
| `entries.json?count=0` with no window: `Deprecation` and default length | 12 | Corrections > count, "count=0 and no date range" |
| Loop remote command error text | 4 | Other fixes |

**R10's two findings.**
- *parity devicestatus: ds-604 stored once on A and twice on B.* The original POST was sent during
  the server restart at 21:17:26: A answered `ECONNREFUSED` (never reached), B's connection dropped
  mid-request (`UND_ERR_SOCKET`), so B's server had it. The driver re-sent it to both at 21:17:29,
  as an uploader re-sends after an error, and v1 devicestatus has no de-duplication on either build
  (every run's `loop.devicestatus.resend` stores a second copy on both arms). A disturbance
  artefact, not a build difference. The analyser now reports a copy-count or presence difference
  as a disturbance artefact when every acknowledgement of that key fell inside a disturbance
  window. R10's databases had already been removed when that rule was added, so it has not been
  re-run on R10, and R16 did not produce such a request: **the rule has not yet been seen to fire.**
- *2 page-load differences (loads at ticks 551 and 743).* In each, one arm's page already had the
  reading written that tick and the other's did not yet. The page-load comparison now skips records
  acknowledged less than 10 s before the load. Re-analysis of R10: 0 differences (2 skipped). The
  same re-analysis of R11 still reports the ab7b22d6 defect: 20 deleted records shown, 47
  differences (1 skipped).

**CANDIDATE-2 explained: "last carbs" can be an older entry, on 15.0.8 too.** Captured with
`DUMP_TICKS` (both arms' raw `/api/v2/properties` at ticks 45 to 63, 6 h runs):

| tick | 15.0.8 | dev 4f705217 | ab7b22d6 | e9dbb1fb |
|---|---|---|---|---|
| 45 | carb-9 | carb-9 | | |
| 48 to 54 | carb-45 | carb-45 | carb-45 (54) | carb-45 (54) |
| 57 | carb-45 | carb-45 | **carb-9** | carb-45 |
| 60, 63 | **carb-9** | **carb-9** | carb-9 (60) | **carb-9** (60) |

carb-9 is a 25 g Carb Correction from 4 hours earlier, edited at tick 44 (`notes: "edited 44"`);
carb-45 is the newest carb entry (15 g). The difference the soak flagged at tick 57 is only when
the switch happens: ab7b22d6 switches one reload earlier than 15.0.8. From tick 60 all four builds
report the older, edited entry as `lastCarbs`, and the header read `treatments.json?count=50` at
ticks 57 and 60 holds only carb-45 on both arms (carb-9 is outside the newest 50).
`lib/plugins/cob.js` takes `lastCarbs` as the last treatment with carbs *in array order*
(`treatments.forEach … lastCarbs = treatment`), so it depends on the order of the in-memory
treatments; which order change the edit or the reload causes was not traced. Not from #8758 and
not from dev: 15.0.8 behaves the same. `lastCarbs` is shown on the COB pill's detail line
("last carbs" amount and time); the COB total adds up every carb entry and is not affected by
which one is picked. A candidate for the register as a defect live on 15.0.8, not a 15.0.9
regression.

**Why `treatments.json?count=50&token=…` never showed CANDIDATE-1.** `lib/api/treatments/index.js`
serves from the in-memory copy only when `Object.keys(query).length == 1`, and
`lib/api/devicestatus/index.js` only when `count` is the only query key. The follower read carries
`token` as a second key, so it always went to MongoDB. The direct probe used a header and `count`
alone. `lib/api/entries/index.js` decides on `find` keys only, which is why
`entries.json?count=288&token=…` did hit the stale copy. Harness 4 adds the header forms; on
ab7b22d6 (R11) they differ from 15.0.8 in 259 reads, on e9dbb1fb (R13) in none.
