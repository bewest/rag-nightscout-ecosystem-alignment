**Superseded.** This record describes the 2026-09-16 Phase 0 branch candidate (`origin/dev` `a8888f0d` plus unmerged `bf/*` branches), not 15.0.9 as it will ship: `official/master..official/dev` `74fc6619` plus eight additions, see [contents.md](contents.md). No new record has been captured. The current evidence for the additions is a **candidate rc, pre-merge** measurement, not a verification record: `rc/15.0.9-additions-c` `b9c9828b`, 2508/0/3 on Node 20/22/24 × MongoDB 4.4/7, in [rc-15.0.9-integration-record.md](../../docs/30-design/remedial/rc-15.0.9-integration-record.md). The merge commits into `dev` do not exist yet. The record below and its JSON are unchanged.

<!--
  ============================================================================
  GENERATED FILE - MUST NOT BE HAND-EDITED, AND MUST NOT BE EDITED AT ALL.

  Source of truth:  verification-record.json (beside this file)
  Generator:        tools/queue/verification-record.py

  A verification record is captured ONCE, at release time, and is never
  revised. If something changes -- a gate starts passing, a branch moves, a
  register entry is reproduced -- that is a NEW record with a new
  captured_at, not an edit to this one. A record that is edited after the fact
  is not a record; it is a claim about the past that nothing can check.
  ============================================================================
-->

# Verification record &mdash; cgm-remote-monitor-15.0.9

**Captured at 2026-09-16T00:05:00-07:00.** Generated from `verification-record.json` by `tools/queue/verification-record.py`. Do not edit this file or its JSON; capture a new record instead.

This is the quality-system record of what was actually checked when this release was cut. It is not a plan and not a changelog. It contains failures and gaps on purpose: a record that shows only passes has been edited, selected or fabricated.

## Headline

| | |
|---|---|
| items in this release | 17 |
| item verdicts | FAIL=5, PASS=9, UNMEASURED=3 |
| gate outcomes | FAIL=7, NO-GATE=19, PASS=35, SKIP=13 |
| register entries carried | 25 |
| register basis (rule 3) | reproduced=13, unstated=12 |
| test commands captured | 19 |
| **things NOT verified** | **76** |

Nothing in this record is closed for an operator. Every branch below is local and unpushed unless the publication line says otherwise; a defect stops reaching operators when a human pushes and a maintainer merges, which is outside this record.

## How this record was taken

| | |
|---|---|
| captured_at | `2026-09-16T00:05:00-07:00` |
| timestamp source | --captured-at argument (this tool never reads a clock) |
| generator | `tools/queue/verification-record.py` |
| generator sha256 | `32a3de8f925db126ac967a9a1213d7ca319f3e76d54f696f383436defe33ae23` |
| selection | ids=`-` parcels=`phase0` states=`-` |
| gate kinds RUN | `static,unit` |
| gate kinds NOT run | `integration,network` |
| manifest | `queue/work-queue.yaml` sha256 `e562fd529c2c5968` |
| register | `docs/30-design/remedial/nightscout-backfix-register.md` sha256 `614b3dee9c211dfe` (69 entries parsed) |
| control surface HEAD | `08753474c23c` (working tree NOT clean: 21 uncommitted path(s)) |

- **cgm-remote-monitor** &mdash; `externals/cgm-remote-monitor-official` at `a8888f0d9fac` (`HEAD`), origin `https://github.com/nightscout/cgm-remote-monitor.git`
- **nightscout-connect** &mdash; `externals/nightscout-connect` at `649a7de27b9e` (`release/v0.0.14`), origin `https://github.com/nightscout/nightscout-connect.git`

---

## What is in the release, and what each item resolved to

`state (claimed)` is what the manifest asserts. `verdict (measured)` is what the gates said when this record was taken. Where they disagree, both are printed and the disagreement is in the gaps section.

| id | repo | branch | commit | ahead of base | state (claimed) | verdict (measured) | gates |
|---|---|---|---|---|---|---|---|
| `P0-A` | cgm-remote-monitor | `bf/alarms` | `5dcf783fdbb2` | 3 | `gate-not-met` | **FAIL** | 4/5 (2 skip, 2 no-gate) |
| `P0-B` | cgm-remote-monitor | `bf/cache` | `4f86bab1637e` | 2 | `gate-not-met` | **PASS** | 2/2 (2 no-gate) |
| `P0-C` | cgm-remote-monitor | `bf/auth` | `64db1f35ca31` | 2 | `gate-not-met` | **FAIL** | 2/3 (2 skip, 2 no-gate) |
| `P0-C-REMEDIATE` | cgm-remote-monitor | `-` | `unresolved` | - | `not-started` | **UNMEASURED** | 0/0 (1 no-gate) |
| `P0-D` | cgm-remote-monitor | `bf/coercion` | `b7234753` † | **2** | `ready-to-push` | **PASS** | 5/5 |
| `P0-E` | cgm-remote-monitor | `bf/reads` | `2ecfeb53ff1e` | 6 | `ready-to-push` | **PASS** | 3/3 (5 skip, 3 no-gate) |
| `P0-F` | nightscout-connect | `fix/connect-timer-jitter` | `c1cce2a2f962` | 1 | `ready-to-push` | **PASS** | 2/2 (1 no-gate) |
| `P0-G` | cgm-remote-monitor | `bf/food` | `73495331e68c` | 1 | `ready-to-push` | **PASS** | 3/3 (1 skip) |
| `P0-H` | cgm-remote-monitor | `bf/merge` | `b06c6faf882e` | 1 | `ready-to-push` | **PASS** | 3/3 (1 no-gate) |
| `P0-I` | cgm-remote-monitor | `bf/parms` | `eb0bc918036a` | 3 | `ready-to-push` | **PASS** | 4/4 |
| `P0-TAG` | nightscout-connect | `release/v0.0.14` | `649a7de27b9e` | 11 | `ready-to-push` | **PASS** | 4/4 (1 skip, 1 no-gate) |
| `P0-PIN` | cgm-remote-monitor | `bf/connect-pin` | `0807eb1c1843` | 1 | `blocked` | **PASS** | 3/3 (1 no-gate) |
| `P0-LOCK` | cgm-remote-monitor | `bf/connect-pin` | `0807eb1c1843` | 0 | `blocked` | **FAIL** | 0/1 (1 skip) |
| `P0-T01` | cgm-remote-monitor | `fix/quadratic-treatment-processing` | `dfe2753dcc1c` | 1 | `in-flight-upstream` | **UNMEASURED** | 0/0 (1 skip, 1 no-gate) |
| `FU-LIMIT` | cgm-remote-monitor | `-` | `unresolved` | - | `blocked` | **FAIL** | 1/2 (1 no-gate) |
| `FU-RESIDUALS` | cgm-remote-monitor | `-` | `unresolved` | - | `gate-not-met` | **FAIL** | 0/3 (1 no-gate) |
| `FU-HYGIENE` | cgm-remote-monitor | `-` | `unresolved` | - | `not-started` | **UNMEASURED** | 0/0 (2 no-gate) |

† `bf/coercion` was `ab197bf86892` and one commit when this table was first measured. It is now
`b7234753` and **two** commits, after two amends and one fold-in on 2026-09-16, all before any
push.

The first was comment-only: two code comments and one paragraph of the commit message asserted
BF-32's refuted mechanism as fact. The second carries code — **BF-68**, a regression the branch
itself introduced. Excluding every non-value operator from conversion is right for `$regex` and
wrong for `$type`, whose operand is a BSON type code: `find[sgv][$type]=2` worked on `origin/dev`
and became an HTTP 500. `operandReaderFor` reads a digits-only `$type` operand as a number and
passes aliases through. One further test change stops that commit pinning the `$exists` operand as
a literal string, which the next commit turns into a boolean.

The fold-in is BF-40, written first as a branch of its own and combined because it does not stand
alone: with only that commit, `$exists=false` stays inverted on the nine fields a `walker` names,
which are the fields anyone filters on. See the register's BF-40 for the measured split.

The gates were re-run at each SHA; `TEST=query` goes from 28 to 29 passing with the `$type` case,
and a fifth gate now runs `TEST=query.operands` at 12 passing. The row above records 5/5.

### `P0-A` &mdash; bf/alarms - BF-28, BF-29, BF-31

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/alarms` |
| commit | `5dcf783fdbb20188c378d79121dcbe860425eede` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 3 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/alarms` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `gate-not-met` |
| verdict (measured) | **FAIL** |
| semver | `major` |
| register | `BF-28`, `BF-29`, `BF-31` |

**What an operator sees.** Three alarm fixes. The "insulin reservoir change overdue" reminder could never appear at all and now can. If you list a plugin by its file name in ENABLE - for example "cannulaage" instead of "cage" - Nightscout used to switch it off without telling you, and now says so and names the plugin it thinks you meant. These do not change when any alarm fires, only whether it can. This is not medical advice; if an alarm you rely on has been silent, talk it through with your care team as well as checking your settings.

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/alarms`
  - exit 0
  - intent: bf/alarms has not fallen behind origin/dev
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-tree --write-tree origin/dev bf/alarms >/dev/null`
  - exit 0
  - intent: trial-merge into origin/dev is conflict-free
- **PASS** `[unit]` _(cwd: `externals/work/crm-bf-alarms`)_ `TEST=insulinage npm run test-single`
  - exit 0
  - intent: BF-28, measured. 5 passing, and E3 ABLATED it - with lib/plugins/insulinage.js put back to origin/dev the same file gives 3 passing / 2 failing. Database-free: re-run with the mongo URL pointed at a dead port, still 5 passing.
  - measured: 5 passing, 0 failing, 0 pending
- **PASS** `[unit]` _(cwd: `externals/work/crm-bf-alarms`)_ `TEST=plugins npm run test-single`
  - exit 0
  - intent: BF-29, measured. 13 passing, and E3 ABLATED it - with lib/plugins/index.js put back to origin/dev the same file gives 5 passing / 8 failing. Database-free.
  - measured: 13 passing, 0 failing, 0 pending
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-alarms`)_ `TEST=api.alexa npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-31, the branch's own test for the locale removal. CANNOT BE RUN IN THIS ENVIRONMENT TODAY and that is a fact about the machine, not the branch: crm-bf-alarms/my.test.env names mongod on port 27034 and nothing is listening there (E3 measured every worktree's port; crm-bf-coercion's 27030 is also dead, and crm-bf-food, crm-bf-merge and crm-bf-parms SHARE 27033). It times out in the before-all hook.
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-alarms`)_ `TEST=api.googlehome npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-31's sibling. Same environmental blocker as api.alexa - port 27034 has no mongod.
- **NO-GATE** &mdash; THE GATE THAT WAS HERE - `npm run test:unit` - ASSERTED LESS THAN IT LOOKED LIKE. It resolves to a 44-file brace list, it needs MongoDB (verifyauth x4 and API_SECRET x2 fail without it even on pristine dev), and tests/api.alexa.test.js and tests/api.googlehome.test.js - two of the four files this branch adds - are in NEITHER local script, so it never ran half the branch's own evidence. CI's `test-ci` runs all 159 files and does cover them; a gate that mirrors the local script is weaker than CI, not equal to it.
- **FAIL** `[static]` `test -f externals/work/crm-bf-alarms/node_modules/.cache/_ns_cache/public/js/bundle.app.js`
  - exit 1
  - intent: GT1 found this worktree is the only one missing the client bundle, and that absence produced a seventh test failure that looked like a defect. `npm run bundle` in that worktree fixes it.
- **NO-GATE** &mdash; Nothing exercises the ENABLE warning through a booted plugin registry. GT4 had to hand-reconstruct the plugin-name list to test it, and a reconstructed registry is not the registry. Needs a boot-level test.

### `P0-B` &mdash; bf/cache - T0.2 and T0.3 read-path cost

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/cache` |
| commit | `4f86bab1637e926502c9b02af008cbba4f424c28` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 2 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/cache` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `gate-not-met` |
| verdict (measured) | **PASS** |
| semver | `patch` |
| register | `BF-06`, `BF-07` |

> **CLAIM DIVERGES FROM MEASUREMENT.** state says gate-not-met, but every RUNNABLE gate passed and the failing property sits behind 2 no-gate marker(s): the claim rests on a recorded measurement this run could not reproduce

**What an operator sees.** Pages that read recent glucose values get faster. Nothing you see changes value or meaning.

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/cache`
  - exit 0
  - intent: bf/cache has not fallen behind origin/dev
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-tree --write-tree origin/dev bf/cache >/dev/null`
  - exit 0
  - intent: trial-merge into origin/dev is conflict-free
- **NO-GATE** &mdash; T0.3's stated gate is "the three cache calls under 1 ms per cycle", and it was MEASURED at 3.747 -> 2.657 ms, so the gate is not met. But that measurement is not currently RE-RUNNABLE: the workload that produced those two figures is not recorded anywhere in this repository, and a benchmark written from scratch here would emit a number that looks like the gate's number without being comparable to it. Rule 9 - quote the ordering, and say how it was measured and on what - forbids that, and a fabricated-workload benchmark is the exact laundering this queue exists to prevent. So `gate-not-met` here rests on a RECORDED measurement by a prior agent, not on a live one. Making it live means first writing down the workload; the house pattern to follow is tools/mt-bench/cycle-fix.js, which states its iteration count, its document counts and its comparison arms in the file.
- **NO-GATE** &mdash; The 98% of the remaining cost is devicestatus, whose caller rewrites fields in place. Taking it needs proof that nothing in the plugin tier writes to a device-status document. A grep is not that proof when the failure mode is a field silently vanishing from every API read served out of the cache. There is no test that would catch it.

### `P0-C` &mdash; bf/auth - BF-17 plaintext token, BF-30 throttle key

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/auth` |
| commit | `64db1f35ca3183fe34afe25d256794ab7701df48` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 2 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/auth` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `gate-not-met` |
| verdict (measured) | **FAIL** |
| semver | `major` |
| register | `BF-17`, `BF-30` |

**What an operator sees.** Two security fixes. Editing a subject through the admin page used to write that subject's API access token into the database in readable form, which turned read access to your database into API access; it no longer does. IMPORTANT: tokens already written that way are still in your database - fixing the code does not remove them. Brute-force slowdown on failed logins was keyed on a value the caller could choose, so it never engaged; it now is. Existing access tokens keep working - they are re-derived on every load and are not invalidated by this change.

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/auth`
  - exit 0
  - intent: bf/auth has not fallen behind origin/dev
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-tree --write-tree origin/dev bf/auth >/dev/null`
  - exit 0
  - intent: trial-merge into origin/dev is conflict-free
- **FAIL** `[static]` _(cwd: `externals/work/crm-bf-auth`)_ `grep -nE "console\\.log\\('Loading',[[:space:]]*opts\\)" lib/authorization/storage.js && exit 1 \|\| exit 0`
  - exit 1
  - intent: BF-05's unfixed sibling, a TRACKING gate: it is meant to FAIL while the residual is present, and the describe it replaces said so in as many words. IT DID NOT FAIL. The pattern was `console.log('Loading', opts)` with a space after the comma; the code at storage.js:113 has NO space, so the grep never matched, the `|| exit 0` arm fired, and P0-C reported 3/3 PASS on a property that is false. The pattern is now whitespace-tolerant and this gate is red, which is the honest reading - hence state gate-not-met, not ready-to-push. TWO WAYS TO GREEN, both a human's call: take the one-line removal onto bf/auth, or move the residual to its own queue item and drop the gate from here. DO NOT loosen the pattern again.

  ```
  113:      console.log('Loading',opts);
  ```
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-auth`)_ `TEST=authdelay npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-30, the branch's OWN test, which `npm run test:unit` never ran - tests/authdelay.test.js matches neither local brace list and is one of the 52 files only CI's `test-ci` reaches. 11 passing with MongoDB up on 27031. E3 ABLATED it: with the seven changed lib files put back to origin/dev and lib/server/peer-address.js removed, 2 passing / 9 failing. Needs the database (6 passing / 1 failing against a dead port), so it is integration and honestly so.
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-auth`)_ `TEST=authsubjects npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-17, same story: not in either local script, 8 passing with MongoDB, and 1 passing / 7 failing under the same ablation.
- **NO-GATE** &mdash; `npm run test:unit` was here and was recorded as "361 passing / 0 failing at GT1's measurement". That number is not evidence for this branch: the 44-file brace list contains NEITHER of the two test files bf/auth adds, so the suite could pass in full with every one of these fixes reverted.
- **NO-GATE** &mdash; Nothing checks that the plaintext tokens ALREADY written into existing databases get cleaned up. The code fix does not remove them and there is no migration. This needs an operator-facing remediation note and a script, both of which are P0-C-REMEDIATE.

### `P0-C-REMEDIATE` &mdash; Operator remediation for tokens already stored in plaintext

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `-` |
| commit | `UNRESOLVED` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `-` |
| branch contains base | - |
| commits ahead of base | - |
| package version at branch | `-` |
| published | - |
| state (claimed) | `not-started` |
| verdict (measured) | **UNMEASURED** |
| semver | `n/a` |
| register | `BF-17` |

**What an operator sees.** If you have ever edited a subject through the admin page, a readable copy of that subject's API access token is sitting in your database. Upgrading does not remove it. You will be told how to check, and how to rotate the token if it is there. Anyone who has had read access to your database since then could have used that token.

**Gates, and what each one actually said.**

- **NO-GATE** &mdash; Nothing written yet. The gate this item needs is a script that reports how many subject rows carry a non-derived accessToken, run against a database with one planted, and a control row that has none - so the script is shown to distinguish the two before anyone trusts a zero.

> **UNRESOLVED.** item declares no branch; there is no commit to record. This is a real state (not-started work), not a tool failure.

### `P0-D` &mdash; bf/coercion - schema-driven query type coercion (T0.5)

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/coercion` |
| commit | `b7234753` (tip); `f829ea1156612daa819e803e416a18c17b462ccd` is commit 1 † |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 1 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/coercion` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `ready-to-push` |
| verdict (measured) | **PASS** |
| semver | `minor` |
| register | `BF-02`, `BF-03`, `BF-11`, `BF-32` |

**What an operator sees.** Searches through the API that filter on a number used to compare that number against text, so many of them quietly matched nothing and still answered "OK". They now compare correctly. If you have a saved search, a script or a third-party app that was returning nothing, it may start returning results - that is the fix working, not a new problem.

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/coercion`
  - exit 0
  - intent: bf/coercion has not fallen behind origin/dev
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-tree --write-tree origin/dev bf/coercion >/dev/null`
  - exit 0
  - intent: trial-merge into origin/dev is conflict-free
- **PASS** `[unit]` _(cwd: `externals/work/crm-bf-coercion`)_ `TEST=query npm run test-single`
  - exit 0
  - intent: 28 passing, database-free (measured against a dead mongo port). E3 RE-ABLATED it properly: GT1's control was "the file cannot even LOAD against pristine dev", which is a module-resolution failure, not a behavioural one. Reverting ONLY lib/server/query.js and keeping the new table gives 24 passing / 4 failing on assertions ("expected '1.5' to be 1.5"), which is the control that means something.
  - measured: 28 passing, 0 failing, 0 pending
- **PASS** `[static]` `T=$(mktemp -d) && trap 'rm -rf "$T"' EXIT && python3 -m tools.nsschema.emit.coercion_emit --bundle "$T/emitted.json" >/dev/null && diff -q "$T/emitted.json" externals/work/crm-bf-coercion/lib/server/query-coercion.json`
  - exit 0
  - intent: House style is "emit, then check the emission", and THE GATE THAT WAS HERE DID NOT CHECK IT. `coercion_emit --drift` ends in `return 0` unconditionally: E3 ran it and it exited 0 while printing "DRIFT vs the shipping walkers: 157 disagreements". It also compares against a HARDCODED transcription of origin/dev's walkers, so it says nothing about bf/coercion at all and would have exited 0 with the branch deleted. What is gateable is the emission itself: the table vendored into the branch must be byte-identical to what the emitter produces today, or the branch is shipping a stale generated file.

### `P0-E` &mdash; bf/reads - six read-path fixes, independent of bf/coercion

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/reads` |
| commit | `2ecfeb53ff1e6121ef5f76e1f08e97af1ca6c2fa` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 6 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/reads` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `ready-to-push` |
| verdict (measured) | **PASS** |
| semver | `major` |
| register | `BF-01`, `BF-05`, `BF-13`, `BF-14`, `BF-15`, `BF-33` |

**What an operator sees.** Six fixes to how the API answers read requests. Counting entries that matched a filter returned nothing at all; paging through results could skip or repeat records; asking for a nested field returned an empty answer; and three different ways of asking for "no limit" returned the entire collection instead. CHANGE YOU MAY NOTICE: six spellings of ?count= that used to be accepted now return an error instead of a surprising answer - count=0, count=0x10, count=2.5, count=-3, count=1e2 and count=abc. If a script of yours uses one of those, it will now fail loudly instead of quietly returning the wrong number of records.

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/reads`
  - exit 0
  - intent: bf/reads has not fallen behind origin/dev. THIS REPLACES an is-ancestor check on bf/coercion. The two-deep stack existed only to resolve a CHANGELOG collision; the CHANGELOG edits were stripped by the maintainer's instruction and bf/reads was re-cut directly onto dev, so the old gate now measures a fact that is deliberately false.
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-tree --write-tree origin/dev bf/reads >/dev/null`
  - exit 0
  - intent: trial-merge into origin/dev is conflict-free
- **PASS** `[static]` `node tools/queue/gates/bf-reads-read-contract.js`
  - exit 0
  - intent: THE GATE THAT WAS HERE was `git log --format=%H bf/reads | grep -q .`, which asserts the branch has at least one commit. The E3 audit ran it against origin/dev, origin/master and bf/alarms and it passed for all three. This replaces it with the database-free half of the branch's behaviour, driven through the four pure modules the six fixes live in: parseCount/hasCount/applyCount, the dotted-?fields= projector, the _id tiebreak in the v3 sort chain, and the count path's use of query_for with nothing printed. 8 assertions. Its own negative control is built in - `--rev origin/dev` runs the identical assertions against dev materialised from the object database and gives 4 failing.
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-reads`)_ `TEST=api.count-parameter npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-13/BF-33, 13 passing. CORRECTION TO THE E3 BRIEF, which said this runs "in 2 seconds with no database": it needs MongoDB. Pointed at a dead port it goes to 0 passing / 1 failing, timing out in the before-all hook. It is fast only because a mongod happens to be listening on crm-bf-reads' port 27032.
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-reads`)_ `TEST=api.count-where npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-01, 5 passing. Needs MongoDB.
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-reads`)_ `TEST=api3.fields npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-15, 6 passing. Needs MongoDB (5 of 6 survive without it).
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-reads`)_ `TEST=api3.limit npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-33, 8 passing. Needs MongoDB.
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-reads`)_ `TEST=api3.paging npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-14, 3 passing. Needs MongoDB.
- **NO-GATE** &mdash; THE `npm run test:unit` GATE WAS REMOVED, not downgraded. It was recorded as "361 passing / 0 failing at GT1's measurement", but not one of the five test files this branch adds is in the 44-file brace list - all five are api*/api3* and match test:integration's glob - so the suite could have passed in full with every fix reverted.
- **NO-GATE** &mdash; The ordering constraint inside the branch (BF-05's commit must follow BF-01's) is not machine-checked. After the CHANGELOG strip and the re-cut onto dev the two commits are 3b588098 and 4772b983, so any gate quoting an earlier SHA is stale by construction. A real gate would assert the relative order of the two by subject line, and nobody has written it.
- **NO-GATE** &mdash; The CHANGELOG on this branch lists read routes only, while the ?count= validator covers writes too. Nothing checks a release note against the code it describes.

### `P0-F` &mdash; fix/connect-timer-jitter - BF-34 backoff precedence and start jitter

| | |
|---|---|
| repo | `nightscout-connect` (`externals/nightscout-connect`) |
| branch | `fix/connect-timer-jitter` |
| commit | `c1cce2a2f9623e1164d85b1625a65d27e3116b51` |
| base declared | `b77e5bb` |
| base resolved | `b77e5bb7a08a677f008af93335c04aa726436245` |
| branch contains base | yes |
| commits ahead of base | 1 |
| package version at branch | `0.0.13` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/fix/connect-timer-jitter` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `ready-to-push` |
| verdict (measured) | **PASS** |
| semver | `minor` |
| register | `BF-08`, `BF-34` |

**What an operator sees.** When a CGM vendor's service is refusing requests, Nightscout's connector used to retry roughly 586 times faster than it was configured to, and every account retried at the same instant. It now waits the interval it was told to wait and spreads the retries out. COUNTER-INTUITIVE: after this fix a vendor outage will look like it recovers MORE SLOWLY, because the connector has stopped retrying in a burst that could not have worked. Your data is not arriving any later than it would have; the burst was never getting through. Also, on restart the connector no longer reaches the vendor all at once - both jitter windows default to 0, so nothing changes for anyone who does not set them.

**Gates, and what each one actually said.**

- **PASS** `[unit]` _(cwd: `externals/work/nc-jitter`)_ `npm test`
  - exit 0
  - intent: 135 passing / 0 failing; the connector suite needs no database
- **PASS** `[unit]` _(cwd: `externals/work/nc-jitter`)_ `node -e "const b=require('./lib/backoff.js'); try { b({jitter:'wild'}); process.exit(1); } catch(e) { process.exit(/unknown jitter mode/.test(e.message)?0:1); }"`
  - exit 0
  - intent: the new throw on an unknown jitter mode is the observable half of the precedence fix - if options were still being discarded, the bad mode would never be read and this would not throw
- **NO-GATE** &mdash; Vendor rate limits are unmeasured (EXP-MT-051) and need real credentials, which rule 0 forbids here. T0.4 shipped CONNECT_START_JITTER_MS so the pool CAN be spread - but the window to set it is exactly the number that is unmeasured.

### `P0-G` &mdash; bf/food - BF-16 quick-pick filter, BF-35 bolus calculator chooser

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/food` |
| commit | `73495331e68c4cda3a63e8c047387bdf404b89b0` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 1 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/food` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `ready-to-push` |
| verdict (measured) | **PASS** |
| semver | `minor` |
| register | `BF-16`, `BF-35` |

**What an operator sees.** IMPORTANT. In the bolus calculator, choosing a quick pick could load a DIFFERENT record's food, so the carbohydrate number that went into the insulin calculation came from a record you did not choose, and nothing on screen said so. Picking the last entry in the list could throw an error, and plain foods that are not quick picks appeared in the chooser. This is fixed. Separately, quick picks saved by a JSON client, or saved before the "hidden" setting existed, were missing from the quick-pick list and now appear. If you have used the bolus calculator's quick picks, the amount it suggested may not have matched the food you selected. The calculator is a suggestion tool, not a dosing instruction - please raise this with your care team if you think a past suggestion was wrong. This is not medical advice.

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/food`
  - exit 0
  - intent: bf/food has not fallen behind origin/dev
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-tree --write-tree origin/dev bf/food >/dev/null`
  - exit 0
  - intent: trial-merge into origin/dev is conflict-free
- **PASS** `[unit]` _(cwd: `externals/work/crm-bf-food`)_ `TEST=boluscalc.quickpick npm run test-single`
  - exit 0
  - intent: BF-35's own test, 11 cases. THIS IS THE GATE GT1 WARNED ABOUT: the file is in NEITHER the test:unit nor the test:integration brace list, so a green `npm run test:unit` on this branch is not evidence that BF-35's fix works. Named explicitly here for that reason.
  - measured: 11 passing, 0 failing, 0 pending
- **SKIP** `[integration]` _(cwd: `externals/work/crm-bf-food`)_ `TEST=api.food.quickpicks npm run test-single`
  - kind `integration` not enabled (use --integration)
  - intent: BF-16 over the real HTTP path; needs MongoDB

### `P0-H` &mdash; bf/merge - BF-36 client delta merge reads past the end

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/merge` |
| commit | `b06c6faf882ebd84d627468c75dade0fe1fd01a1` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 1 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/merge` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `ready-to-push` |
| verdict (measured) | **PASS** |
| semver | `patch` |
| register | `BF-36` |

**What an operator sees.** A treatment being removed at the same time as another update arrived could make the page stop updating until you reloaded it. The clock that tells you how old the reading is runs on its own timer, so it would still have gone stale and warned you - the reading shown was never wrong, it just stopped advancing.

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/merge`
  - exit 0
  - intent: bf/merge has not fallen behind origin/dev
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-tree --write-tree origin/dev bf/merge >/dev/null`
  - exit 0
  - intent: trial-merge into origin/dev is conflict-free
- **PASS** `[unit]` _(cwd: `externals/work/crm-bf-merge`)_ `TEST=receiveddata.merge npm run test-single`
  - exit 0
  - intent: in NEITHER local brace list - GT1's finding. Named here so the gate actually runs the file that proves the fix.
  - measured: 12 passing, 0 failing, 0 pending
- **NO-GATE** &mdash; `dataUpdate` still has no try/catch, so the NEXT throw from anywhere in the merge path has the same effect. This branch fixes one throw, not the missing boundary. No test asserts the boundary exists because it does not.

### `P0-I` &mdash; bf/parms - BF-37, BF-38, BF-39

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/parms` |
| commit | `eb0bc918036a7802a0b88156e9722f45fd9107f3` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 3 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/parms` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `ready-to-push` |
| verdict (measured) | **PASS** |
| semver | `patch` |
| register | `BF-37`, `BF-38`, `BF-39` |

**What an operator sees.** A web address with a bare option in it - anything ending in "?", or containing "&&", or a setting with no value such as "?debug" - stopped the page loading entirely, leaving only the loading message. That is fixed. Two smaller fixes go with it: a translation containing ten or more substitutions came out with a stray digit, and an access token belonging to a subject whose name contains an underscore was being corrupted in the address bar (the server was accepting it anyway, so nothing was broken for you).

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/parms`
  - exit 0
  - intent: bf/parms has not fallen behind origin/dev
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-tree --write-tree origin/dev bf/parms >/dev/null`
  - exit 0
  - intent: trial-merge into origin/dev is conflict-free
- **PASS** `[unit]` _(cwd: `externals/work/crm-bf-parms`)_ `TEST=browser-utils.queryparms npm run test-single`
  - exit 0
  - intent: BF-37 and BF-39; in NEITHER local brace list (GT1)
  - measured: 6 passing, 0 failing, 0 pending
- **PASS** `[unit]` _(cwd: `externals/work/crm-bf-parms`)_ `TEST=language npm run test-single`
  - exit 0
  - intent: BF-38. Non-vacuous by GT1's control - 1 failing when the fix is removed from pristine dev code.
  - measured: 17 passing, 0 failing, 0 pending

### `P0-TAG` &mdash; nightscout-connect release/v0.0.14 and tag - prepared, needs a human push

| | |
|---|---|
| repo | `nightscout-connect` (`externals/nightscout-connect`) |
| branch | `release/v0.0.14` |
| commit | `649a7de27b9e869154e718b0ec1d1bfa0388d0d8` |
| base declared | `v0.0.13@b394411` |
| base resolved | `b394411a39236b614beebffabf23ccc04f730a68` |
| branch contains base | yes |
| commits ahead of base | 11 |
| package version at branch | `0.0.14` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/release/v0.0.14` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `ready-to-push` |
| verdict (measured) | **PASS** |
| semver | `minor` |
| register | `BF-08`, `BF-34` |

**What an operator sees.** A new version of the CGM connector. See P0-F for what changes in behaviour.

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/nightscout-connect merge-base --is-ancestor v0.0.13 release/v0.0.14`
  - exit 0
  - intent: v0.0.13 fast-forwards to the release - no divergence to reconcile
- **PASS** `[static]` `test "$(git -C externals/nightscout-connect cat-file -t v0.0.14)" = tag`
  - exit 0
  - intent: v0.0.14 is an ANNOTATED tag, not a lightweight one
- **PASS** `[static]` `test "$(git -C externals/nightscout-connect rev-parse v0.0.14^{commit})" = "$(git -C externals/nightscout-connect rev-parse release/v0.0.14)"`
  - exit 0
  - intent: the tag points at the release branch tip
- **PASS** `[static]` `test "$(git -C externals/nightscout-connect show release/v0.0.14:package.json \| python3 -c 'import json,sys; print(json.load(sys.stdin)["version"])')" = 0.0.14`
  - exit 0
  - intent: package.json version matches the tag
- **SKIP** `[network]` `git -C externals/nightscout-connect ls-remote --tags origin v0.0.14 \| grep -q . && exit 1 \|\| exit 0`
  - kind `network` not enabled (use --network)
  - intent: RULE 0. This gate PASSES only while the tag is NOT on origin. It is the machine-checkable form of "nothing has been pushed". Read-only.
- **NO-GATE** &mdash; Whether all seven connector commits are the RIGHT content for a release is a release-content decision, not a mechanical bump, and it is the maintainer's. Nothing can gate it.

### `P0-PIN` &mdash; bf/connect-pin - move dev's connector pin to the v0.0.14 tarball

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/connect-pin` |
| commit | `0807eb1c184353792e95e56b0869e42a774732ff` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 1 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/connect-pin` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `blocked` |
| verdict (measured) | **PASS** |
| semver | `patch` |

**What an operator sees.** Nightscout picks up the new connector. Three fixes that keep CGM vendor credentials and patient data out of the log file come with it. Those fixes matter most exactly when you turn debug logging on to diagnose a problem - today's release narrows WHEN the leak happens but does not stop it happening then.

**Gates, and what each one actually said.**

- **PASS** `[static]` _(cwd: `externals/work/crm-bf-connect-pin`)_ `grep -q 'archive/refs/tags/v0.0.14.tar.gz' package.json`
  - exit 0
  - intent: package.json points at the v0.0.14 tag tarball
- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official merge-base --is-ancestor origin/dev bf/connect-pin`
  - exit 0
  - intent: bf/connect-pin has not fallen behind origin/dev
- **PASS** `[static]` _(cwd: `externals/work/crm-bf-connect-pin`)_ `grep -q '234d47c85510a77f07b3be0d2c026dd0272715d6' package-lock.json`
  - exit 0
  - intent: DELIBERATELY INVERTED. This gate passes while the lockfile is STILL on the old SHA. The lock's integrity is a hash over a tarball GitHub does not generate until the tag is pushed; a hash invented locally would break `npm ci` for everyone. Leaving it stale makes `npm ci` fail LOUDLY as out-of-sync, which is the correct failure. When P0-LOCK is done this gate SHOULD go red - that is the handoff signal.
- **NO-GATE** &mdash; Nothing can verify the tarball's integrity hash before the tag exists on GitHub. That is the whole reason P0-LOCK is a separate item.

### `P0-LOCK` &mdash; Regenerate package-lock.json after the v0.0.14 tag is pushed

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `bf/connect-pin` |
| commit | `0807eb1c184353792e95e56b0869e42a774732ff` |
| base declared | `bf/connect-pin@0807eb1c` |
| base resolved | `0807eb1c184353792e95e56b0869e42a774732ff` |
| branch contains base | yes |
| commits ahead of base | 0 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/bf/connect-pin` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `blocked` |
| verdict (measured) | **FAIL** |
| semver | `n/a` |

**What an operator sees.** Nothing you see. Until this is done, `npm ci` fails with an out-of-sync error - which is intentional and correct, not a bug.

**Gates, and what each one actually said.**

- **FAIL** `[static]` _(cwd: `externals/work/crm-bf-connect-pin`)_ `grep -q 'archive/refs/tags/v0.0.14.tar.gz' package-lock.json`
  - exit 1
  - intent: the lockfile agrees with package.json. FAILS today, on purpose, and must not be made to pass locally - see P0-PIN's inverted gate.
- **SKIP** `[network]` _(cwd: `externals/work/crm-bf-connect-pin`)_ `npm ci --dry-run`
  - kind `network` not enabled (use --network)
  - intent: npm ci resolves; requires the pushed tag to exist on GitHub

### `P0-T01` &mdash; T0.1 - PR

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `fix/quadratic-treatment-processing` |
| commit | `dfe2753dcc1cf39c3e3abfc7bf5c844131480acb` |
| base declared | `origin/dev` |
| base resolved | `a8888f0d9facb8a9bb54c2ab15333f3f241b45bf` |
| branch contains base | yes |
| commits ahead of base | 1 |
| package version at branch | `15.0.9` |
| published | local remote-tracking refs (no network) &mdash; remote-tracking ref `refs/remotes/origin/fix/quadratic-treatment-processing` does not exist. **The remote itself was not contacted.** |
| state (claimed) | `in-flight-upstream` |
| verdict (measured) | **UNMEASURED** |
| semver | `patch` |

**What an operator sees.** Sites with a lot of treatment records load faster. Nothing you see changes value or meaning.

**Gates, and what each one actually said.**

- **SKIP** `[network]` `git -C externals/cgm-remote-monitor-official ls-remote --heads origin bewest/wip/optimize-treatment-processing \| grep -q dfe2753d`
  - kind `network` not enabled (use --network)
  - intent: the one Phase 0 branch that IS legitimately pushed. GT1 verified this live. Read-only.
- **NO-GATE** &mdash; PR merge state is upstream's and cannot be gated from here without a GitHub API call. This item is tracked, not driven.

### `FU-LIMIT` &mdash; Follow-up 2 - the limit rule is written twice, and that is the root cause

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `-` |
| commit | `UNRESOLVED` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `-` |
| branch contains base | - |
| commits ahead of base | - |
| package version at branch | `-` |
| published | - |
| state (claimed) | `blocked` |
| verdict (measured) | **FAIL** |
| semver | `patch` |

**Gates, and what each one actually said.**

- **PASS** `[static]` `git -C externals/cgm-remote-monitor-official cat-file -e origin/dev:lib/server/count.js 2>/dev/null && git -C externals/cgm-remote-monitor-official show origin/dev:lib/api3/generic/collection.js \| grep -q "self.parseLimit" && exit 1 \|\| exit 0`
  - exit 0
  - intent: CONTROL, and it PASSES - and it carries a correction. The sequencing document says the rule is written twice, in lib/server/count.js and v3's parseLimit. THAT FILE DOES NOT EXIST ON origin/dev; bf/reads creates it. So the duplication does not exist yet and this follow-up is created by P0-E landing.
- **FAIL** `[static]` `git -C externals/cgm-remote-monitor-official cat-file -e bf/reads:lib/server/count.js 2>/dev/null && git -C externals/cgm-remote-monitor-official show bf/reads:lib/api3/generic/collection.js \| grep -q "self.parseLimit" && exit 1 \|\| exit 0`
  - exit 1
  - intent: FAILS on bf/reads, where both readings of the rule are present. Paired with the control above, a red here is the duplication and not the command - the identical command run against origin/dev exits 0.
- **NO-GATE** &mdash; Nothing asserts that the two implementations AGREE while they both exist. A differential test - the same limit value through both paths, including 0, a negative, a non-numeric and a value above the maximum - is what would make the duplication safe until it is removed, and it does not exist. Two readings of one rule is the root cause of this whole family, so leaving it duplicated is a debt with a name.

> **UNRESOLVED.** item declares no branch; there is no commit to record. This is a real state (not-started work), not a tool failure.

### `FU-RESIDUALS` &mdash; Follow-ups 3, 4, 7 - three named residuals beside branches already prepared

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `-` |
| commit | `UNRESOLVED` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `-` |
| branch contains base | - |
| commits ahead of base | - |
| package version at branch | `-` |
| published | - |
| state (claimed) | `gate-not-met` |
| verdict (measured) | **FAIL** |
| semver | `patch` |

**What an operator sees.** One of these three is visible to you. If an Amazon Alexa request arrives that Nightscout does not recognise, Nightscout answers nothing at all and the request hangs until Alexa gives up, rather than saying it did not understand. The other two are internal - a check that always answers "yes" but that nothing currently asks, and a leftover log line that prints request details to the server log.

**Gates, and what each one actually said.**

- **FAIL** `[static]` `git -C externals/cgm-remote-monitor-official show origin/dev:lib/plugins/index.js \| grep -q "return (p !== null)" && exit 1 \|\| exit 0`
  - exit 1
  - intent: Follow-up 3. FAILS today - plugins.isPluginEnabled compares find()'s result with !== null, and find() returns UNDEFINED when it misses, so the function always returns true. No caller today, which is why it has no register id, but it is what the next instrument will reach for.
- **FAIL** `[static]` `git -C externals/cgm-remote-monitor-official show origin/dev:lib/authorization/storage.js \| grep -qE "console\\.log\\('Loading',[[:space:]]*opts\\)" && exit 1 \|\| exit 0`
  - exit 1
  - intent: Follow-up 4. FAILS today. BF-05's unfixed sibling - the same shape, a different file. THE PATTERN IS THE STORY OF THIS GATE. The shipping source is console.log('Loading',opts) with NO SPACE after the comma. The register (L1005), the sequencing document and P0-C's gate all quote it WITH a space, and the first draft of this gate copied that quotation - so it searched for a string that is not in the file, matched nothing, and PASSED while the defect was present. A vacuous green, caught by running it. The same defect was found concurrently in P0-C's copy of the gate by another session and fixed there; this is the same space-tolerant pattern, so the two cannot diverge again. :84 AND :113 ARE BOTH RIGHT - storage.js:84 on origin/dev, :113 on bf/auth. The sequencing document's "measured, it is :113, not :84" is true of bf/auth and false of dev; neither number names its ref. This gate measures origin/dev, which is where the fix has to land.
- **FAIL** `[static]` `bash -c 's=$(git -C externals/cgm-remote-monitor-official show origin/dev:lib/api/alexa/index.js); echo "$s" \| grep -q "switch (req.body.request.type)" \|\| exit 1; echo "$s" \| grep -q "default:"'`
  - exit 1
  - intent: Follow-up 7. FAILS today. The switch on request.type has no default, so an unrecognised type calls neither res.json nor next() and the request hangs until the client times out. The first clause is the control - it asserts the switch is still there, so a red result cannot come from the file having been restructured. Measured: 0 occurrences of "default:" in the file. googlehome has no switch at all, so this is alexa-only.
- **NO-GATE** &mdash; Reachability of follow-up 7 is not measured. It needs an Alexa request type outside SessionEndedRequest, LaunchRequest and IntentRequest, and nothing here enumerates what Amazon can send.

> **UNRESOLVED.** item declares no branch; there is no commit to record. This is a real state (not-started work), not a tool failure.

### `FU-HYGIENE` &mdash; Follow-ups 9, 10 - the two audits that have no instrument

| | |
|---|---|
| repo | `cgm-remote-monitor` (`externals/cgm-remote-monitor-official`) |
| branch | `-` |
| commit | `UNRESOLVED` |
| base declared | `origin/dev@a8888f0d` |
| base resolved | `-` |
| branch contains base | - |
| commits ahead of base | - |
| package version at branch | `-` |
| published | - |
| state (claimed) | `not-started` |
| verdict (measured) | **UNMEASURED** |
| semver | `n/a` |

**Gates, and what each one actually said.**

- **NO-GATE** &mdash; Follow-up 9 - audit the suppressions OUTSIDE lib/: detect-non-literal-fs-filename, detect-possible-timing-attacks, no-cond-assign. There is no instrument because the audit IS the work: object-injection's 34 suppressed lines yielded five real defects (BF-35, BF-36, BF-37, BF-38 and BF-39 - the register's own tally of four is stale), and the same reasoning applies to each remaining category. What could be gated afterwards is a rule that no NEW suppression is added without an entry, and that rule does not exist.
- **NO-GATE** &mdash; Follow-up 10 - jsdom test hygiene has no enforcement. A suite that sets global.window or global.document must restore them in afterEach or it breaks browser-settings.test.js later in the same run. hashauth.modern.test.js does the restore; nothing requires it, and the failure lands in a DIFFERENT file from the one that caused it, which is the shape that costs the most review time. The instrument is a harness-level check, and cut 1 retires jsdom entirely, so it should be decided against the release train rather than built twice.

> **UNRESOLVED.** item declares no branch; there is no commit to record. This is a real state (not-started work), not a tool failure.

---

## Register entries this release touches, and each entry's basis

Rule 3 of this programme: read-derived versus reproduced. Measured across the programme, every
register claim that had to be retracted was derived from READING source; not one that began
with a REPRODUCTION has been. So this table labels each entry from the words in its own
register row, quotes the row, and names the phrase that decided it. `unstated` means the
register does not say -- it is not a guess, and it is a finding about the register, not about
the defect.

| entry | basis | closure state | carried by | severity | where |
|---|---|---|---|---|---|
| `BF-01` | **reproduced** | `claimed-fixed-gates-pass` | `P0-E` | high — wrong answer, HTTP 200 | lib/server/aggregate.js:21 |
| `BF-02` | **unstated** | `claimed-fixed-gates-pass` | `P0-D` | high — wrong answer, HTTP 200 | lib/server/treatments.js:259-266 |
| `BF-03` | **unstated** | `claimed-fixed-gates-pass` | `P0-D` | high — wrong answer, HTTP 200 | lib/server/query.js walker, per-collection |
| `BF-05` | **unstated** | `claimed-fixed-gates-pass` | `P0-E` | medium — log noise, filter contents to stdout | lib/server/aggregate.js:30-31 |
| `BF-06` | **unstated** | `claimed-fixed-gates-pass` | `P0-B` | medium — CPU | lib/server/cache.js:73-76 |
| `BF-07` | **unstated** | `claimed-fixed-gates-pass` | `P0-B` | medium — 65 % of the load cycle | lib/server/cache.js:81 |
| `BF-08` | **unstated** | `claimed-fixed-gates-pass` | `P0-F`, `P0-TAG` | medium — thundering herd on restart | nightscout-connect lib/machines/cycle.js Init, and run() |
| `BF-11` | **unstated** | `claimed-fixed-gates-pass` | `P0-D` | high — wrong answer, HTTP 200 | lib/server/treatments.js:259-266 |
| `BF-13` | **reproduced** | `claimed-fixed-gates-pass` | `P0-E` | high — silent data loss on a read | lib/api3/generic/search/input.js parseSort |
| `BF-14` | **reproduced** | `claimed-fixed-gates-pass` | `P0-E` | high — on PostgreSQL an empty 200 on a glucose read; unbounded read on MongoDB | lib/server/entries.js:56 + 4 siblings |
| `BF-15` | **reproduced** | `claimed-fixed-gates-pass` | `P0-E` | medium — silently empty response to a valid request | lib/api3/shared/fieldsProjector.js applyProjection |
| `BF-16` | **reproduced** | `claimed-fixed-gates-pass` | `P0-G` | medium — a JSON writer's quick picks vanish from /api/v1/food/quickpicks, which has no in-tree consumer; restoreBoolValue un-hides a boolean-hidden pick in the editor | lib/server/food.js listquickpicks + lib/food/food.js:69 restoreBoolValue |
| `BF-17` | **reproduced** | `claimed-fixed-but-a-gate-failed` | `P0-C`, `P0-C-REMEDIATE` | high — turns read access to the database into API access; no key required | lib/authorization/endpoints.js:38-42 + lib/admin_plugins/subjects.js:43 + lib/authorization/storage.js save |
| `BF-28` | **unstated** | `claimed-fixed-but-a-gate-failed` | `P0-A` | medium — a site-change reminder that silently never arrives, and a severity that is wrong the whole time | lib/plugins/insulinage.js:92 |
| `BF-29` | **unstated** | `claimed-fixed-but-a-gate-failed` | `P0-A` | medium — an operator believes an alarm plugin is on when it is off | lib/plugins/index.js:140 |
| `BF-30` | **reproduced** | `claimed-fixed-but-a-gate-failed` | `P0-C` | high — restores unthrottled guessing against API_SECRET and tokens | lib/authorization/delaylist.js + the un-whitelisted forwarded-for call in lib/authorization/index.js:9-12 (not TRUST_PROXY, which does not exist on dev) |
| `BF-31` | **unstated** | `claimed-fixed-but-a-gate-failed` | `P0-A` | low–medium — gated on the assistant plugin being enabled; reaches the assistant's own answers, not alarm text | lib/api/googlehome/index.js:27 and lib/api/alexa/index.js:28 + the one language instance at lib/server/server.js:34 |
| `BF-32` | **unstated** | `claimed-fixed-gates-pass` | `P0-D` | low — re-graded from medium: a 500 on $regex, not a wrong answer on $exists. The fix remains right; its stated reason was wrong | lib/server/query.js walk_prop |
| `BF-33` | **reproduced** | `claimed-fixed-gates-pass` | `P0-E` | high — unbounded read, HTTP 200, and the ceiling that exists to prevent it is bypassed | lib/api3/generic/collection.js parseLimit |
| `BF-34` | **unstated** | `claimed-fixed-gates-pass` | `P0-F`, `P0-TAG` | high — a vendor that is refusing requests gets hammered by every account at once, which is when it can least afford it | nightscout-connect lib/backoff.js |
| `BF-35` | **reproduced** | `claimed-fixed-gates-pass` | `P0-G` | high — the carbs that reach the insulin calculation come from a record the user did not choose, with no error shown. Regression from 3457de5b (2017) | lib/client/boluscalc.js loadFoodQuickpicks |
| `BF-36` | **reproduced** | `claimed-fixed-gates-pass` | `P0-H` | medium — availability, not a wrong reading: the time-ago watchdog is on its own timer and still marks the page stale | lib/client/receiveddata.js mergeTreatmentUpdate |
| `BF-37` | **reproduced** | `claimed-fixed-gates-pass` | `P0-I` | medium–high — total, silent failure to load, on a URL shape anyone can produce | lib/client/browser-utils.js queryParms |
| `BF-38` | **reproduced** | `claimed-fixed-gates-pass` | `P0-I` | low — latent: no shipped catalogue uses more than %3 | lib/language.js translate |
| `BF-39` | **reproduced** | `claimed-fixed-gates-pass` | `P0-I` | low — a real corruption absorbed by a leniency nobody chose | lib/client/browser-utils.js queryParms |

**`BF-01`** &mdash; GET /api/v1/count/entries/where silently matches nothing

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (bf/reads 4a398d47); reproduced live; count/treatments/where was affected too
- carried by: `P0-E` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-02`** &mdash; insulin/carbs query bounds truncated by parseInt

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (T0.5, bf/coercion 88d1f8a4)
- carried by: `P0-D` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-03`** &mdash; Numeric filters on devicestatus, activity, food, profile match nothing

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 for devicestatus + profile (T0.5, bf/coercion 88d1f8a4); food and activity misfiled, see detail
- carried by: `P0-D` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-05`** &mdash; Unguarded console.log of every count query on the request path

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (bf/reads c8fb536b) — deleted, not gated; the module has no env handle
- carried by: `P0-E` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-06`** &mdash; /api/v1/entries?count=10 costs 42× a typed read

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (T0.2, bf/cache ddcdb1a8); 0.837 → 0.025 ms, response asserted identical over HTTP
- carried by: `P0-B` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-07`** &mdash; cache.insertData JSON round-trips the whole retained array

- basis: **unstated**
- register status, verbatim: partly fixed 2026-09-15 (T0.3, bf/cache 4f86bab1); 3.75 → 2.66 ms per cycle — devicestatus keeps its clone on purpose, see detail
- carried by: `P0-B` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-08`** &mdash; nightscout-connect actors have no start jitter — a pool reaches the vendor inside one second on every restart. The interval half of this entry was wrong: all four drivers already jitter the aligned path by 18 s

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (T0.4, fix/connect-timer-jitter c1cce2a); measured at 400 actors, busiest second 400 → 15
- carried by: `P0-F` (PASS), `P0-TAG` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-11`** &mdash; treatments.duration and rate have no walker entry — temp-basal filters match nothing

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (T0.5, bf/coercion 88d1f8a4)
- carried by: `P0-D` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-13`** &mdash; API v3 skip/limit paging silently loses and duplicates documents when the whole sort chain ties

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (bf/reads 399dc283); reproduced through the v3 HTTP path
- carried by: `P0-E` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-14`** &mdash; API v1 ?count=0 (and -3, 1e2) reaches the driver unvalidated — .limit(0) means *unbounded*

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (bf/reads 1640b64b); reproduced live; the v3 validation it says to copy is itself defective — see BF-33
- carried by: `P0-E` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-15`** &mdash; API v3 ?fields=<dotted.path> returns an empty document with HTTP 200

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (bf/reads ba70f1fc); reproduced live
- carried by: `P0-E` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-16`** &mdash; Food quick-pick hidden filter compares to the string 'false'; the field has no declared type and its stored type depends on the request's content type. The "wrong order in the built-in editor" half of this entry was wrong — the editor re-sorts numerically itself and does not use the endpoint

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (bf/food 73495331); reproduced live, both spellings written over HTTP
- carried by: `P0-G` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-17`** &mdash; Editing a subject through the stock admin UI persists the API access token in plaintext, into a field the server otherwise only derives

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 — bf/auth 64db1f35; reproduced live; existing rows still hold tokens, see the report
- carried by: `P0-C` (FAIL), `P0-C-REMEDIATE` (UNMEASURED)
- closure state: `claimed-fixed-but-a-gate-failed`; closed for operators: **no**

**`BF-28`** &mdash; insulinage's URGENT branch is unreachable — it compares against insulinInfo.urgent, which is never assigned, where all three sibling plugins use prefs.urgent. "Insulin reservoir change overdue!" can never fire, and the reported level stays WARN for as long as the reservoir is overdue

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (bf/alarms 8714093b)
- carried by: `P0-A` (FAIL)
- closure state: `claimed-fixed-but-a-gate-failed`; closed for operators: **no**

**`BF-29`** &mdash; An unknown name in ENABLE is silently ignored — matching is against plugin.name (bwp, cage, iage, sage, bage, basal — six, not five), not the file name. An operator who writes ENABLE=cannulaage gets no plugin and no warning

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (bf/alarms 99e46a52)
- carried by: `P0-A` (FAIL)
- closure state: `claimed-fixed-but-a-gate-failed`; closed for operators: **no**

**`BF-30`** &mdash; The auth-failure delay is keyed on a client-controlled value, so brute-force throttling never engages

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 — bf/auth a26ba416; reproduced live; the register's preferred fix was refuted by measurement
- carried by: `P0-C` (FAIL)
- closure state: `claimed-fixed-but-a-gate-failed`; closed for operators: **no**

**`BF-31`** &mdash; A Google Home or Alexa request re-points the shared language instance and moment's global locale for the whole process, until something changes it back. Measured 2026-09-15: it does *not* change alarm text — the catalogue is read once at boot and never reloaded

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (bf/alarms 5dcf783f)
- carried by: `P0-A` (FAIL)
- closure state: `claimed-fixed-but-a-gate-failed`; closed for operators: **no**

**`BF-32`** &mdash; Query coercion was applied to operands that are not field values, so find[sgv][$exists]=true became {$exists: NaN} and find[notes][$regex]=ab became {$regex: NaN}. The consequence this entry claimed is refuted — {$exists: NaN} is read by MongoDB as true, not false, so $exists=true was already answering correctly by accident. What the coercion actually did on the ten walker fields is turn a $regex into a server error. The residual $exists=false defect that survives the fix is BF-40

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (found during T0.5, bf/coercion 88d1f8a4); claim refuted 2026-09-15, see detail and BF-40
- carried by: `P0-D` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-33`** &mdash; API v3 ?limit=0x10 passes the API3_MAX_LIMIT check as 16 and reaches the driver as .limit(0) — *no limit*; ?limit=1e2 returns one document

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (bf/reads ea50cf52); found while fixing BF-14, reproduced live
- carried by: `P0-E` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-34`** &mdash; backoff() merges its options as { ...config, ...defaults }, so every value any caller passes is discarded. All five vendor sources configure a 2.5-minute retry interval and every one of them gets the 256 ms default — 586× faster — and use_random_slot is forced false, so a pool that fails together retries in exact lockstep

- basis: **unstated**
- register status, verbatim: fixed 2026-09-15 (found during T0.4, fix/connect-timer-jitter c1cce2a); 100 actors delivered the same 800 requests across 3 s before and 67 s after
- carried by: `P0-F` (PASS), `P0-TAG` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-35`** &mdash; The bolus calculator's quick-pick chooser builds its <option> list from the whole food collection but resolves the selection against the filtered quick-pick array. Picking one quick pick loads a different one's foods; the last entry throws; plain foods appear in the chooser

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (found during BF-16, bf/food 73495331); reproduced in jsdom, ablated six ways
- carried by: `P0-G` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-36`** &mdash; The client's delta merge captured the cached array's length once and then spliced that array, so a remove followed by an item matching nothing read past the end and threw. The throw escapes into dataUpdate, which has no try/catch — the page stops advancing until reloaded

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (found by auditing the suppressions BF-35 turned up under, bf/merge b06c6faf); reproduced directly, ablated against the shipped shape
- carried by: `P0-H` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-37`** &mdash; queryParms() reads [1] of each key=value split without checking one exists, so a valueless parameter — ?debug, a trailing &, &&, a lone ? — throws. It is the first statement of client.init, so the page stops loading with nothing on screen but the loading message

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (suppression audit, bf/parms 522c6ffb); reproduced directly
- carried by: `P0-I` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-38`** &mdash; Translation substitution loops forwards over %1…%n; %1 is a prefix of %10, so the first pass rewrites the %1 inside %10 and leaves a stray 0. Same prefix-order trap as sorting a text position

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (suppression audit, bf/parms c9a7a21c); reproduced directly
- carried by: `P0-I` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

**`BF-39`** &mdash; queryParms() replaced _ with a space, corrupting every access token whose subject name contains one. Measured to have no live effect: findSubject matches on the last --separated segment and ignores the abbreviated name the corruption lands in

- basis: **reproduced** (matched 'reproduc')
- register status, verbatim: fixed 2026-09-15 (bf/parms eb0bc918); reproduced against a live instance, both spellings authorise
- carried by: `P0-I` (PASS)
- closure state: `claimed-fixed-gates-pass`; closed for operators: **no**

---

## Test evidence

The exact command, where it ran, and what it said. A command that was not run in this capture is listed with outcome `SKIP` and no summary &mdash; it is not a pass.

| item | kind | command | cwd | outcome | summary |
|---|---|---|---|---|---|
| `P0-A` | unit | `TEST=insulinage npm run test-single` | `externals/work/crm-bf-alarms` | **PASS** | 5 passing, 0 failing, 0 pending |
| `P0-A` | unit | `TEST=plugins npm run test-single` | `externals/work/crm-bf-alarms` | **PASS** | 13 passing, 0 failing, 0 pending |
| `P0-A` | integration | `TEST=api.alexa npm run test-single` | `externals/work/crm-bf-alarms` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-A` | integration | `TEST=api.googlehome npm run test-single` | `externals/work/crm-bf-alarms` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-C` | integration | `TEST=authdelay npm run test-single` | `externals/work/crm-bf-auth` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-C` | integration | `TEST=authsubjects npm run test-single` | `externals/work/crm-bf-auth` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-D` | unit | `TEST=query npm run test-single` | `externals/work/crm-bf-coercion` | **PASS** | 28 passing, 0 failing, 0 pending |
| `P0-E` | integration | `TEST=api.count-parameter npm run test-single` | `externals/work/crm-bf-reads` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-E` | integration | `TEST=api.count-where npm run test-single` | `externals/work/crm-bf-reads` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-E` | integration | `TEST=api3.fields npm run test-single` | `externals/work/crm-bf-reads` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-E` | integration | `TEST=api3.limit npm run test-single` | `externals/work/crm-bf-reads` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-E` | integration | `TEST=api3.paging npm run test-single` | `externals/work/crm-bf-reads` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-F` | unit | `npm test` | `externals/work/nc-jitter` | **PASS** | exit 0 &mdash; last line: `ℹ duration_ms 277.22902` |
| `P0-F` | unit | `node -e "const b=require('./lib/backoff.js'); try { b({jitter:'wild'}); process.exit(1); } catch(e) { process.exit(/unknown jitter mode/.test(e.message)?0:1); }"` | `externals/work/nc-jitter` | **PASS** | exit 0 &mdash; last line: `` |
| `P0-G` | unit | `TEST=boluscalc.quickpick npm run test-single` | `externals/work/crm-bf-food` | **PASS** | 11 passing, 0 failing, 0 pending |
| `P0-G` | integration | `TEST=api.food.quickpicks npm run test-single` | `externals/work/crm-bf-food` | **SKIP** | _not run: kind `integration` not enabled (use --integration)_ |
| `P0-H` | unit | `TEST=receiveddata.merge npm run test-single` | `externals/work/crm-bf-merge` | **PASS** | 12 passing, 0 failing, 0 pending |
| `P0-I` | unit | `TEST=browser-utils.queryparms npm run test-single` | `externals/work/crm-bf-parms` | **PASS** | 6 passing, 0 failing, 0 pending |
| `P0-I` | unit | `TEST=language npm run test-single` | `externals/work/crm-bf-parms` | **PASS** | 17 passing, 0 failing, 0 pending |

---

## What was NOT verified

This section is mandatory and the generator refuses to write a record whose gaps section is
empty. It is assembled mechanically from the same data as everything above -- no-gate markers
from the manifest, skips and absent instruments from the runner, bases from the register
parse, scope from arithmetic on the selection -- because a hand-written gaps section records
only what somebody remembered was missing.

**76 entries.**

| category | n |
|---|---|
| `branch-unresolved` | 4 |
| `capture-environment` | 1 |
| `claim-diverges-from-measurement` | 1 |
| `gate-not-run` | 13 |
| `item-unmeasured` | 3 |
| `no-instrument-declared` | 19 |
| `out-of-scope` | 1 |
| `publication-unverified` | 13 |
| `record-method` | 3 |
| `register-basis-unstated` | 12 |
| `register-entry-not-demonstrated` | 5 |
| `register-untouched` | 1 |

### `branch-unresolved`

- **P0-C-REMEDIATE** &mdash; Could not resolve the work to a commit: item declares no branch; there is no commit to record. This is a real state (not-started work), not a tool failure.
  - An item whose branch does not resolve has no SHA in this record, so nothing about its content was verified here.
- **FU-LIMIT** &mdash; Could not resolve the work to a commit: item declares no branch; there is no commit to record. This is a real state (not-started work), not a tool failure.
  - An item whose branch does not resolve has no SHA in this record, so nothing about its content was verified here.
- **FU-RESIDUALS** &mdash; Could not resolve the work to a commit: item declares no branch; there is no commit to record. This is a real state (not-started work), not a tool failure.
  - An item whose branch does not resolve has no SHA in this record, so nothing about its content was verified here.
- **FU-HYGIENE** &mdash; Could not resolve the work to a commit: item declares no branch; there is no commit to record. This is a real state (not-started work), not a tool failure.
  - An item whose branch does not resolve has no SHA in this record, so nothing about its content was verified here.

### `capture-environment`

- **control surface** &mdash; The control-surface working tree was NOT clean at capture (21 uncommitted path(s)).
  - The inputs are digested by content (manifest_sha256, register_sha256) so the record is still re-derivable, but the inputs were not committed when it was taken.

### `claim-diverges-from-measurement`

- **P0-B** &mdash; state says gate-not-met, but every RUNNABLE gate passed and the failing property sits behind 2 no-gate marker(s): the claim rests on a recorded measurement this run could not reproduce
  - The manifest state is a claim; the gate result is the measurement. Where they disagree the record carries both.

### `gate-not-run`

- **P0-A gates[4]** &mdash; TEST=api.alexa npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-A gates[5]** &mdash; TEST=api.googlehome npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-C gates[3]** &mdash; TEST=authdelay npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-C gates[4]** &mdash; TEST=authsubjects npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-E gates[3]** &mdash; TEST=api.count-parameter npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-E gates[4]** &mdash; TEST=api.count-where npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-E gates[5]** &mdash; TEST=api3.fields npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-E gates[6]** &mdash; TEST=api3.limit npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-E gates[7]** &mdash; TEST=api3.paging npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-G gates[3]** &mdash; TEST=api.food.quickpicks npm run test-single -- NOT RUN in this capture.
  - kind `integration` not enabled (use --integration) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-TAG gates[4]** &mdash; git -C externals/nightscout-connect ls-remote --tags origin v0.0.14 | grep -q . && exit 1 || exit 0 -- NOT RUN in this capture.
  - kind `network` not enabled (use --network) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-LOCK gates[1]** &mdash; npm ci --dry-run -- NOT RUN in this capture.
  - kind `network` not enabled (use --network) This capture enabled kinds static,unit. A skipped gate is not a pass.
- **P0-T01 gates[0]** &mdash; git -C externals/cgm-remote-monitor-official ls-remote --heads origin bewest/wip/optimize-treatment-processing | grep -q dfe2753d -- NOT RUN in this capture.
  - kind `network` not enabled (use --network) This capture enabled kinds static,unit. A skipped gate is not a pass.

### `item-unmeasured`

- **P0-C-REMEDIATE** &mdash; Nothing ran for this item at all; it appears in the record with no measurement behind it.
  - Every gate it carries is a no-gate marker or was skipped. Never read UNMEASURED as green.
- **P0-T01** &mdash; Nothing ran for this item at all; it appears in the record with no measurement behind it.
  - Every gate it carries is a no-gate marker or was skipped. Never read UNMEASURED as green.
- **FU-HYGIENE** &mdash; Nothing ran for this item at all; it appears in the record with no measurement behind it.
  - Every gate it carries is a no-gate marker or was skipped. Never read UNMEASURED as green.

### `no-instrument-declared`

- **P0-A gates[6]** &mdash; No gate exists for this property; the manifest records why.
  - THE GATE THAT WAS HERE - `npm run test:unit` - ASSERTED LESS THAN IT LOOKED LIKE. It resolves to a 44-file brace list, it needs MongoDB (verifyauth x4 and API_SECRET x2 fail without it even on pristine dev), and tests/api.alexa.test.js and tests/api.googlehome.test.js - two of the four files this branch adds - are in NEITHER local script, so it never ran half the branch's own evidence. CI's `test-ci` runs all 159 files and does cover them; a gate that mirrors the local script is weaker than CI, not equal to it.
- **P0-A gates[8]** &mdash; No gate exists for this property; the manifest records why.
  - Nothing exercises the ENABLE warning through a booted plugin registry. GT4 had to hand-reconstruct the plugin-name list to test it, and a reconstructed registry is not the registry. Needs a boot-level test.
- **P0-B gates[2]** &mdash; No gate exists for this property; the manifest records why.
  - T0.3's stated gate is "the three cache calls under 1 ms per cycle", and it was MEASURED at 3.747 -> 2.657 ms, so the gate is not met. But that measurement is not currently RE-RUNNABLE: the workload that produced those two figures is not recorded anywhere in this repository, and a benchmark written from scratch here would emit a number that looks like the gate's number without being comparable to it. Rule 9 - quote the ordering, and say how it was measured and on what - forbids that, and a fabricated-workload benchmark is the exact laundering this queue exists to prevent. So `gate-not-met` here rests on a RECORDED measurement by a prior agent, not on a live one. Making it live means first writing down the workload; the house pattern to follow is tools/mt-bench/cycle-fix.js, which states its iteration count, its document counts and its comparison arms in the file.
- **P0-B gates[3]** &mdash; No gate exists for this property; the manifest records why.
  - The 98% of the remaining cost is devicestatus, whose caller rewrites fields in place. Taking it needs proof that nothing in the plugin tier writes to a device-status document. A grep is not that proof when the failure mode is a field silently vanishing from every API read served out of the cache. There is no test that would catch it.
- **P0-C gates[5]** &mdash; No gate exists for this property; the manifest records why.
  - `npm run test:unit` was here and was recorded as "361 passing / 0 failing at GT1's measurement". That number is not evidence for this branch: the 44-file brace list contains NEITHER of the two test files bf/auth adds, so the suite could pass in full with every one of these fixes reverted.
- **P0-C gates[6]** &mdash; No gate exists for this property; the manifest records why.
  - Nothing checks that the plaintext tokens ALREADY written into existing databases get cleaned up. The code fix does not remove them and there is no migration. This needs an operator-facing remediation note and a script, both of which are P0-C-REMEDIATE.
- **P0-C-REMEDIATE gates[0]** &mdash; No gate exists for this property; the manifest records why.
  - Nothing written yet. The gate this item needs is a script that reports how many subject rows carry a non-derived accessToken, run against a database with one planted, and a control row that has none - so the script is shown to distinguish the two before anyone trusts a zero.
- **P0-E gates[8]** &mdash; No gate exists for this property; the manifest records why.
  - THE `npm run test:unit` GATE WAS REMOVED, not downgraded. It was recorded as "361 passing / 0 failing at GT1's measurement", but not one of the five test files this branch adds is in the 44-file brace list - all five are api*/api3* and match test:integration's glob - so the suite could have passed in full with every fix reverted.
- **P0-E gates[9]** &mdash; No gate exists for this property; the manifest records why.
  - The ordering constraint inside the branch (BF-05's commit must follow BF-01's) is not machine-checked. After the CHANGELOG strip and the re-cut onto dev the two commits are 3b588098 and 4772b983, so any gate quoting an earlier SHA is stale by construction. A real gate would assert the relative order of the two by subject line, and nobody has written it.
- **P0-E gates[10]** &mdash; No gate exists for this property; the manifest records why.
  - The CHANGELOG on this branch lists read routes only, while the ?count= validator covers writes too. Nothing checks a release note against the code it describes.
- **P0-F gates[2]** &mdash; No gate exists for this property; the manifest records why.
  - Vendor rate limits are unmeasured (EXP-MT-051) and need real credentials, which rule 0 forbids here. T0.4 shipped CONNECT_START_JITTER_MS so the pool CAN be spread - but the window to set it is exactly the number that is unmeasured.
- **P0-H gates[3]** &mdash; No gate exists for this property; the manifest records why.
  - `dataUpdate` still has no try/catch, so the NEXT throw from anywhere in the merge path has the same effect. This branch fixes one throw, not the missing boundary. No test asserts the boundary exists because it does not.
- **P0-TAG gates[5]** &mdash; No gate exists for this property; the manifest records why.
  - Whether all seven connector commits are the RIGHT content for a release is a release-content decision, not a mechanical bump, and it is the maintainer's. Nothing can gate it.
- **P0-PIN gates[3]** &mdash; No gate exists for this property; the manifest records why.
  - Nothing can verify the tarball's integrity hash before the tag exists on GitHub. That is the whole reason P0-LOCK is a separate item.
- **P0-T01 gates[1]** &mdash; No gate exists for this property; the manifest records why.
  - PR merge state is upstream's and cannot be gated from here without a GitHub API call. This item is tracked, not driven.
- **FU-LIMIT gates[2]** &mdash; No gate exists for this property; the manifest records why.
  - Nothing asserts that the two implementations AGREE while they both exist. A differential test - the same limit value through both paths, including 0, a negative, a non-numeric and a value above the maximum - is what would make the duplication safe until it is removed, and it does not exist. Two readings of one rule is the root cause of this whole family, so leaving it duplicated is a debt with a name.
- **FU-RESIDUALS gates[3]** &mdash; No gate exists for this property; the manifest records why.
  - Reachability of follow-up 7 is not measured. It needs an Alexa request type outside SessionEndedRequest, LaunchRequest and IntentRequest, and nothing here enumerates what Amazon can send.
- **FU-HYGIENE gates[0]** &mdash; No gate exists for this property; the manifest records why.
  - Follow-up 9 - audit the suppressions OUTSIDE lib/: detect-non-literal-fs-filename, detect-possible-timing-attacks, no-cond-assign. There is no instrument because the audit IS the work: object-injection's 34 suppressed lines yielded five real defects (BF-35, BF-36, BF-37, BF-38 and BF-39 - the register's own tally of four is stale), and the same reasoning applies to each remaining category. What could be gated afterwards is a rule that no NEW suppression is added without an entry, and that rule does not exist.
- **FU-HYGIENE gates[1]** &mdash; No gate exists for this property; the manifest records why.
  - Follow-up 10 - jsdom test hygiene has no enforcement. A suite that sets global.window or global.document must restore them in afterEach or it breaks browser-settings.test.js later in the same run. hashauth.modern.test.js does the restore; nothing requires it, and the failure lands in a DIFFERENT file from the one that caused it, which is the shape that costs the most review time. The instrument is a harness-level check, and cut 1 retires jsdom entirely, so it should be decided against the release train rather than built twice.

### `out-of-scope`

- **queue/work-queue.yaml** &mdash; 57 of the 74 items in the manifest are NOT in this record.
  - Selection was ids=- parcels=['phase0'] states=-. Excluded: A7A-3, A7A-4, A7A-7, A7A-GATE, BFQ-04, BFQ-09, BFQ-10, BFQ-18, BFQ-19, BFQ-20, BFQ-21, BFQ-22, BFQ-23, BFQ-24, BFQ-25, BFQ-26, BFQ-27, BFQ-40, BFQ-41, BFQ-46, BFQ-47, BFQ-52, BFQ-66, BFQ-67, BFQ-CAP01, BFQ-CAP02, BFQ-CONNECTOR, BFQ-ENV, BFQ-MINIMED, DOC-EXPOSURE, DOC-LAYOUT, DOC-MEMORY, DOC-PLAN, DOC-REGISTER, DOC-SEQUENCING, DOC-TESTSCRIPTS, RT-0, RT-1, RT-2, RT-3, RT-4, RT-5, RT-BOOTERROR, RT-CONNECT-PIN-CUTS, RT-D3, RT-NODE-FLOOR-TESTED, RT-REBASE, RT-VERSION, SEAM-REFRESH, T30-RESEARCH, T30-SCHEMA, T30-WIRING, T31-REM, T32-REM, T33-REM, T43, T44

### `publication-unverified`

- **P0-A** &mdash; Whether bf/alarms exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-B** &mdash; Whether bf/cache exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-C** &mdash; Whether bf/auth exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-D** &mdash; Whether bf/coercion exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-E** &mdash; Whether bf/reads exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-F** &mdash; Whether fix/connect-timer-jitter exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-G** &mdash; Whether bf/food exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-H** &mdash; Whether bf/merge exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-I** &mdash; Whether bf/parms exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-TAG** &mdash; Whether release/v0.0.14 exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-PIN** &mdash; Whether bf/connect-pin exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-LOCK** &mdash; Whether bf/connect-pin exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.
- **P0-T01** &mdash; Whether fix/quadratic-treatment-processing exists on the remote was NOT checked against the remote.
  - Determined from local remote-tracking refs (no network). That is a statement about this checkout's last fetch, not about the remote. Re-run with --network for a read-only ls-remote.

### `record-method`

- **basis classification** &mdash; The basis of each register entry was read ONLY from its row in the register's summary tables (sections 1, 1b, 1c), not from its detail section in section 2 / 2b.
  - The register's own header records that provenance marking is not yet consistent between the two - three entries carry a `reproduced` block prepended above a body that still reads `not reproduced against a live instance`. Where the table and the detail disagree, this record follows the table and does not reconcile them.
- **gate output** &mdash; Only the last 40 lines of each gate's output are stored, beside a sha256 of the whole of it.
  - The digest lets a reader prove a full log they hold is the one this record was taken from; it does not let them reconstruct it. Full logs were not retained by this capture.
- **reproducibility** &mdash; This record is NOT byte-reproducible end to end, and the claim that it is has been narrowed to what was measured.
  - Two captures of this release were diffed field by field. STABLE: resolved SHAs, commits-ahead counts, every gate outcome, every item verdict, every register closure state and basis, the COUNTED test summaries (passing/failing/pending) and every entry in this section. MOVES: the gate transcripts output_tail, output_bytes and output_sha256 - 25 of 164 transcript fields moved - because the commands are not deterministic (mocha prints its own millisecond timings, node prints its pid). ONE NON-TRANSCRIPT FIELD MOVES WITH THEM and an earlier draft of this record wrongly implied it did not: output_summary.last_line, where a benchmark ends its output with a duration - P0-D's coercion benchmark gave duration_ms 282.795205 on one capture and 278.901709 on the next, and that string reaches both the gate summary and the test evidence. So two captures may be diffed on outcomes, verdicts, bases and gaps, and must NOT be diffed on digests or on a last_line that quotes a duration.

### `register-basis-unstated`

- **BF-02** &mdash; Register entry BF-02 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (T0.5, bf/coercion 88d1f8a4)
- **BF-03** &mdash; Register entry BF-03 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 for devicestatus + profile (T0.5, bf/coercion 88d1f8a4); food and activity misfiled, see detail
- **BF-05** &mdash; Register entry BF-05 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (bf/reads c8fb536b) — deleted, not gated; the module has no env handle
- **BF-06** &mdash; Register entry BF-06 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (T0.2, bf/cache ddcdb1a8); 0.837 → 0.025 ms, response asserted identical over HTTP
- **BF-07** &mdash; Register entry BF-07 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: partly fixed 2026-09-15 (T0.3, bf/cache 4f86bab1); 3.75 → 2.66 ms per cycle — devicestatus keeps its clone on purpose, see detail
- **BF-08** &mdash; Register entry BF-08 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (T0.4, fix/connect-timer-jitter c1cce2a); measured at 400 actors, busiest second 400 → 15
- **BF-11** &mdash; Register entry BF-11 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (T0.5, bf/coercion 88d1f8a4)
- **BF-28** &mdash; Register entry BF-28 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (bf/alarms 8714093b)
- **BF-29** &mdash; Register entry BF-29 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (bf/alarms 99e46a52)
- **BF-31** &mdash; Register entry BF-31 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (bf/alarms 5dcf783f)
- **BF-32** &mdash; Register entry BF-32 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (found during T0.5, bf/coercion 88d1f8a4); claim refuted 2026-09-15, see detail and BF-40
- **BF-34** &mdash; Register entry BF-34 does not state whether it was read or run.
  - No phrase in its status cell says. The classifier will not infer one from measurement-shaped prose, because inferring a reproduction is exactly the laundering rule 3 exists to stop. Status cell verbatim: fixed 2026-09-15 (found during T0.4, fix/connect-timer-jitter c1cce2a); 100 actors delivered the same 800 requests across 3 s before and 67 s after

### `register-entry-not-demonstrated`

- **BF-17** &mdash; Closure state is claimed-fixed-but-a-gate-failed.
  - This release does not demonstrate that BF-17 is repaired.
- **BF-28** &mdash; Closure state is claimed-fixed-but-a-gate-failed.
  - This release does not demonstrate that BF-28 is repaired.
- **BF-29** &mdash; Closure state is claimed-fixed-but-a-gate-failed.
  - This release does not demonstrate that BF-29 is repaired.
- **BF-30** &mdash; Closure state is claimed-fixed-but-a-gate-failed.
  - This release does not demonstrate that BF-30 is repaired.
- **BF-31** &mdash; Closure state is claimed-fixed-but-a-gate-failed.
  - This release does not demonstrate that BF-31 is repaired.

### `register-untouched`

- **docs/30-design/remedial/nightscout-backfix-register.md** &mdash; 42 register entries are not fixed and are not carried by anything in this release.
  - Not a defect in the release; a statement of what shipping it does not address. Ids: BF-09, BF-10, BF-18, BF-19, BF-20, BF-21, BF-22, BF-23, BF-24, BF-25, BF-26, BF-27, BF-40, BF-41, BF-42, BF-43, BF-44, BF-45, BF-46, BF-47, BF-48, BF-49, BF-50, BF-51, BF-52, BF-53, BF-54, BF-55, BF-56, BF-57, BF-58, BF-59, BF-60, BF-61, BF-62, BF-63, BF-64, BF-65, BF-66, BF-67, CAP-01, CAP-02

---

*End of verification record. Machine-readable source: `verification-record.json`. Captured `2026-09-16T00:05:00-07:00`. Never edited.*
