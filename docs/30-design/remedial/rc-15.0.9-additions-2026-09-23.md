# 15.0.9 additions integration record, 2026-09-23

*Contributor-facing. Snapshot: measured 2026-09-23 on a local scratch branch
that has not been pushed. cgm-remote-monitor `origin/dev` `74fc6619` (unmoved at
fetch). Item state lives in the queue.*

`bf2/backports` fixes defects that are live on the shipping release (15.0.8).
This record describes mechanisms only.

## Branch

`rc/15.0.9-additions`, created from `74fc6619` by SHA. Tip `cfa8cb39`. Each unit
is one `git merge --no-ff` commit. No unit was dropped.

| first-parent | commit | unit (tip) |
|---|---|---|
| 5 | `cfa8cb39` | `bf/connect-pin-0.1.0` (`338deb7f`) |
| 4 | `5b37735d` | `bf2/backports` (`b5038500`) |
| 3 | `623e7928` | `bf/count-zero-empty` (`7b32d9ab`) |
| 2 | `8d7a3f1a` | `bf/qs-6.16` (`46b20b38`) |
| 1 | `f06b60d7` | `docs/mongodb-floor` (`aabce4b1`) |
| 0 | `74fc6619` | `origin/dev` |

Before merging, `git merge-tree` of each unit onto the previous simulated step
was clean for all five. `bf/qs-6.16` and `bf/connect-pin-0.1.0` both edit
`package.json` and `package-lock.json`; their hunks are disjoint (qs override
lines and the `qs` / `side-channel` lock entries, against the connector
dependency line and its lock entry), and the lockfile merged without conflict.
`bf/count-zero-empty` does not edit `lib/api/entries/index.js`; only
`bf2/backports` does, so the two units share no file.

`bf/count-zero-empty` was measured at `7b32d9ab`. That branch has since gained a
commit (a DELETE with an invalid count is refused again). Step 3 onward must be
re-measured against the new tip.

## Full suite (`npm test`, own MongoDB 7 container)

| step | tree | Node | passing | failing | pending | delta |
|---|---|---|---|---|---|---|
| 0 | `74fc6619` | 20.20.0 | 2386 | 0 | 3 | |
| 1 | + `docs/mongodb-floor` | 20.20.0 | 2386 | 0 | 3 | 0 |
| 2 | + `bf/qs-6.16` | 20.20.0 | 2386 | 0 | 3 | 0 |
| 3 | + `bf/count-zero-empty` | 20.20.0 | 2404 | 0 | 3 | +18 |
| 4 | + `bf2/backports` | 20.20.0 | 2416 | 0 | 3 | +12 |
| 5 | + `bf/connect-pin-0.1.0` | 20.20.0 | 2416 | 0 | 3 | 0 |
| 5 | final | 22.23.2 | 2416 | 0 | 3 | |
| 5 | final | 24.20.0 | 2416 | 0 | 3 | |
| 5 | final, MongoDB 4.4.24 | 20.20.0 | 2416 | 0 | 3 | |

The final figure equals the additive expectation (2386 + 0 + 0 + 18 + 12 + 0).
`npm ci` ran after each dependency change, and from an empty `node_modules`
under each of Node 20, 22 and 24 on the final tree; all succeeded.

## Targeted tests and break-its on the integrated tree

A break-it reverts the unit's library file(s) on the integrated tree, runs the
unit's own tests, and restores. It counts only when the failures reproduce the
defect the unit fixes.

| unit | targeted | result | break-it | failing | reproduces the defect |
|---|---|---|---|---|---|
| qs-6.16 | `npm ls qs`, `npm audit --omit=dev` | one `qs@6.16.0`; no qs entry | n/a | | |
| count-zero | `api.count-parameter` | 31/31 | `lib/api/index.js`, `lib/api/devicestatus/index.js`, `lib/server/count.js`, `lib/server/profile.js` from dev | 21 of 31 | yes: count=0 reads answer 400 (20); the storage layer returns records for a zero count (1) |
| backports | `api3.alarm-logging`, `storage-read-permissions` | 12/12 | `lib/api3/alarmSocket.js`, `lib/api/entries/index.js` from dev | 7 of 12 | yes: credentials reach the log (6); a read is allowed where it should be refused (1) |
| connect-pin | `debug-logging` | 23/23 | connector `v0.0.13` (`git archive`) swapped into `node_modules/nightscout-connect`, restored with `npm ci` | 5 of 23 | yes: the five DEBUG_LOGGING x CONNECT_DEBUG cases; assertion failures on log counts, no module load error |

## Dependencies on the final tree

| check | result |
|---|---|
| `npm ls qs nightscout-connect` | one `qs@6.16.0` (deduped under body-parser, express, request, nightscout-connect, superagent); `nightscout-connect@0.1.0-dev.1` |
| `npm audit --omit=dev` | 13 (1 low, 9 moderate, 3 high); no `qs`, no `nightscout-connect` entry |

## `git merge-tree` against `rc/backfix-2` (`e9a4ef62`, not merged)

| other side | result | shared paths |
|---|---|---|
| `rc/backfix-2` `e9a4ef62` | clean (tree `cbb7243f`) | `bf2/backports` files (same commits); `lib/api/index.js`, `lib/api3/alarmSocket.js`, `package.json`, `package-lock.json`, `README.md` with `bf2/auth-hardening` |

In the merged tree `package.json` carries the connector pin, `proxy-addr` and
the qs 6.16.0 overrides together. `rc/backfix-2` contains `bf2/backports` as its
second first-parent merge. Once this branch lands, `rc/backfix-2` is re-cut by
replay: a new branch from the new base by SHA, merging `bf2/ops` and
`bf2/auth-hardening` in their original order and omitting `bf2/backports`
because it is already an ancestor, with the suite and break-its measured again
at each step.

## Replay with `bf/count-zero-empty` at `ce9503ac` (2026-09-23)

The maintainer changed the DELETE half of RT-COUNT0 after the run above: a DELETE with an invalid
count, zero included, is refused with 400 again. `bf/count-zero-empty` moved from `7b32d9ab` to
`ce9503ac`. Following the replay policy, the run above was not patched. A fresh branch,
`rc/15.0.9-additions-b`, was cut from `74fc6619` by SHA, and the same five units were merged in the
same order, with count-zero taken at `ce9503ac`. All merges were clean.

Ledger: `af0d964c` docs/mongodb-floor, `b1c3b8e0` bf/qs-6.16, `cf228ba2` bf/count-zero-empty (`ce9503ac`),
`44d17e5e` bf2/backports, `865bcba3` bf/connect-pin-0.1.0 (tip).

| measurement (Node 20.20.0, `mongo:7`) | result |
|---|---|
| `npm ci` from clean; `npm ls qs nightscout-connect` | ok; one `qs@6.16.0`, `nightscout-connect@0.1.0-dev.1` |
| count tests, final tree | 36 passing |
| count tests with count-zero's `lib/` files reverted to `74fc6619` | 18 passing, 18 failing (the DELETE-refusal cases pass on dev too, since dev refuses them) |
| full suite after the count-zero merge (`cf228ba2`) | 2409 passing, 0 failing, 3 pending, the same as count-zero alone |
| full suite, final tree (`865bcba3`) | 2421 passing, 0 failing, 3 pending, exactly 2386 + 23 + 12 |

The step-3 suite ran with the final tree's installed dependencies (connector `0.1.0-dev.1` in place of
dev's `234d47c` pin). Earlier arms measured the two connectors as identical on this suite. The Node 22/24
and `mongo:4.4` runs above were at `7b32d9ab` and were not repeated for this replay.
