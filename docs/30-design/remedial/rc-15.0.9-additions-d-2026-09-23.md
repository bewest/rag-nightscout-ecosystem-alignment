# 15.0.9 additions, integration record d, 2026-09-23

*Contributor-facing. Snapshot: measured 2026-09-23 on a local scratch branch
that has not been pushed. cgm-remote-monitor `origin/dev` `74fc6619`, `origin/master`
`92d08342` (tag 15.0.8), both unmoved at fetch before the run. Method and record
format as in [rc-15.0.9-additions-c](rc-15.0.9-additions-c-2026-09-23.md). The two
units are recorded in [BF-90](../../60-research/remedial/bf90-alarm-no-reading-2026-09-23.md)
and [BF-69](../../60-research/remedial/bf69-quickpick-rebuild-2026-09-23.md). Item state
lives in the queue.*

Record d adds the two client fixes the maintainer placed in 15.0.9 on 2026-09-23,
BF-90 and BF-69, on top of record c's eight units. Nothing in record c's tree was
changed or re-merged.

## Branch

`rc/15.0.9-additions-d`, created from `rc/15.0.9-additions-c` `b9c9828b` by SHA in its
own worktree (`externals/work/crm-rc-1509d`). Tip `5764156e`. Each unit is one
`git merge --no-ff` commit, made by SHA after checking that the SHA was still the
branch tip and that `git merge-tree` reported no conflict. Both merges were clean
and no unit was dropped.

| first-parent | commit | unit (tip) | PR |
|---|---|---|---|
| 10 | `5764156e` | `bf3/quickpick-rebuild` (`83cfff14`, BF-69) | not opened |
| 9 | `d84a1edd` | `bf3/alarm-no-reading` (`92544d8f`, BF-90) | not opened |
| 8 | `b9c9828b` | `rc/15.0.9-additions-c` tip (eight units, see record c) | |

Both units are one commit on `74fc6619`. Tips were checked before the first merge
and again after the break-its. Neither had moved, and `rc/15.0.9-additions-c` was
still at `b9c9828b`.

**Overlap.** The two units touch disjoint files: `lib/client/index.js` (BF-90) and
`lib/client/boluscalc.js` (BF-69), plus one new test file each. None of record c's
eight units touches `lib/client/` (`git diff --stat 74fc6619 b9c9828b -- lib/client`
is empty). `git diff b9c9828b 5764156e` is those four files, +380/−6.
`package.json` and `package-lock.json` are unchanged from record c.

**BF-69's ordering constraint.** BF-69 must ship with or after BF-35 (#8735). #8735
is in `74fc6619`, which is an ancestor of both `83cfff14` and this branch.

## Full suite (`npm test`, own MongoDB containers)

`npm test` runs under `my.test.env`, which is `tests/ci.test.env` with the connection
string pointed at database `rc1509d_test` on the agent's own container. That is the
official `mongo` image at tag 7 (7.0.43) or tag 4.4 (4.4.24), each run with
`--ulimit nofile=64000:64000`. The database was dropped before every run. Port 1337
was checked free before each suite.

Both units' tests run against the **production bundle** (`tests/fixtures/headless.js`),
so `npm run bundle` was run after each merge, before the suite. `npm ci` ran from an
empty `node_modules` under Node 20 at step 8 and under each Node version on the
final tree. Its `postinstall` builds the bundle. Every install and build succeeded.

| step | tree | Node | MongoDB | passing | failing | pending | delta |
|---|---|---|---|---|---|---|---|
| 8 | `b9c9828b` (record c final) | 20.20.0 | 7.0.43 | 2508 | 0 | 3 | |
| 9 | + `bf3/alarm-no-reading` | 20.20.0 | 7.0.43 | 2512 | 0 | 3 | +4 |
| 10 | + `bf3/quickpick-rebuild` | 20.20.0 | 7.0.43 | 2520 | 0 | 3 | +8 |
| 10 | final | 20.20.0 | 4.4.24 | 2520 | 0 | 3 | |
| 10 | final | 22.23.2 | 7.0.43 | 2520 | 0 | 3 | |
| 10 | final | 22.23.2 | 4.4.24 | 2520 | 0 | 3 | |
| 10 | final | 24.20.0 | 7.0.43 | 2520 | 0 | 3 | |
| 10 | final | 24.20.0 | 4.4.24 | 2520 | 0 | 3 | |

Step 8 reproduces record c's final figure (2508/0/3) in this worktree. 2520 = 2508 +
4 + 8. The deltas are the per-unit figures in the BF-90 doc (+4) and the BF-69 doc
(+8).

**Additivity was checked by test title, not only by count.** At each step, the sorted
list of passing test titles was diffed against the previous step's. Lines that carry
a generated ObjectId were left out.

- Step 9: 4 titles added, none removed. They are the four tests in
  `tests/client.alarm-no-reading.test.js`.
- Step 10: 8 titles added, none removed. They are the eight tests in
  `tests/boluscalc.quickpick-rebuild.test.js`.
- The five final-tree matrix cells: title lists identical to step 10 on Node 20 with
  MongoDB 7.

No other test changed state.

## Break-its on the final tree (`5764156e`)

A break-it reverse-applies the unit's own fix hunk (`git diff <unit>^ <unit> -- <lib file>`)
on the integrated tree, rebuilds the production bundle, and runs the unit's tests on
Node 20.20.0 against MongoDB 7 with a dropped database. It then restores with
`git checkout HEAD -- lib` and rebuilds the bundle again. `git status` was clean after
each one, and the unit's tests passed again on the rebuilt bundle. A break counts
only when the failures show the defect the unit fixes.

| unit | unbroken | break | failing | symptom |
|---|---|---|---|---|
| BF-90 | `client.alarm-no-reading` 4/4 | the whole `lib/client/index.js` hunk of `92544d8f` reverse-applied | 2 of 4 | both discriminating tests: `TypeError: Cannot read properties of undefined (reading 'mgdl')`. The two invariants (no sound with no reading; the control sounds with one) pass. Same as the branch (break A) |
| BF-69 | `boluscalc.quickpick-rebuild` 8/8 + `boluscalc.quickpick` 11/11 | the whole `lib/client/boluscalc.js` hunk of `83cfff14` reverse-applied | 6 of 19 | all six discriminating tests see the chooser empty: five `expected Array [ '(none)' ] to equal Array [ '(none)', … ]`, one `no option labelled /^Lunch/ among ["(none)"]`. #8735's 11 BF-35 tests and the handler-count guard pass. Same count as the branch's break B2 |

The full revert of BF-90 restores both unguarded lines. The first throw
(`reading 'mgdl'`) is the one reached, so this break does not exercise the second
guard (`chart`). The BF-90 doc's break B, which does, was not repeated here.

The full revert of BF-69 also puts the `change` binding back inside
`loadFoodQuickpicks`. That binding then runs once, at construction, so the
handler-count guard stays green. The BF-69 doc's break B1, which fails it, was not
repeated here.

## Not measured

- The browser probes (`alarm-no-reading-browser.js`, `quickpick-rebuild-browser.js`,
  `food-boluscalc-browser.js`, `quickpick-chooser-browser.js`) on the integrated tree.
  They ran on the unit branches only.
- The BF-90 and BF-69 break-its other than the full-hunk reverts above.
- The final tree against MongoDB 5.0 or 6.0, which dev's CI matrix tests.
- Break-its under Node 22/24 or MongoDB 4.4. Both ran on Node 20.20.0 with
  MongoDB 7.0.43.
- `git merge-tree` of this branch against `origin/chore/nightscout-modernization`.
- `tests/security.test.js` listens on the fixed port 1337. Each suite checked that
  the port was free when it started, but other agents were running suites on the
  same machine.
