# The five open security advisories — disposition, and what each one needs from a person

*Contributor-facing.*

> **Details withheld (2026-09-23).** This document described the verdicts on five security advisories, three of which are still open, which is tracked in a
> private GitHub security advisory and is still present in the shipping release, 15.0.8. The
> detail is withheld until a release containing the fix ships and the advisory is published,
> matching the pull request (#8743, #8744, #8745), whose description was shortened the same way. The full text
> is in git history at `ef376ecb`. Current state: queue items `ADV-*` and `RT-0` in
> [`queue/work-queue.yaml`](../../../queue/work-queue.yaml).
