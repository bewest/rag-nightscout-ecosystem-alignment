# 15.0.9 integration record

*Contributor-facing. Living record of how the cgm-remote-monitor 15.0.9 candidate was assembled
and tested. Current run: 2026-09-25, run 015, #8758 `ab7b22d6` + wip/object-id-crud-fixes-2 `e9dbb1fb`
(local, not pushed; tree `ea4c4852`), the #8758 head to push. It is not the release candidate: the
merge with #8568/#8419/#8530 is not built. Item
state is in `queue/work-queue.yaml` (`RT-0`); defect facts are in the
[backfix register](nightscout-backfix-register.md); release contents are in
[releases/cgm-remote-monitor-15.0.9](../../../releases/cgm-remote-monitor-15.0.9/contents.md).
Several units fix defects that are live on the shipping release (15.0.8, `origin/master`
`92d08342`), so this record describes mechanisms only. Not medical advice.*

This record has four parts:

- [Current run](#current-run) is the evidence for the candidate as it stands.
- [Evidence carried from earlier runs](#evidence-carried-from-earlier-runs) covers checks that were
  not repeated on the current tree and still stand for it.
- [Run history](#run-history) lists every run, with a link to its full record at the commit that
  holds it.
- [Method](#method) gives the rules every run follows.

A new run replaces the **Current run** section. The run it replaces moves to one row of
**Run history**. Anything from that run that the new run did not repeat moves to **Evidence carried
from earlier runs**.

## Current run

**2026-09-25. Run 015: #8758 `ab7b22d6` + wip/object-id-crud-fixes-2 `e9dbb1fb` (local, not pushed).**

`e9dbb1fb` is the #8758 head the maintainer will push.

**This is not the release candidate.** 15.0.9 also carries #8568 (BF-114), #8419 and #8530. The
merge of `e9dbb1fb` with those three heads has not been built (see
[What the current run does not cover](#what-the-current-run-does-not-cover)).

### What was tested

Measured 2026-09-25 after `git -C externals/cgm-remote-monitor-official fetch origin`:

| ref | commit | tree |
|---|---|---|
| `origin/dev` | `4f705217` | `b69c9fd9` |
| `origin/bf/object-id-crud` (#8758) | `ab7b22d6` | `25ab7afc` |
| `wip/object-id-crud-fixes-2` (local, not pushed) | `e9dbb1fb` | `ea4c4852` |
| `origin/master` = tag 15.0.8 | `92d08342` | |

`e9dbb1fb` is six commits on `ab7b22d6`:

| commit | register | change |
|---|---|---|
| `17add44b` | BF-115 | An entry or treatment create with an empty or unusable `_id` drops it, so the server assigns one. An ObjectId from another copy of the bson package is kept. The loaders accept a stored record with no `_id`. |
| `c3a34bac` | BF-116 | A devicestatus re-sent with the `_id` it is stored under is answered with that record. The rest of the batch is stored: the insert is unordered and tolerates duplicate keys. |
| `63dd716c` | BF-117 | An API v3 DELETE removes every stored form of the record. v3 reads and writes also sort by `_id` −1, so they take the ObjectId copy of a record stored twice. |
| `cb7d4110` | BF-117, BF-115 | The v3 every-form DELETE reaches only records without their own `identifier`. `dropEmptyId` reads an Extended JSON `{$oid}` as the ObjectId it names. |
| `6c3ccce6` | BF-130 | The treatments batch path removes a record's string-stored forms right after each write matched by `_id`, using an index map for upserted ids. A batch item that deduplicates onto a string-stored copy of another item's record is no longer lost. |
| `e9dbb1fb` | BF-131 | A record deleted by its `_id` is removed from the in-memory cache. `remove()` reported the rewritten filter as its `changes`, so the cache kept the record and pages and unfiltered reads still showed it. The new `object-id-forms.cacheRemoval` is used by entries, treatments and devicestatus `remove` and by websocket `dbRemove`. |

- `git diff --stat origin/dev...e9dbb1fb`: 38 files, +4194/−107.
- **`17add44b` reaches outside the server-only paths.** It changes `lib/data/calcdelta.js`,
  `lib/data/dataloader.js` and `lib/data/ddata.js`. `ddata.js` is in the browser bundle
  (`lib/client/index.js` requires it). Every other path is under `lib/server/`, `lib/api3/` or
  `tests/`.
- No `package.json` or `package-lock.json` change. `nightscout-connect` installs as `0.1.0`.

### Suite

- **Command:** CI's, from `.github/workflows/main.yml`: `npm run-script test-ci` (mocha under nyc,
  env from `tests/ci.test.env`), then `npm run-script test:core`.
- **Worktrees:** `node_modules` from `npm ci` on Node 22.23.2 (at `8e0684b1`, whose manifests match
  `e9dbb1fb`'s), with the webpack bundle rebuilt from `e9dbb1fb` by `npm run bundle`. Each cell ran
  in its own worktree at `e9dbb1fb`, with `node_modules` hard-linked.
- **Isolation:** the test runner shared the network namespace of its cell's mongod container, so the
  suite's fixed addresses (`127.0.0.1:27017`, port 1337) were private to the cell.

| suite | Node | MongoDB (read) | test-ci passing | failing | pending | test:core passing |
|---|---|---|---:|---:|---:|---:|
| `e9dbb1fb` | 20.20.0 | 4.4.24 | 3106 | 0 | 3 | 286 |
| `e9dbb1fb` | 20.20.0 | 7.0.43 | 3106 | 0 | 3 | 286 |
| `e9dbb1fb` | 22.23.2 | 4.4.24 | 3106 | 0 | 3 | 286 |
| `e9dbb1fb` | 22.23.2 | 7.0.43 | 3106 | 0 | 3 | 286 |
| `e9dbb1fb` | 24.20.0 | 4.4.24 | 3106 | 0 | 3 | 286 |
| `e9dbb1fb` | 24.20.0 | 7.0.43 | 3106 | 0 | 3 | 286 |

- **Every cell passed.** None was re-run. All six sorted title lists hash identically.
- **The flaky test passed.** `tests/api.entries.repost-with-id.test.js` "the read asks only for the
  matched entries" passed in all six cells.
- **The fixes add 40 tests over `ab7b22d6`** (3066 → 3106). On Node 22 / MongoDB 7, a diff of the
  sorted titles against run 011 gives 51 new titles and 11 gone. All 11 gone are renames:

| title on `ab7b22d6` | title on `e9dbb1fb` |
|---|---|
| a re-send of a record stored with the string _id is refused, leaving the one string record | … is acknowledged, leaving the one string record |
| a re-send in upper case of a record stored with the lower-case string _id is refused | … is acknowledged |
| a re-send of a record stored with an upper-case string _id, in upper case, is refused | … is acknowledged |
| a re-send of a record stored with an ObjectId _id is still refused (control) | … is acknowledged, not stored as a string copy |
| an in-process create of a string-stored _id is refused too | … is acknowledged too |
| v1 resend \| devicestatus \| lower -> 500 [string] | … lower -> 200 [string] |
| v1 resend \| devicestatus \| upper -> 500 [string] | … upper -> 200 [string] |
| v1 resend \| devicestatus \| oid -> 500 [ObjectId] | … oid -> 200 [ObjectId] |
| v1 resend \| devicestatus \| oidU -> 500 [ObjectId] | … oidU -> 200 [ObjectId] |
| DELETE marks the copy GET returns as deleted | DELETE marks the copy GET returns, and the v1 record, as deleted |
| one read for a batch carrying 24-hex _ids, asking for their string forms | … asking for every form of each |

- **Where the other 40 new titles come from:**
  - `tests/api.empty-id.test.js`: entries and treatments POST/PUT with an unusable `_id`,
    `dropEmptyId`, and the loaders.
  - `tests/api3.delete-every-form.test.js`: DELETE, GET and PATCH on a record stored twice.
  - New `tests/api.devicestatus.resend-guard.test.js` cases: a batch after a re-send, a duplicate
    `_id` in one batch, and other insert errors.
  - `tests/api.object-id.treatments-entries.test.js`: the BF-130 treatments batch cases.
  - `tests/cache.remove-by-id.test.js` and one `tests/websocket.object-id.test.js` case: BF-131.
- **What `cb7d4110` adds over `63dd716c`:** four titles, none removed.
  - "DELETE leaves a record whose _id matches but whose identifier differs"
  - "DELETE ?permanent=true leaves a record whose _id matches but whose identifier differs"
  - "POST /treatments with an Extended JSON {$oid} _id keeps that id, and a re-POST adds no copy"
  - "reads an Extended JSON {$oid} as the ObjectId it names"
- **What `6c3ccce6` adds over `cb7d4110`:** three titles, none removed, all in
  `tests/api.object-id.treatments-entries.test.js`.
  - "a batch answers each item with the _id it is stored under"
  - "a batch item deduplicated onto a string-stored record is not lost"
  - "the same batch with no string-stored record ends the same way (control)"
- **What `e9dbb1fb` adds over `6c3ccce6`:** nine titles, none removed. That is 8 tests in
  `tests/cache.remove-by-id.test.js` and 1 in `tests/websocket.object-id.test.js`.
  - "names the one record removed, lower-case hex or any other string"
  - "asks the cache to reload for more than one record, an upper-case hex or a non-string"
  - "leaves the cache alone when nothing was removed"
  - "entries.remove by _id removes the entry from the cache"
  - "treatments.remove by _id removes the treatment from the cache, and keeps another"
  - "devicestatus.remove by _id removes the status from the cache"
  - "a delete asked in upper case removes the record from the cache"
  - "a delete of both copies of an id stored twice leaves neither in the cache"
  - "dbRemove of both copies of an id leaves neither in the in-memory cache (BF-131)"
- **Harness red control:** the same runner was pointed at an `e9dbb1fb` worktree whose container's
  mongod never started. The suite printed no totals and exited 124 when `timeout 300` stopped it.
- **No tracked file changed.** After the runs, `git status` in every cell's worktree showed only
  `.nyc_output/`.

### Object-id lab

`tools/lab/object-id` ran against six builds on MongoDB 7.0.43: a = 15.0.8 `92d08342`,
b = `dev` `4f705217`, d = `ab7b22d6`, f = `cb7d4110`, h = `6c3ccce6` and i = `e9dbb1fb`.
It ran twice, with identical cells both times.

The `probes.js` used is the one committed at `c76acf87`. It has three cells more than `68484d57`:
a P-ID-11 control with two ObjectIds, and two P-ID-12 cells for a subject stored with a string or
ObjectId `_id` and deleted by its hex. Builds a, b, d, f and h give the same values as in run 014 in
every cell.

Cells where i differs from d:

| cell | d `ab7b22d6` | i `e9dbb1fb` | cause |
|---|---|---|---|
| P-ID-6 devicestatus POST, re-POST, find, DELETE | 200:OID / 500:OID / find n=1 / DELETE 200 n=0 | 200:OID / **200**:OID / find n=1 / DELETE 200 n=0 | BF-116 `c3a34bac`: the re-send is acknowledged and nothing is stored twice |
| P-ID-7 v3 permanent DELETE | 200 / n=1 | 200 / **n=0** | BF-117: v3 DELETE removes both copies of a twin, as v1 and the websocket do |
| P-ID-10 hex DELETE, then GET | 200 / v1-original,v3-copy(invalid) / GET 410 | 200 / v1-original(invalid),v3-copy(invalid) / GET 410 | BF-117: v3 DELETE marks the v1 record deleted too. The lab's v1 record has no `identifier`, so `cb7d4110`'s narrowing leaves this cell as on `63dd716c` |
| P-ID-10 non-hex DELETE, then GET | as hex | as hex | BF-117 |

Every other cell of i equals d's. Every cell of i equals h's (`6c3ccce6`) and f's (`cb7d4110`).
The lab counts documents in MongoDB and does not read the in-memory cache, so it has no cell for
BF-131; the soak probe below covers it. It has no cell for BF-130's batch case either; the three
suite tests cover it.

### Consumer replay

This run repeated the 13-probe replay from the
[consumer-impact survey](../../../reports/consumer-impact-15.0.9/lab-results.md), using the same
script copies as run 011.

- **Builds:** A = 15.0.8 `92d08342`, B = `ab7b22d6`, C = `e9dbb1fb`.
- **Setup:** Node 22.23.2, MongoDB 7.0.43, configs `r`, `d`, `d2`, `ut` and `uf`.
- **Comparison:** each probe's result object was compared after masking ids, timestamps, tokens,
  database names and server file paths.

| probe | `e9dbb1fb` vs `ab7b22d6` |
|---|---|
| P11 devicestatus re-post with own `_id` | **changed**: the re-post and the re-send of a legacy string-stored copy both answer 200 with the stored record, and one document remains. On `ab7b22d6` and 15.0.8 both answer 500 E11000, with one document. BF-116. |
| P1–P10, P12, P13 | same, in every config |

- **Liveness:** every probe group answered 200 before and after, except 15.0.8's P4, which ends its
  own process (BF-107).
- **Matches earlier runs:** A and B equal their run 014 results. C equals `6c3ccce6`'s from
  run 014 in every probe but P9 (below).
- **P9 tie in the seed data:** the seed writes a Loop and an OpenAPS status with the same
  `created_at`, and `devicestatus.json?count=1` returns one of the two. In the full run, 15.0.8,
  `ab7b22d6` and `6c3ccce6` returned the OpenAPS status and `e9dbb1fb` the Loop one. With P9 run alone three times, every
  build, 15.0.8 included, returned the Loop status every time. Status (200) and length (1) are the
  same on every build and run. This is the probe's tie, not a change in `e9dbb1fb`.

### AAPS offline-marker probe (BF-114)

`tools/lab/aaps-offline/probe.js` measures whether an AAPS open-ended "disable loop" record keeps
Nightscout's "loop offline" marker on 5 h after the loop is re-enabled.

| arm | `e9dbb1fb` |
|---|---|
| control finite (want false) | false |
| control still-off (want true) | true |
| `aaps-release` (duration 2147483647 min) | true (still on) |
| `aaps-dev` (10-year duration, `originalDuration` 0) | true (still on) |
| exit status | 1 |

`e9dbb1fb` does not carry #8568, so both arms stay on, as on 15.0.8. #8568's own head was probed in
run 012 (see [Evidence carried from earlier runs](#evidence-carried-from-earlier-runs)).

### Deleted-reading probe (BF-131)

`tools/lab/rc-soak/probe-deleted-entry.js` writes 30 synthetic readings and deletes one with
`DELETE /api/v1/entries/<id>`. It then reads `/api/v1/entries.json?count=10` twice: once plain,
which can be answered from memory, and once with `find[sgv][$gte]=0`, which must be answered from
MongoDB.

Each head ran as a server with an empty database on MongoDB 7.0.43, on Node 22.23.2.

| | `e9dbb1fb` | `6c3ccce6` (control) |
|---|---|---|
| liveness before / after | 200 / 200 | 200 / 200 |
| DELETE | 200 | 200 |
| deleted reading in the plain read | no | **yes** |
| deleted reading in the storage read | no | no |
| exit status | 0 | 1 |

On `6c3ccce6` the reading is gone from MongoDB but still served from memory; on `e9dbb1fb` it is
gone from both.

### Browser hand-check gate

**The gate is red.** Command:
`node tools/queue/gates/client-unchanged-since-hand-check.js --with e9dbb1fb`.
Its control passes (`4011193e..8d797ba4` reports `lib/client/renderer.js`). The gate names five
files:

| file | from |
|---|---|
| `lib/api2/index.js`, `lib/api2/notifications-v2.js` | `dev`: `71987bb4` (#8764, merged with #8754) |
| `lib/data/calcdelta.js`, `lib/data/dataloader.js`, `lib/data/ddata.js` | BF-115 `17add44b`: the loaders no longer throw on a record with a null `_id` |

**The gate now also names the three `lib/data` files from BF-115.** `ab7b22d6` alone did not.
`lib/data/ddata.js` is in the browser bundle, so a hand check of the browser's data load is owed.

Neither `6c3ccce6` nor `e9dbb1fb` adds to what the gate names. Their non-test changes
(`lib/server/treatments.js`, `entries.js`, `devicestatus.js`, `object-id-forms.js` and
`websocket.js`) are all under `lib/server/`, which is on the gate's server-only list, so the gate
names the same five files as for `cb7d4110`.

**Hand checks owed:**

- **Loop remote commands through `lib/api2`:** override, carbs and bolus, from careportal and from
  LoopCaregiver.
- **The browser's data load through `ddata.js`:** the main view's first load and its live updates.

## Evidence carried from earlier runs

These checks ran on earlier trees and were not repeated on `ea4c4852`. Each row names the tree it
ran on. Every unit listed here is now in `dev` `4f705217`, and its code is unchanged since it was
checked, except where the row says otherwise.

### From run 012 (tree `ee10f1b1`, #8758 `ab7b22d6` + wip/object-id-crud-fixes-2 `63dd716c`)

- **AAPS offline-marker probe on #8568's head `de8efff0`** (run with `63dd716c`'s `node_modules`).
  Both controls behaved: finite false, still-off true.

  | arm | marker 5 h after re-enable |
  |---|---|
  | `aaps-release` | cleared |
  | `aaps-dev` | still on |

  The probe exits 1. #8568 clears the marker for the AAPS release shape but not for the AAPS
  development-branch shape. Not yet run on a merge that carries #8568.
- **Paths the three outside PRs change outside the gate's server-only list.** Read from each PR's
  diff against `dev`; the gate was not run on them.

  | PR | paths |
  |---|---|
  | #8568 `de8efff0` | `lib/data/ddata.js`, `lib/profile/profileeditor.js` |
  | #8419 `b1c23e74` | `.gitignore`, `.nycrc.json` |
  | #8530 `5353ff64` | `views/index.html` |

### From run 011 (tree `25ab7afc`, `dev` `4f705217` + #8758 `ab7b22d6`)

Runs 012 to 015 compared their results with run 011's rather than repeating these.

- **`dev` baseline suite:** `dev` `4f705217` (tree `b69c9fd9`) on Node 22.23.2 / MongoDB 7.0.43
  gave 2577/0/3 and 286 `test:core`. #8758 at `ab7b22d6` adds 489 passing tests over it: 490 new
  titles, 1 renamed in `a2c7eb39` (BF-109).
- **Object-id lab against the 2026-09-24 run:** on `ab7b22d6`, P-ID-10 matches 15.0.8 and `dev`
  (BF-109 `a2c7eb39`), and P-ID-11 reads and deletes string-stored records (BF-111 `1c2d1afd`).
  P-ID-12 stores nothing on `dev` and `ab7b22d6`, because subject create drops a client `_id`
  (`dev` `a8f75da5`, #8754). Results:
  [object-id-2026-09-25](../../../tools/lab/object-id/results/object-id-2026-09-25.md).
- **Consumer replay against the survey:** on `ab7b22d6`, P1 (oref0 `count=1?<token>` and
  `count=1?<sha1>`) and P2 (GluPredKit `count=0`) answer as 15.0.8 does, since #8761. P3, the activity
  numeric-`date` filter, returns 0 records where 15.0.8 returns 7, on `dev` and `ab7b22d6`. P4b
  (xdripswift `find[date][$in][]`) answers 500 on every build. P7, P8 and P10 are fixed relative to
  15.0.8. On P13, only a subject with the ack permission clears an alarm. Every other probe matches
  15.0.8.
- **The gate's default `--with` list is stale.** It names `origin/bf2/auth-hardening`,
  `origin/bf/object-id-crud` and `origin/bf/count-client-compat`, and the first and last are
  already ancestors of `dev`.

### Break-its of the units in `dev`

A break-it reverts or edits one unit's fix on the integrated tree and runs that unit's own tests. It
counts only when the failures show the defect the fix addresses. All of these ran on Node 20.20.0
with MongoDB 7.0.43, and every tree was restored and re-checked afterwards.

| unit | break | failing | failures show | tree (run) |
|---|---|---|---|---|
| count=0 answers `[]` (#8748) | count-zero's `lib/` files from `dev` `74fc6619` | 18 of 36 | 17 count=0 reads and writes answer 400; 1 storage-layer read returns records for a zero count | `b9c9828b` (c) |
| | DELETE branch of `validateCount` disabled | 6 of 36 | DELETE with an invalid or zero count is accepted | `b9c9828b` (c) |
| backports (#8751) | `lib/api3/alarmSocket.js`, `lib/api/entries/index.js` from `dev` `74fc6619` | 7 of 12 | 6 credentials reach the log; 1 read allowed that should be refused | `b9c9828b` (c) |
| ops (#8753) | `lib/plugins/index.js`, `lib/api/alexa/index.js`, `lib/server/booterror.js` from `dev`, one at a time | 1 of 14, 1 of 5, 2 of 4 | each unit's own symptom; the boot-error controls pass | `b9c9828b` (c) |
| auth-hardening (#8754): client address | `lib/server/client-ip.js` from `395f3207` | 7 of 67 | the seven `dev` edge-case pins, as on the branch | `b9c9828b` (c) |
| | unset `TRUST_PROXY` trusts nothing | 28 of 67 | 23 `client-ip` + 5 `authdelay`, as on the branch | `b9c9828b` (c) |
| | throttle keys every address alike / drops the address key | 5 of 67 / 9 of 67 | a different client shares, or loses, its own counter | `b9c9828b` (c) |
| | `TRUST_PROXY` hop-count branch removed | 8 of 81 | a hop-count setting is refused at startup | `f37ac198` (e) |
| | hop trust off by one | 5 of 81 | the wrong forwarded entry is taken as the client | `f37ac198` (e) |
| auth-hardening (#8754): subjects | `lib/authorization/storage.js` from `dev` | 11 of 13 | the access token is stored in the document; fields outside the allow-list are stored; edits lose stored fields | `b9c9828b` (c) |
| | the `keepStoredFields` call removed | 5 of 13 | an edit resets `created_at` and clears notes | `b9c9828b` (c) |
| alarm with no reading, BF-90 (#8755) | the fix hunk reverse-applied, bundle rebuilt | 2 of 4 | `TypeError` on the missing reading; the two invariants pass | `f37ac198` (e) |
| quick-pick rebuild, BF-69 (#8756) | the fix hunk reverse-applied, bundle rebuilt | 6 of 19 | the chooser shows only `(none)`; #8735's 11 tests pass | `f37ac198` (e) |
| connector `0.1.0` pin (#8762) | connector `v0.0.13` (the version 15.0.8's lockfile names) swapped into `node_modules` | 5 of 23 | the five `DEBUG_LOGGING` × `CONNECT_DEBUG` cases fail on log-line counts, with no load error | `f37ac198` (e), on `0.1.0-dev.2`; `0.1.0` differs from `0.1.0-dev.3` only in `docs/releasing.md` |

**#8758 has no break-its on an integrated tree.** Its own branch has them.

### Live server checks

These ran against a server booted from the real `lib/server/app.js` on `1b1977e0` (run e). That tree
is `dev` `74fc6619` plus every unit except #8758, and it carries #8754's final trust-proxy code.
Each check sent a request without forwarding headers, then one with a two-entry `X-Forwarded-For`
chain and `X-Forwarded-Proto: https`, both from the loopback peer.

| `TRUST_PROXY` | boots | client address for a chained request (`req.ip` = throttle key) | `req.secure` |
|---|---|---|---|
| unset | yes, with the unset security warning | left-most forwarded entry, as 15.0.8 resolves it | true |
| `false` | yes | the socket peer | false |
| `127.0.0.1,::1` | yes | the entry the trusted peer added | true |
| `1` | yes | the entry added one hop away | true |
| `true` | yes, with the every-hop security warning | left-most forwarded entry | true |
| `0` | no: startup error naming the accepted forms | | |

With no forwarding headers, every setting that booted gave the peer and `secure: false`.

The count rules held on each of those servers. `count=0` answers 200 `[]`, a valid count answers
with records, and a malformed count answers 400. A DELETE with a zero or invalid count answers 400
and removes nothing. The full request table is in run c's record.

### Nightscout-to-Nightscout connector lab

Run on `d087588f` (combined run of 2026-09-23), connector `0.1.0-dev.3` from npm. `0.1.0` differs
from that version only in `docs/releasing.md`.

- **Setup:** source `dev` `1f9a9d10` with `AUTH_DEFAULT_ROLES=denied`, fed synthetic data by
  `tools/lab/connector-soak/writer.js`. The sink was the candidate, reading through a byte-counting
  proxy with the source's API secret.
- **Duration:** 5 polls, 21:48–22:05Z. One request per collection per poll, no retries, and all 28
  proxied requests answered 200.
- **Completeness:** entries 587/587, treatments 225/225, devicestatus 192/192 and the in-effect
  profile 1/1, with no twins and nothing missing in the window.
- **Update on change:** a source profile edit reached the sink on the next poll under the same
  `_id`, with no twin.
- **Secrets:** the source, sink, proxy and writer logs were scanned for each API secret (raw and
  SHA-1) and for the reader's access token. There were 0 matches.

The bounded fetch and update-on-change were also lab-run for 80 and 46 minutes against
15.0.9-candidate sinks, as recorded in
[connector profile sync](../../60-research/remedial/connector-profile-sync.md).
Update-on-change is active only against a sink that has #8758.

## Run history

Each record is kept at the commit shown. Suite results are passing/failing/pending, and every run
had 0 failing. Before run 011, runs used `npm test`. From run 011 on, runs use CI's `test-ci` + `test:core`.

| run | date | base | added | tip | suite | record |
|---|---|---|---|---|---|---|
| **015 (current)** | 2026-09-25 | `dev` `4f705217` | #8758 `ab7b22d6` + wip/object-id-crud-fixes-2 `e9dbb1fb` (local, not pushed); the head to push; not the release candidate | tree `ea4c4852` | 3106/0/3, 6 cells; object-id lab; replay; AAPS probe; deleted-reading probe; gate | this page |
| 014 | 2026-09-25 | `dev` `4f705217` | #8758 `ab7b22d6` + wip/object-id-crud-fixes-2 `6c3ccce6` (local, not pushed); not the release candidate | tree `fd3e5c94` | 3097/0/3, 6 cells; object-id lab; replay; AAPS probe; gate | [rc-15.0.9-integration-record](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/4cb0f1a0/docs/30-design/remedial/rc-15.0.9-integration-record.md) |
| 013 | 2026-09-25 | `dev` `4f705217` | #8758 `ab7b22d6` + wip/object-id-crud-fixes-2 `cb7d4110` (local, not pushed); not the release candidate | tree `1c3685fe` | 3094/0/3, 6 cells; object-id lab; replay; AAPS probe; gate | [rc-15.0.9-integration-record](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/d6399575/docs/30-design/remedial/rc-15.0.9-integration-record.md) |
| 012 | 2026-09-25 | `dev` `4f705217` | #8758 `ab7b22d6` + wip/object-id-crud-fixes-2 `63dd716c` (local, not pushed); not the release candidate | tree `ee10f1b1` | 3090/0/3, 6 cells; object-id lab; replay; AAPS probe on `63dd716c` and #8568 `de8efff0`; gate | never committed; its results are in this row and in run 013's record, which differs from it only by `cb7d4110` |
| 011 | 2026-09-25 | `dev` `4f705217` | #8758 `ab7b22d6` | tree `25ab7afc` | 3066/0/3, 6 cells; `dev` 2577/0/3; object-id lab; replay | [rc-15.0.9-integration-record](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/a35e7e25/docs/30-design/remedial/rc-15.0.9-integration-record.md) |
| 010 | 2026-09-24 | `dev` `f1591069` | exact `0.1.0` pin (#8762), #8754 `ef3404fd`, #8758 `6d120fa2` | tree `4114f45a` | 3046/0/3, 6 cells | [rc-15.0.9-combined-010](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/b362302b/docs/30-design/remedial/rc-15.0.9-combined-010-2026-09-24.md) |
| 59 | 2026-09-23 | `dev` `ddd9b600` | #8754 `ef3404fd`, #8758 `6d120fa2` | tree `2ce67b27` | 3028/0/3, 6 cells | [rc-15.0.9-combined-59](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/b362302b/docs/30-design/remedial/rc-15.0.9-combined-59-2026-09-23.md) |
| 36b | 2026-09-23 | `dev` `1f9a9d10` | ten units #8748–#8759, including #8758 `6d120fa2` and the `0.1.0-dev.3` pin | `d087588f` | 3015/0/3, 6 cells; connector lab | [rc-15.0.9-combined](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/b362302b/docs/30-design/remedial/rc-15.0.9-combined-2026-09-23.md) |
| e | 2026-09-23 | `dev` `74fc6619` | every unit's current head, `0.1.0-dev.2` pin, #8754's trust-proxy trims | `1b1977e0` | 2534/0/3, 12 cells; break-its; live checks | [rc-15.0.9-additions-e](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/b362302b/docs/30-design/remedial/rc-15.0.9-additions-e-2026-09-23.md) |
| d | 2026-09-23 | run c's tip | BF-90, BF-69 | `5764156e` | 2520/0/3, 6 cells; break-its | [rc-15.0.9-additions-d](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/b362302b/docs/30-design/remedial/rc-15.0.9-additions-d-2026-09-23.md) |
| c | 2026-09-23 | `dev` `74fc6619` | eight units, #8748–#8754 | `b9c9828b` | 2508/0/3, 6 cells; break-its; count rules live | [rc-15.0.9-additions-c](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/b362302b/docs/30-design/remedial/rc-15.0.9-additions-c-2026-09-23.md) |
| a, b | 2026-09-23 | `dev` `74fc6619` | five units, #8748–#8751 and the `0.1.0-dev.1` pin | `cfa8cb39`, `865bcba3` | 2416/0/3, 2421/0/3 | [rc-15.0.9-additions](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/b362302b/docs/30-design/remedial/rc-15.0.9-additions-2026-09-23.md) |
| bf2 | 2026-09-23 | `dev` `74fc6619` | bf2/ops, bf2/backports, bf2/auth-hardening (`29e6430e`) | `e9a4ef62` | 2480/0/3, 2 cells | [backfix-2-integration](https://github.com/bewest/rag-nightscout-ecosystem-alignment/blob/b362302b/docs/30-design/remedial/backfix-2-integration-2026-09-23.md) |

To read a record locally: `git show b362302b:docs/30-design/remedial/<file>`, or for run 011
`git show a35e7e25:docs/30-design/remedial/rc-15.0.9-integration-record.md`, or for run 013
`git show d6399575:docs/30-design/remedial/rc-15.0.9-integration-record.md`, or for run 014
`git show 4cb0f1a0:docs/30-design/remedial/rc-15.0.9-integration-record.md`.

**How each unit adds to the suite count**, measured one step at a time with the sorted titles
compared:

| step | tests added |
|---|---:|
| count=0 answers `[]` (#8748) | +23 (26 added, 3 renamed) |
| qs 6.16 (#8749), MongoDB floor docs (#8750), connector pins | 0 |
| backports (#8751) | +12 |
| ops (#8753) | +6 |
| auth-hardening (#8754, including subject-edit and `TRUST_PROXY` hop counts) | +95 |
| alarm with no reading (#8755) | +4 |
| quick-pick rebuild (#8756) | +8 |
| mmconnect deprecation warning (#8757) | +1 |
| #8760 | +13 |
| #8761 | +18 |
| object-id CRUD (#8758) at `ab7b22d6` | +489 |
| #8758 fixes BF-115, BF-116, BF-117 at `63dd716c` | +24 (35 added, 11 renamed) |
| #8758 fix narrowing and `{$oid}` (BF-117, BF-115) at `cb7d4110` | +4 |
| #8758 treatments batch fix (BF-130) at `6c3ccce6` | +3 |
| #8758 cache removal on delete by `_id` (BF-131) at `e9dbb1fb` | +9 |

## Method

Every run follows these rules. A run that departs from one says so in its own section.

- **Candidate by SHA.** The candidate is built from `dev` by SHA, one `git merge --no-ff` per unit,
  and never pushed. Each unit's head is checked against its PR head before merging and again at the
  end of the run.
- **One mongod per cell.** Each cell gets a fresh `mongo:<ver>` container with
  `--ulimit nofile=64000:64000` (the #8758 suite peaks near 1130 open files). The server version is
  read from the container before the cell runs. The database is dropped before every run.
- **Rebuild the bundle after every merge.** The client tests run against the production webpack
  bundle, so a stale bundle reports an already-fixed defect.
- **A cell that prints no mocha totals counts as a failure.**
- **Additivity by title.** At each step, the sorted list of passing test titles is compared with the
  previous step's, leaving out lines that carry a generated ObjectId. A step may add only its own
  unit's titles.
- **Break-its count only when the failures show the defect.** A break reverts or edits one unit's
  fix, runs that unit's tests, and restores. `git status` must be clean afterwards, and the tests must
  pass again.
- **Harness red control.** The same runner is pointed at a tree or database set up to fail, to show
  that a green result depends on what it claims to test.

## What the current run does not cover

- **Not the release candidate.** 15.0.9 also carries #8568 `de8efff0` (BF-114), #8419 `b1c23e74`
  and #8530 `5353ff64`. The merge of `e9dbb1fb` with those three heads is not built. This run's
  suite cells, lab cells, replay probes, gate result and AAPS probe do not cover the merge.
  Whether the three merge cleanly onto `e9dbb1fb` is not measured.
- **The AAPS probe has not run on a tree that carries both #8568 and the #8758 fixes.**
- **No browser check on this tree.** The hand checks owed are listed under
  [Browser hand-check gate](#browser-hand-check-gate).
- **No `dev` cell in this run.** The `dev` baseline comes from run 011.
- **No MongoDB 5.0 or 6.0 cell.** CI's matrix runs 4.4, 5.0 and 6.0; this run covers 4.4.24 and
  7.0.43.
- **No break-its of #8758 or its fixes on an integrated tree.** Only the harness red control was run.
- **No lab cell for BF-130's treatments batch case.** The three new suite tests cover it.
- **The deleted-reading probe covers entries only.** Treatments and devicestatus cache removal are
  covered by the new `cache.remove-by-id` suite tests.
- **No lab cell for `cb7d4110`'s identifier narrowing.** The lab's v1/v3 pair has no second
  record with its own `identifier`. The narrowing is covered by the two new
  `api3.delete-every-form` suite tests.
- **No connector lab run on this tree.** The last one ran on `d087588f`.
- **Break-its ran on Node 20.20.0 with MongoDB 7.0.43 only.**
