# 15.0.9 combined run: dev `ddd9b600` + #8754 + #8758

*Contributor-facing. Snapshot, 2026-09-23. A combined run of the last two open 15.0.9 pull requests on
current `dev`, ordered by the maintainer ("run now, and again at the pin") so both can merge on evidence.
The earlier combined run is [rc-15.0.9-combined-2026-09-23](rc-15.0.9-combined-2026-09-23.md); this one
covers the only combination it did not: `dev` after #8760.*

## What was merged

Local branch `rc/15.0.9-combined-59`, not pushed, in `externals/work/crm-rc-combined-59`:

| step | commit | merged |
|---|---|---|
| 0 | `ddd9b600` | `origin/dev` after #8760 |
| 1 | `2731b658` | #8754 `bf2/auth-hardening` at `ef3404fd` (`--no-ff`) |
| 2 | `509235b3` | #8758 `bf/object-id-crud` at `6d120fa2` (`--no-ff`) |

Both merges were automatic (`lib/api/entries/index.js`, `lib/server/profile.js` and
`lib/server/websocket.js` auto-merged). The tip's tree is `2ce67b27ce`, the tree session -6a computed for
these three heads, which differs from `rc/15.0.9-combined-36b` `d087588f` in exactly #8760's five files.

## Method

- One fresh MongoDB container per suite, started with `--ulimit nofile=64000:64000` (the #8758 suite
  peaks near 1130 open files; a default-limit container stops partway, see BF-10).
- At each step: `npm install --ignore-scripts` against that step's lockfile (#8754 adds `proxy-addr`
  as a direct dependency; no new package), then `npm run bundle`, because the client tests read the
  built bundle.
- `npm test` (env-cmd with `NODE_ENV=test`); a suite that printed no totals would have counted as a
  failure.

## Results

| suite | Node | MongoDB | passing | failing | pending |
|---|---|---|---:|---:|---:|
| step 0, `dev` `ddd9b600` | 20.20.0 | 7 | 2453 | 0 | 3 |
| step 1, + #8754 | 20.20.0 | 7 | 2548 | 0 | 3 |
| step 2, + #8758 | 20.20.0 | 7 | 3028 | 0 | 3 |
| tip | 20.20.0 | 4.4 | 3028 | 0 | 3 |
| tip | 20.20.0 | 7 | 3028 | 0 | 3 |
| tip | 22.23.2 | 4.4 | 3028 | 0 | 3 |
| tip | 22.23.2 | 7 | 3028 | 0 | 3 |
| tip | 24.20.0 | 4.4 | 3028 | 0 | 3 |
| tip | 24.20.0 | 7 | 3028 | 0 | 3 |

Each step adds exactly its own tests: #8754 +95 (its 76, the subject-edit 5, the `TRUST_PROXY` hop-count
14) and #8758 +480. The tip's 3028 is the earlier combined run's 3015 plus #8760's 13 new tests. The
create/read/update/delete-by-`_id` matrix ("CRUD by _id matrix: v1, v3 and websocket") runs inside the
suite on every tip cell.

## What this does not cover

- Break-its were not repeated: the tree differs from `d087588f`, where they were run, only by #8760,
  whose break-its were run on its own branch.
- No browser or Nightscout-to-Nightscout lab run; those were done on `ec70aab0` and `d087588f`.
- The pin is `0.1.0-dev.3`. One more combined run is owed after the pin moves to exact `0.1.0`, before
  15.0.9 is tagged.
