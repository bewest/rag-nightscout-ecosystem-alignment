{
 "collection": "auth_subjects",
 "generated_by": "tools/nsschema/code_model.py",
 "provenance": "code-derived",
 "measured": false,
 "evidence_note": "NOT MEASURED. Every field here was read out of cgm-remote-monitor's source, so it records what the server and its built-in clients CAN write \u2014 not what any deployment DOES write, how often, or on how many sites. No node in this file carries an evidence block, and none may be given one without a census to back it. never collected. The corpus is built from public REST reads, and these collections are reachable only through the admin API \u2014 which is correct: a census of them would be a census of bearer credentials.",
 "source_root": "externals/work/crm-seam",
 "source_files": [
  "lib/authorization/storage.js",
  "lib/authorization/endpoints.js",
  "lib/admin_plugins/subjects.js"
 ],
 "source_digest": "sha256:db0b91947776e8a06f665c2cab7da9e7be8ed7fc6b09e46cfe0feb1ad458c00a",
 "reached_by": [
  "/api/v2/authorization/subjects (admin)"
 ],
 "collection_name": "<MONGO_AUTHENTICATION_COLLECTIONS_PREFIX>subjects, default auth_subjects",
 "indexed_fields": [
  "name"
 ],
 "served_fields": [
  "_id",
  "name",
  "accessToken",
  "roles"
 ],
 "credential_warning": "accessToken is the bearer credential that authorises API access. It is derived at read time, but GET /subjects serves it and the admin UI PUTs the whole subject back, so it is persisted in plaintext by an ordinary subject edit. Every field labelled secret here must be redacted before this collection is exported, logged, replicated to another tenant, or shown to a support operator.",
 "form_encoded_writer": "lib/admin_plugins/subjects.js ($.ajax with no contentType)",
 "root": {
  "path": "",
  "types": [
   "object"
  ],
  "nullable": false,
  "provenance": "structural",
  "declared": false,
  "observed": false,
  "placement": "core",
  "children": {
   "_id": {
    "path": "_id",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/authorization/storage.js"
    ],
    "sensitivity": "identifying",
    "data_category": "identity",
    "sensitivity_why": "name is on the redaction denylist",
    "required_read": true,
    "notes": [
     "server-assigned when absent; a v1 client may supply a 24-character hex string"
    ]
   },
   "accessToken": {
    "path": "accessToken",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "extension",
    "code_sources": [
     "lib/authorization/storage.js (reload)"
    ],
    "sensitivity": "secret",
    "data_category": "credential",
    "sensitivity_why": "name matches a credential fragment",
    "notes": [
     "recomputed in storage.reload() from the subject's _id, its name and the enclave API key. The server never writes it, so it is absent from a freshly created document",
     "GET /subjects returns it and the admin UI PUTs the whole subject back, so it does reach storage in plaintext when a subject is edited through the admin UI"
    ]
   },
   "accessTokenDigest": {
    "path": "accessTokenDigest",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "extension",
    "code_sources": [
     "lib/authorization/storage.js (reload)"
    ],
    "sensitivity": "secret",
    "data_category": "credential",
    "sensitivity_why": "name matches a credential fragment",
    "notes": [
     "recomputed in storage.reload() from the subject's _id, its name and the enclave API key. The server never writes it, so it is absent from a freshly created document"
    ]
   },
   "created_at": {
    "path": "created_at",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/authorization/storage.js"
    ],
    "sensitivity": "identifying",
    "data_category": "temporal",
    "sensitivity_why": "not present in the census; unlabelled defaults to identifying",
    "format": "date-time",
    "required_read": true,
    "notes": [
     "set only when absent, by save() and create()"
    ]
   },
   "digest": {
    "path": "digest",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "extension",
    "code_sources": [
     "lib/authorization/storage.js (reload)"
    ],
    "sensitivity": "secret",
    "data_category": "credential",
    "sensitivity_why": "the enclave hash of the subject's _id; the access token is built from its first 16 characters, so it is token material and not a mere identifier",
    "notes": [
     "recomputed in storage.reload() from the subject's _id, its name and the enclave API key. The server never writes it, so it is absent from a freshly created document"
    ]
   },
   "name": {
    "path": "name",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/admin_plugins/subjects.js + lib/authorization/storage.js"
    ],
    "sensitivity": "identifying",
    "data_category": "identity",
    "sensitivity_why": "name is on the redaction denylist",
    "notes": [
     "indexed; also the input to the access token's readable prefix"
    ]
   },
   "notes": {
    "path": "notes",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/admin_plugins/subjects.js"
    ],
    "sensitivity": "identifying",
    "data_category": "free-text",
    "sensitivity_why": "name is on the redaction denylist",
    "notes": [
     "free text from the admin dialog. GET /subjects does not return it, and the dialog writes back whatever the (empty) input holds, so an edit through the admin UI clears it"
    ]
   },
   "roles": {
    "path": "roles",
    "types": [
     "array"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/admin_plugins/subjects.js"
    ],
    "sensitivity": "identifying",
    "data_category": "vocabulary",
    "sensitivity_why": "not present in the census; unlabelled defaults to identifying",
    "notes": [
     "role names, resolved against auth_roles and the built-in defaults at authorization time",
     "an empty array is dropped entirely by form encoding, so this field can be absent rather than empty when written by lib/admin_plugins/subjects.js ($.ajax with no contentType)"
    ],
    "children": {
     "[]": {
      "path": "roles[]",
      "types": [
       "string"
      ],
      "nullable": false,
      "provenance": "code",
      "declared": false,
      "observed": false,
      "placement": "core",
      "code_sources": [
       "lib/admin_plugins/subjects.js"
      ],
      "sensitivity": "identifying",
      "data_category": "vocabulary",
      "sensitivity_why": "not present in the census; unlabelled defaults to identifying"
     }
    }
   }
  }
 }
}
