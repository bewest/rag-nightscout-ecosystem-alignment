{
 "collection": "auth_roles",
 "generated_by": "tools/nsschema/code_model.py",
 "provenance": "code-derived",
 "measured": false,
 "evidence_note": "NOT MEASURED. Every field here was read out of cgm-remote-monitor's source, so it records what the server and its built-in clients CAN write \u2014 not what any deployment DOES write, how often, or on how many sites. No node in this file carries an evidence block, and none may be given one without a census to back it. never collected. The corpus is built from public REST reads, and these collections are reachable only through the admin API.",
 "source_root": "externals/work/crm-seam",
 "source_files": [
  "lib/authorization/storage.js",
  "lib/authorization/endpoints.js",
  "lib/admin_plugins/roles.js"
 ],
 "source_digest": "sha256:a0a1e71a24ca054fd8d2deaf74ddaf20faac6f8b1b40a9fdb4af9d59b11a3cad",
 "reached_by": [
  "/api/v2/authorization/roles (admin)"
 ],
 "collection_name": "<MONGO_AUTHENTICATION_COLLECTIONS_PREFIX>roles, default auth_roles",
 "indexed_fields": [
  "name"
 ],
 "built_in_roles": [
  "admin",
  "denied",
  "status-only",
  "readable",
  "careportal",
  "devicestatus-upload",
  "activity"
 ],
 "built_in_roles_note": "declared in storage.defaultRoles and merged into the in-memory role list at every reload, so they are served by GET /roles without existing as documents. They carry no _id and no created_at, which is how the admin UI tells them apart.",
 "known_absent_fields": {
  "autoGenerated": "the admin dialog deletes it before saving and nothing in cgm-remote-monitor ever sets it; it is a guard against a producer this repository cannot see, so it is recorded here rather than modelled as a field"
 },
 "form_encoded_writer": "lib/admin_plugins/roles.js ($.ajax with no contentType)",
 "root": {
  "path": "",
  "types": [
   "object"
  ],
  "nullable": false,
  "provenance": "structural",
  "declared": false,
  "observed": false,
  "placement": "core",
  "children": {
   "_id": {
    "path": "_id",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/authorization/storage.js"
    ],
    "sensitivity": "identifying",
    "data_category": "identity",
    "sensitivity_why": "name is on the redaction denylist",
    "required_read": true,
    "notes": [
     "server-assigned when absent; a v1 client may supply a 24-character hex string"
    ]
   },
   "created_at": {
    "path": "created_at",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/authorization/storage.js"
    ],
    "sensitivity": "identifying",
    "data_category": "temporal",
    "sensitivity_why": "not present in the census; unlabelled defaults to identifying",
    "format": "date-time",
    "required_read": true,
    "notes": [
     "set only when absent, by save(); create() sets it the same way"
    ]
   },
   "name": {
    "path": "name",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/authorization/storage.js (defaultRoles) + lib/admin_plugins/roles.js"
    ],
    "sensitivity": "identifying",
    "data_category": "identity",
    "sensitivity_why": "name is on the redaction denylist",
    "notes": [
     "indexed; roleToShiro() looks a role up by this name"
    ]
   },
   "notes": {
    "path": "notes",
    "types": [
     "string"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/admin_plugins/roles.js"
    ],
    "sensitivity": "identifying",
    "data_category": "free-text",
    "sensitivity_why": "name is on the redaction denylist",
    "notes": [
     "free text from the admin dialog"
    ]
   },
   "permissions": {
    "path": "permissions",
    "types": [
     "array"
    ],
    "nullable": false,
    "provenance": "code",
    "declared": false,
    "observed": false,
    "placement": "core",
    "code_sources": [
     "lib/authorization/storage.js (defaultRoles)"
    ],
    "sensitivity": "identifying",
    "data_category": "vocabulary",
    "sensitivity_why": "not present in the census; unlabelled defaults to identifying",
    "notes": [
     "shiro-trie permission strings; `*` is the wildcard and `:` the hierarchy separator",
     "an empty array is dropped entirely by form encoding, so this field can be absent rather than empty when written by lib/admin_plugins/roles.js ($.ajax with no contentType)"
    ],
    "children": {
     "[]": {
      "path": "permissions[]",
      "types": [
       "string"
      ],
      "nullable": false,
      "provenance": "code",
      "declared": false,
      "observed": false,
      "placement": "core",
      "code_sources": [
       "lib/authorization/storage.js (defaultRoles)"
      ],
      "sensitivity": "identifying",
      "data_category": "vocabulary",
      "sensitivity_why": "not present in the census; unlabelled defaults to identifying"
     }
    }
   }
  }
 }
}
