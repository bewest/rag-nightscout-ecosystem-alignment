# NightscoutKit (LoopKit) and its Loop consumers — Nightscout API surface

All claims **read-derived** (source read with `git show` / `git grep` on the named commit; nothing
was run, no server contacted).

## What was analysed

| component | repo | ref analysed | how obtained | local staleness |
|---|---|---|---|---|
| NightscoutKit (library) | externals/NightscoutKit | `origin/main` **4ec9fd1** 2026-06-26 | git grep/show on ref | behind=0, dirty=0 |
| Loop's pin of NightscoutKit | externals/LoopWorkspace | `origin/main` **f841285** 2026-09-19, `Package.resolved` pins NightscoutKit `branch: main`, revision **4ec9fd1** (= the ref above) | `git show origin/main:LoopWorkspace.xcworkspace/xcshareddata/swiftpm/Package.resolved` | LoopWorkspace local HEAD 4319ce5 is 79 behind; analysed origin/main, not the working tree |
| NightscoutService (Loop's uploader + remote commands) | LoopWorkspace submodule | recorded commit **fe075ef** 2026-09-17 (`git -C LoopWorkspace ls-tree origin/main`) | the submodule directory's checkout is at 7721a8d (2026-05-12), **not** the recorded commit; the recorded commit's objects are present, so it was analysed with `git -C LoopWorkspace/NightscoutService grep/show fe075ef` | — |
| NightscoutRemoteCGM (Loop's "Nightscout as CGM") | LoopWorkspace submodule | recorded commit **e2230e3** 2026-09-05 | objects present; analysed at the recorded commit (checkout is at 383d3c1) | — |
| Loop app | LoopWorkspace submodule | recorded commit **c2fddb7** 2026-09-19 | analysed at recorded commit | — |
| LoopKit | LoopWorkspace submodule | recorded commit **325bd82** 2026-09-17 | analysed at recorded commit | — |

**Checkout refresh (coordinator, 2026-09-23):** the main session has since moved LoopWorkspace to origin/main f841285 with every submodule checked out at its recorded commit. Verified: NightscoutService fe075ef, NightscoutRemoteCGM e2230e3, Loop c2fddb7 and LoopKit 325bd82 are now each HEAD. These are the same commits this file analysed through git objects, so no anchor changes. NightscoutKit is an SPM package, not a submodule, and was read from externals/NightscoutKit at 4ec9fd1, the revision Package.resolved pins.

`origin/dev` of LoopWorkspace records the same NightscoutService/NightscoutRemoteCGM commits as
`origin/main`. **`origin/next-dev` differs**: NightscoutService **5379bba** (2026-06-02, not an
ancestor of fe075ef — a separate async-rewrite lineage), NightscoutRemoteCGM **1a73e9e**, and
NightscoutKit pinned at **ca8e2ce** (an ancestor of 4ec9fd1, missing the `isAPNSProduction` and
`isCharging` commits). Differences on hits are noted per row.

Only NightscoutKit builds URLs. NightscoutService, NightscoutRemoteCGM and Loop call NightscoutKit's
`NightscoutClient` methods; they contain no `api/v` literal of their own.

### Positive controls (same grep method reaches the Nightscout code)

- NightscoutKit: `git grep -n 'api/v' origin/main -- Sources` hits the endpoint enum at
  `NightscoutKit@4ec9fd1 Sources/NightscoutKit/NightscoutClient.swift:13-20`
  (`/api/v1/entries`, `/treatments`, `/devicestatus`, `/experiments/test`, `/profile`, `/profiles`,
  `/profile/current`, `/api/v2/notifications/loop`).
- NightscoutService: `git grep -n 'upload(\|deleteTreatments\|modifyTreatments\|uploadProfiles\|fetchCurrentProfile\|checkAuth' fe075ef`
  hits `NightscoutService@fe075ef NightscoutServiceKit/NightscoutService.swift:112,167,174,304,345,367,376,511`
  and `NightscoutServiceKit/Extensions/NightscoutUploader.swift:20,43,66,85,107,139,163`.
- NightscoutRemoteCGM: `git grep -n 'fetchGlucose\|maxCount' e2230e3` hits
  `NightscoutRemoteCGM@e2230e3 NightscoutRemoteCGM/NightscoutFetcher.swift:28,30`.
- Loop: `git grep -c remoteDataService c2fddb76 -- Loop/Managers/RemoteDataServicesManager.swift` = 103.

Absence searches across Loop / LoopKit / NightscoutService / NightscoutRemoteCGM at the recorded
commits (pattern, case-insensitive, excluding tests and `.strings`):
`api/v[123]|socket\.io|SocketIO|socketio|"authorize"|api-secret|verifyauth|/api/v2/|pebble|find\[` — **no hits** in
any of the four, consistent with all HTTP going through NightscoutKit.

## Surface table

| S-id | what the client does | anchor (repo@sha path:line) | patterns searched |
|---|---|---|---|
| S1 | `count` is sent only when a caller passes `maxCount`, as `String(Int)`, on **GET** only (profiles, devicestatus, treatments, entries). No DELETE carries `count`. The only in-Loop caller is NightscoutRemoteCGM: `maxCount = (minutes / 5) * 2` with integer division and `minutes` defaulting to 60 → **count=24**. Both call sites use the default (`fetchRecent()` / `fetchRecent { }`), so the value is always 24; it would be 0 only if a caller passed minutes<5, and none does. Never fractional (Int), never huge. NightscoutService never calls `fetchGlucose/fetchTreatments/fetchDeviceStatus/fetchProfiles` (those are used by other apps, e.g. LoopCaregiver — see its file). next-dev: NightscoutFetcher diff touches only type imports; count unchanged. | NightscoutKit@4ec9fd1 NightscoutClient.swift:192-194, 230-232, 268-270, 306-308; NightscoutRemoteCGM@e2230e3 NightscoutRemoteCGM/NightscoutFetcher.swift:23,28,30; NightscoutRemoteCGM.swift:114; NightscoutAPIService.swift:52 | `count`, `maxCount`, `fetchRecent`, `URLQueryItem(name: "count"` |
| S2 | Only `$gte` and `$lte`: `find[startDate][$gte/$lte]` (profiles), `find[created_at][$gte/$lte]` (devicestatus, treatments), `find[dateString][$gte/$lte]` (entries). Values are ISO-8601 strings from `ISO8601DateFormatter` (no fractional seconds). No `$exists`, `$in`, `$regex`, `$ne`, `$or`, `$expr`, `pipeline`. | NightscoutKit@4ec9fd1 NightscoutClient.swift:188-189, 226-227, 264-265, 302-303; Extensions/TimeFormat.swift:15-17 | `find\[`, `\$exists`, `\$in`, `\$regex`, `\$ne`, `\$or`, `\$expr`, `pipeline` |
| S3 | Filters compare **string** date fields only (`dateString`, `created_at`, `startDate`). In the candidate's generated `lib/server/query-coercion.json` (cgm-remote-monitor@6d120fa2) none of these three is typed — only `date`/`mills`/`srvModified`/`sgv` and numeric treatment fields are — so the comparison stays string-vs-string, as on 15.0.8. No numeric filter (insulin, carbs, duration, sgv) is sent. Loop's reads are therefore not expected to change result set from the coercion work. | as S2; server check: cgm-remote-monitor@6d120fa2 lib/server/query-coercion.json (collections.entries/treatments/devicestatus/profile) | `find\[`, field names `dateString`,`created_at`,`startDate`,`date`,`insulin`,`carbs`,`duration` |
| S4 | `GET /api/v1/experiments/test` is the auth check (`checkAuth`), used by NightscoutService's `verifyConfiguration` at setup. `GET /api/v1/profile/current` is read by `fetchStoredTherapySettings`. No `/count`, `/times`, `/slice`, `/echo`, `/api/v2/properties`, `/api/v2/ddata`, `/status`, `/verifyauth`. | NightscoutKit@4ec9fd1 NightscoutClient.swift:16,19,166-182,692-725; NightscoutService@fe075ef NightscoutServiceKit/NightscoutService.swift:105-113,370-389 | `api/v1/count`, `times`, `slice`, `echo`, `properties`, `ddata`, `status`, `verifyauth`, `experiments` |
| S5 | **`api-secret` header = SHA-1 hex of the configured secret**, on every request. No `token=` query, no `Authorization: Bearer`, no `/api/v2/authorization/*` use, no subject/role creation, no `X-Forwarded-For`. `callNS` skips the header when the secret is empty; the async `postToNS` (remote-command notifications) and `checkAuth` send it whenever the string is non-nil. `checkAuth`: 401 → `.unauthorized`, other non-200 → `.httpError(status, body)`. Uses `URLSession.shared` default timeouts (no custom timeout found); no retry on 401/403. Loop requires a non-empty secret (`hasConfiguration`). | NightscoutKit@4ec9fd1 NightscoutClient.swift:524-526, 609-611, 702-704, 711-719; Extensions/String+Crypto.swift:15-17; NightscoutService@fe075ef NightscoutService.swift:103 | `api-secret`, `sha1`, `token`, `Bearer`, `authorization`, `X-Forwarded`, `timeoutInterval`, `401`, `403` |
| S6 | **No websocket.** No socket.io client library in NightscoutKit or in LoopWorkspace's `Package.resolved` (identities listed: amplitude-ios, analytics-connector-ios, base32, bluecryptor, blueecc, bluersa, cryptoswift, kituracontracts, loggerapi, mixpanel-swift, mkringprogressview, nightscoutkit, onetimepassword, slidebutton, swift-jwt, swift-log, swiftcharts, tidepoolkit, zipfoundation). | LoopWorkspace@f841285 LoopWorkspace.xcworkspace/xcshareddata/swiftpm/Package.resolved | `socket`, `SocketIO`, `socket.io`, `EIO`, `authorize`, `loadRetro`, `dataUpdate`, `alarm`, `dbAdd` |
| S7 | Three distinct `_id` behaviours, all on **treatments**: **(a) Temporary Override treatments are POSTed with `_id` = the override's `syncIdentifier.uuidString` — an upper-case UUID string** — and deleted with **`DELETE /api/v1/treatments/<UPPER-CASE-UUID>`** (no query string, no count). The server has explicit handling for this (`normalizeTreatmentId`: non-hex string `_id` → `identifier` when `UUID_HANDLING` is on, otherwise the `_id` is dropped) — unchanged in shape between 15.0.8 and 6d120fa2 at read. **(b) Carbs: POST without `_id`; the `_id` strings from the POST response are cached per `syncIdentifier` for 24 h** (`ObjectIdCache`), then used for `PUT /api/v1/treatments` with `_id` in the body (modify) and `DELETE /api/v1/treatments/<24-hex>` (delete). **(c) Doses (bolus, temp basal, suspend): POST without `_id`** — the `id: objectId` argument is deliberately commented out — carrying `syncIdentifier` (a non-NS field) instead; dedup is left to the server's time+eventType upsert. Deletes of doses use the cached 24-hex `_id`. Entries: POST without `_id` (`GlucoseEntry` built with `id: nil`). Devicestatus: no `_id`; NightscoutKit can send a top-level **`identifier`** field but Loop's `deviceStatus(...)` does not set one. Profiles: no `_id` on POST. Reads `_id` back from every POST response and requires the response array length to equal the request length (else failure); a missing `_id` becomes `"NA"` and is skipped on later PUT/DELETE. No upper-case hex ids (server returns lower-case hex); the UUIDs are upper-case. No v3. next-dev: same UUID `_id` on overrides (OverrideTreament.swift:63). | NightscoutKit@4ec9fd1 Models/Treatments/NightscoutTreatment.swift:105-120; Models/Treatments/OverrideTreatment.swift:25-31; NightscoutClient.swift:74-100, 106-128, 134-156, 430-445, 479-511; Models/GlucoseEntry.swift:101-108; Models/DeviceStatus.swift:60-62; NightscoutService@fe075ef NightscoutServiceKit/Extensions/OverrideTreament.swift:59; NightscoutService.swift:157-186, 203-236, 246-272; Extensions/NightscoutUploader.swift:30-73, 120-170; Extensions/DoseEntry.swift:30,61; Extensions/SyncCarbObject.swift:16-26; Extensions/StoredGlucoseSample.swift:34-42; NightscoutService.swift:27 (24 h cache) | `_id`, `id:`, `objectId`, `syncIdentifier`, `uuidString`, `identifier`, `DELETE`, `PUT` |
| S8 | Edit = **PUT `/api/v1/treatments`** with the full treatment and `_id` in the body (carbs only). Override edits are delete-then-re-POST under the same UUID. Doses are never edited, only re-POSTed/deleted. `created_at` and `timestamp` both sent as ISO-8601 without fractional seconds (remote-carb notification uses fractional seconds). No split/merge. Loop does not re-read treatments, so it does not depend on deleted records staying deleted. | NightscoutKit@4ec9fd1 NightscoutClient.swift:74-100, 447-456; NightscoutTreatment.swift:106-109; NightscoutClient.swift:379-383 | `PUT`, `modifyTreatments`, `created_at`, `timestamp` |
| S9 | none found — no `/api/v1/food`, no quickpick. | — | `food`, `quickpick`, `hideafteruse`, `position` |
| S10 | **POST `/api/v1/profile`** with an array of up to **400** profile sets per batch (`settingsDataLimit`), **no `_id`**, `defaultProfile: "Default"`, one store entry `"Default"`, `startDate` ISO string, **`mills` as a string** (`String(format: "%.0f")`), `units`, `enteredBy: "Loop"`, `loopSettings` (includes `deviceToken`, `bundleIdentifier` — the APNS routing data Nightscout uses for remote commands), optional `isAPNSProduction` (not in next-dev's NightscoutKit pin ca8e2ce). `updateProfile` (PUT with `_id`) exists in NightscoutKit but **has no caller** in NightscoutService. Reads `GET /api/v1/profile/current` and requires `_id` to be a string. No profile-switch treatments. | NightscoutKit@4ec9fd1 NightscoutClient.swift:398-420; Models/ProfileSet.swift:143-165, 177; NightscoutService@fe075ef NightscoutService.swift:359-368; Extensions/StoredSettings.swift:50-61, 96-106 | `profile`, `updateProfile`, `uploadProfiles`, `mills`, `defaultProfile`, `startDate`, `Profile Switch` |
| S11 | Writes devicestatus with `device: "loop://<device name>"`, `created_at`, `pump`, `uploader` (battery, isCharging), `override`, and **`loop.iob = {timestamp, iob}`** (`basaliob` supported by the model, set nil by Loop) and **`loop.cob = {timestamp, cob}`**, plus `loop.predicted`, `automaticDoseRecommendation`, `recommendedBolus`, `enacted`, `failureReason`. Devicestatus is only uploaded for dosing decisions with reasons `updateRemoteRecommendation`, `normalBolus`, `simpleBolus`, `watchBolus`, paired with the last `loop` decision. Loop **does not read** Nightscout-computed COB/IOB. | NightscoutKit@4ec9fd1 Models/LoopStatus.swift:51-96; Models/COBStatus.swift:25-26; Models/IOBStatus.swift:28-35; Models/DeviceStatus.swift:34-65; NightscoutService@fe075ef Extensions/StoredDosingDecision.swift:16-27, 148-164; NightscoutService.swift:274-313 | `cob`, `iob`, `COB`, `IOB`, `loop`, `openaps`, `properties`, `pebble` |
| S12 | Query strings built with `URLComponents.queryItems`; bracket notation; no arrays, no `$in`, max 3 query items. Note: `url(with:)` rebuilds the URL from scheme/host/port and **drops any path prefix** of the site URL (pre-existing; a site under a sub-path cannot be used). DELETE/PUT/POST bodies are JSON arrays (POST up to 1000 treatments/entries per batch; 400 profiles; 50 devicestatus). | NightscoutKit@4ec9fd1 NightscoutClient.swift:43-51; NightscoutService@fe075ef NightscoutService.swift:189,195,238,274,315,326,359 | `queryItems`, `\$in`, `[]=`, `DataLimit` |
| S13 | none found — no API v3. | — | `api/v3`, `limit`, `lastModified`, `sort$desc`, `identifier` (only the devicestatus v1 field) |
| S14 | NightscoutKit **sends** Loop remote commands: `POST /api/v2/notifications/loop` with string-valued JSON: `Temporary Override` (`reason`, `reasonDisplay`, `duration` minutes as string, `notes: ""`), `Temporary Override Cancel` (`duration: "0"`), `Remote Bolus Entry` (`remoteBolus`, `otp`), `Remote Carbs Entry` (`remoteCarbs`, `remoteAbsorption` in hours, `otp`, optional `created_at`). Non-200 throws `httpError(status, body)` with the body text. (Callers are follower apps such as LoopCaregiver; Loop itself does not call these.) Loop **receives** commands via APNS (Nightscout pushes), validates OTP locally, and replies to the caregiver **directly via APNS** (not via Nightscout). On a command error it POSTs a `Note` treatment whose `notes` contain the error text plus the whole notification JSON. No Alexa/Google Home, no `/api/v1/notifications/ack`. | NightscoutKit@4ec9fd1 NightscoutClient.swift:336-394, 604-625; NightscoutService@fe075ef NightscoutService.swift:401-485, 487-520; RemoteCommands/RemoteNotificationResponseManager.swift:89-109 | `notifications/loop`, `otp`, `remoteBolus`, `remoteCarbs`, `ack`, `alexa`, `googlehome` |
| S15 | **Any non-200 is a failure** (`callNS`); response body wrapped into `NightscoutError`. In Loop's `RemoteDataServicesManager`, a failed batch does **not** advance the query anchor, so the same batch (up to 1000 records) is re-sent on the next trigger — a **persistent 400 on any record in a batch stalls that data type indefinitely (head-of-line)**. Also a POST whose response array length differs from the request length is a failure → same stall. Two specific pre-existing defects make 4xx outcomes worse: **(i) overrides** — if the `DELETE /api/v1/treatments/<UUID>` step returns an error, NightscoutService logs it and **never calls `completion`**, so Loop's override upload queue blocks on `semaphore.wait()` until the app is relaunched; **(ii) pump events** — `uploadPumpEventData` calls `completion(.success(false))` synchronously after starting the upload, so the anchor advances and a failed pump-event batch is **silently dropped** (completion also fires a second time). next-dev: (ii) is fixed (async/throws, would stall instead of drop); (i) is restructured into async/throws (the error path was not fully read). Empty array on GET: NightscoutRemoteCGM returns `.noData`; no fallback. Profile/current failure surfaces as `incompatibleTherapySettings` or the error. | NightscoutKit@4ec9fd1 NightscoutClient.swift:545-548, 576-579, 488-490; Loop@c2fddb7 Loop/Managers/RemoteDataServicesManager.swift:404-431, 452-463, 520-547; NightscoutService@fe075ef NightscoutService.swift:167-185 (no completion on delete error), 345-356 (early success); NightscoutService@5379bba NightscoutService.swift:338-366; NightscoutRemoteCGM@e2230e3 NightscoutRemoteCGM.swift:118-125 | `statusCode != 200`, `failure`, `completion(`, `setQueryAnchor`, `semaphore`, `isEmpty` |

## Join questions (ranked: data stops > wrong numbers > cosmetic)

1. **Data stops (overrides queue hang).** Does 15.0.9 (dev ddd9b600 + #8754 + #8758) answer
   `DELETE /api/v1/treatments/<upper-case UUID>` (no `count`, no `find`) with anything other than
   HTTP 200, with or without `UUID_HANDLING`? The server injects `count=10` when absent
   (`lib/api/treatments/index.js` `delete_records`); confirm that injected value passes the new count
   validation on DELETE and that the `_id` normaliser still maps a non-hex, upper-case string without
   a 400. Any non-200 here hangs Loop's override upload queue until relaunch (NightscoutService
   fe075ef:167-185). #8758 says "the 400 for a non-hex `_id` on the other v1 routes ... unchanged" —
   confirm treatments DELETE is not one of those routes.
2. **Data stops (treatments/entries batch stall).** Does any change make `POST /api/v1/treatments`
   or `/entries` (arrays of up to 1000, no `_id` except override UUIDs) return non-200, or a response
   array whose length differs from the request (e.g. dedup collapsing two identical items)? Either
   stalls the whole data type. In particular: an override POST carrying a UUID `_id` together with
   the #8758 `staleFormsFor`/`withStaleStringsRemoved` path — confirm non-hex ids are skipped and the
   bulk response still has one element per input.
3. **Data stops (carb edit/delete).** Loop caches the `_id` string from the treatments POST response
   and later PUTs/DELETEs with it. Confirm the candidate still returns a lower-case 24-hex string `_id`
   for every element of a treatments POST response (including upserted-by-identifier and deduped
   ones), and that `PUT /api/v1/treatments` with that `_id` in the body updates rather than inserting a
   second copy.
4. **Data stops (profile / APNS routing).** Confirm `POST /api/v1/profile` with a 400-element array,
   no `_id`, and `mills` as a *string* is accepted unchanged, and that the remote-command push path
   still reads `loopSettings.deviceToken`/`bundleIdentifier`/`isAPNSProduction` from the latest
   profile.
5. **Wrong numbers (COB display).** The release notes say Nightscout now shows the looping app's COB.
   Confirm the server reads Loop's shape `devicestatus.loop.cob.cob` (with `loop.cob.timestamp`), and
   note that Loop only uploads devicestatus for certain decision reasons, so the COB age may lag.
6. **Cosmetic/setup.** `GET /api/v1/experiments/test` with the SHA-1 `api-secret` header must still
   return 200 for a valid secret and 401 (not 403 or a delayed reply beyond URLSession's default
   timeout) for a wrong one; the throttle change should only affect failed attempts.
7. **Remote-command senders (via LoopCaregiver).** `POST /api/v2/notifications/loop` with string
   `duration`/`remoteBolus`/`remoteCarbs`/`remoteAbsorption` values: confirm the candidate's clearer
   error bodies are still non-200 for failures and 200 for success (NightscoutKit treats only 200 as
   success).
