# LoopCaregiver — Nightscout API / websocket usage (read-derived)

All claims below are **read-derived** from source at the refs named; nothing was run.

## Header

| | |
|---|---|
| repo | externals/LoopCaregiver (upstream LoopKit/LoopCaregiver) |
| ref analysed | `origin/dev` **2305718** (2026-09-22) — the default branch (`origin/HEAD -> origin/dev`) |
| local checkout staleness | HEAD 8ce464b, 7 behind origin/dev, 5 dirty files (working tree ignored) |
| Nightscout client library | **not** LoopKit/NightscoutKit. All three `Package.resolved` files pin **gestrich/NightscoutKit.git**, branch `feature/2023-07/bg/remote-commands`, revision **d63fb73** (2023-07-05, "Override interface updates for Caregiver"): `LoopCaregiver@2305718 LoopCaregiverKit/Package.resolved`, `LoopCaregiver.xcworkspace/xcshareddata/swiftpm/Package.resolved`, `LoopCaregiver/LoopCaregiver.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved` |
| where the fork was read | the fork is **not** in `externals/` (commit d63fb73 is absent from `externals/NightscoutKit`). It was read, read-only via `git show d63fb737:<path>`, from a separate local clone whose `origin` is gestrich/NightscoutKit: `a separate local clone of gestrich/NightscoutKit`. Anchors below written `gestrich/NightscoutKit@d63fb73`. That clone was not fetched by me; the commit object matches the pinned full sha `d63fb7370522f0dd441cd790aa960e991e118cda`. |

### Positive controls (same `git grep <pattern> origin/dev` method reaches the Nightscout code)

- `LoopCaregiver@2305718 LoopCaregiverKit/Sources/LoopCaregiverKit/Nightscout/NightscoutDataSource.swift:37` — `nightscoutUploader.fetchGlucose(dateInterval:maxCount:)` (entries read).
- `LoopCaregiver@2305718 LoopCaregiverKit/Sources/LoopCaregiverKit/Nightscout/NightscoutDataSource.swift:294` — `fetchTreatments(dateInterval:maxCount:)` (treatments read).
- Library side: `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:13-20` — endpoint enum `/api/v1/entries`, `/api/v1/treatments`, `/api/v1/devicestatus`, `/api/v1/experiments/test`, `/api/v1/profile`, `/api/v1/profile/current`, `/api/v2/notifications/loop`, `/api/v2/remotecommands`.

Patterns used over LoopCaregiver (`git grep -n <p> origin/dev -- '*.swift'`, tests excluded): `api/v`, `maxCount`, the NightscoutClient method names (`fetchGlucose|fetchTreatments|fetchDeviceStatus|fetchProfiles|fetchCurrentProfile|fetchRemoteCommands|deleteRemoteCommands|uploadRemoteCommand|deleteTreatment|updateTreatment|uploadTreatments|uploadProfile|updateProfile|postNotification`), `URLSession`, `"/api`, `socket`, `token=`, `Bearer`, `verifyauth`, `properties`, `pebble`, `authorization`, `X-Forwarded`, `api-secret`, `.cob`/`.iob`, `statusCode|httpError|unauthorized`. Over the fork file: `api/v|count|find|\$|api-secret|token|httpMethod|"_id"|PUT|DELETE|statusCode|limit|socket|notifications`.

## S1–S15

| S-id | what the client does | anchor (repo@sha path:line) | patterns searched |
|---|---|---|---|
| S1 count | Every `count` is a compile-time-constant `Int`, never 0/fractional/huge. Entries and treatments: `maxFetchCount()` = `Int((abs(lookback)+abs(lookback))/60*2)` with lookback fixed at −86400 s → **5760** (the formula adds the lookback twice instead of lookback+lookahead; still constant). Devicestatus: **no count** sent (server default applies) over a 2-hour `created_at` window. `/profile/current`: no count. Remote-commands 2.0 fetch: **50** (endpoint not on official Nightscout, see S14). All GETs; no DELETE carries `count`. | `LoopCaregiver@2305718 LoopCaregiverKit/Sources/LoopCaregiverKit/Nightscout/NightscoutDataSource.swift:91-118`, `:37`, `:78-79`, `:294`; `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:230-231,268-269,307-308,736-740` | `maxCount`, `"count"`, `count=` |
| S2 operators | `find[dateString][$gte]/[$lte]` (entries, profile), `find[created_at][$gte]/[$lte]` (treatments, devicestatus, remotecommands), `find[status.state][$eq]` (remotecommands only). All inside the allowlist. No `$exists`, no `$expr`, no `pipeline`. | `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:188-189,226-227,264-265,303-304,738,743,880` | `find\[`, `\$` |
| S3 numeric comparisons | None. All range filters are on string date fields (`dateString`, `created_at`) with values from `ISO8601DateFormatter` (`…Z`, no `+` offset), so neither schema coercion nor the `+`-offset repair in `enforceDateFilter` changes them. No numeric-field `$gt/$gte/$lt/$lte`. | `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/Extensions/TimeFormat.swift:15-17` | `\$gte`, `\$lte`, `\$gt`, `\$lt` on any numeric field name |
| S4 shared/aggregate reads | `GET /api/v1/experiments/test` as the auth probe (`checkAuth`); `GET /api/v1/profile/current`. No `/count`, `/times`, `/slice`, `/echo`, `/api/v2/properties`, `/ddata`, `/status`, `/verifyauth`. | `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:16,18,695-725`; `LoopCaregiver@2305718 …/NightscoutDataSource.swift:124-134,192-198` | `count/`, `times/`, `slice/`, `echo/`, `properties`, `ddata`, `status`, `verifyauth` |
| S5 auth | Only the `api-secret` header, value = SHA-1 of the stored "secret key" (so it works only with the site's `API_SECRET`, not with an access token). No `token=`, no JWT/`Bearer`, no subject/role creation, no `X-Forwarded-For`. `checkAuth` maps 401 → `.unauthorized`, other non-200 → `httpError`. No timeout/slow-reply handling beyond URLSession defaults. Remote bolus/carbs also carry an `otp` field (TOTP from the Looper's QR). | `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:526-528,611-613,705-707,716-717`; `LoopCaregiver@2305718 …/NightscoutDataSource.swift:19` | `api-secret`, `token=`, `Bearer`, `authorization`, `X-Forwarded` |
| S6 websocket | **none found** in LoopCaregiver or the fork. Polling only. | — | `socket`, `SocketIO`, `authorize`, `dataUpdate`, `loadRetro`, `/alarm` |
| S7 `_id` | LoopCaregiver writes no v1 collection records, so sends no `_id`. The fork's `postToNS` would read `_id` back from POST responses (falls back to `"NA"`), and `updateProfile` would PUT a profile with a caller-supplied `_id`, but **LoopCaregiver has no call site** for either. Remote-commands 2.0 `updateRemoteCommand` PUTs `/api/v2/remotecommands/<id>` (non-official endpoint; not called by LoopCaregiver). | `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:413-422,495-506,798-825` | `"_id"`, `updateProfile`, `uploadProfile`, `deleteTreatment`, `updateTreatment` |
| S8 treatments edit | **none found** — no v1 treatment POST/PUT/DELETE. Commands are sent as Loop notifications, which Loop turns into its own treatments. Remote-commands 2.0 "delete all" issues `DELETE /api/v2/remotecommands?find[created_at][$gte]=<1 year ago>` (non-official endpoint). | `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:873-914`; `LoopCaregiver@2305718 …/NightscoutDataSource.swift:212-218` | `uploadTreatments`, `deleteTreatment`, `updateTreatment`, `DELETE` |
| S9 food | **none found** | — | `food`, `quickpick` |
| S10 profile | Reads `GET /api/v1/profile/current` and uses `ProfileSet` (override presets, settings). No profile writes, no profile-switch treatments. | `LoopCaregiver@2305718 …/NightscoutDataSource.swift:192-198`; `…/Models/RemoteDataServiceManager.swift:174-180` | `fetchCurrentProfile`, `uploadProfile`, `updateProfile`, `Profile Switch` |
| S11 COB/IOB | Displays **devicestatus** `loop.iob` / `loop.cob` from the newest devicestatus in a 2 h window (Loop-computed). Does not read Nightscout-computed COB/IOB (no v2 properties, no pebble). Unaffected by the 15.0.9 web-COB change. | `LoopCaregiver@2305718 LoopCaregiverKit/Sources/LoopCaregiverKit/Models/RemoteDataServiceManager.swift:149-157` | `.cob`, `.iob`, `properties`, `pebble` |
| S12 query shape | Bracket notation via `URLQueryItem`, one scalar per key. No arrays, no `$in`, max 3 find keys per request, no valueless parameters. | `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:187-197` | `\$in`, `\$nin`, `\[\]` |
| S13 API v3 | **none found** | — | `api/v3`, `identifier`, `lastModified`, `limit` |
| S14 remote commands | Default path: `POST /api/v2/notifications/loop` with string-valued JSON — `Remote Bolus Entry` {`remoteBolus`, `otp`}, `Remote Carbs Entry` {`remoteCarbs`, `remoteAbsorption` (hours), `otp`, optional `created_at`}, `Temporary Override` {`reason`, `reasonDisplay`, `duration` minutes, `notes`}, `Temporary Override Cancel` {`duration: "0"`}. Success requires HTTP **200**; any other status throws `httpError(status, body)` and the body is shown to the user (`errorText = error.localizedDescription`). On candidate the server still answers 200/500 (unchanged `lib/server/loop.js`), but the 500 body is now fixed explanatory text from `lib/api2/loop-notification-errors.js` rather than the raw error — LoopCaregiver will display the new text. Opt-in "Remote Commands 2" (experimental toggle) instead uses `/api/v2/remotecommands` GET/POST/PUT/DELETE, **which does not exist on cgm-remote-monitor** (`git grep remotecommands origin/dev -- lib` is empty); it targets a fork of the server. | `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:338-398,601-627,836-860`; `LoopCaregiver@2305718 …/NightscoutDataSource.swift:136-190`; `…/Views/Actions/BolusInputView.swift:144-149`; `…/Views/Settings/SettingsView.swift:246-248` | `notifications`, `remotecommands`, `otp`, `Remote Bolus`, `remoteCommands2Enabled` |
| S15 errors / empty | Any non-200 GET becomes `NightscoutError.httpError`. The refresh loop runs `updateGlucoseData()` first, then a `withThrowingTaskGroup` of carbs/bolus/basal/override/devicestatus/remote-commands/profile: a throw from glucose skips the whole cycle, a throw from any child cancels its siblings; the error is only `print`ed and the next timer tick retries. Nothing is uploaded, so nothing can be dropped or marked-uploaded. An empty glucose array replaces the chart data but leaves `currentGlucoseSample` at its last value. With Remote Commands 2 enabled against official Nightscout the non-existent endpoint fails every cycle and cancels sibling fetches (pre-existing, not a 15.0.9 change). | `LoopCaregiver@2305718 LoopCaregiverKit/Sources/LoopCaregiverKit/Models/RemoteDataServiceManager.swift:51-108`; `gestrich/NightscoutKit@d63fb73 Sources/NightscoutKit/NightscoutClient.swift:547-549,578-580` | `statusCode`, `httpError`, `catch`, `print("Error` |

## Join questions (ranked by how likely a user would notice)

1. **S14 (remote bolus/carbs/override)** — Does candidate `POST /api/v2/notifications/loop` still return exactly HTTP 200 on success (LoopCaregiver treats anything else, e.g. 201/204, as failure and shows an error even though Loop may have received the push)? Read-derived: `notifications-v2.js` still uses `res.sendStatus(HTTP_OK)`; confirm on the #8754 head too.
2. **S14 permission** — the route still requires `notifications:loop:push`; LoopCaregiver always sends the admin `api-secret`, so `AUTH_DEFAULT_ROLES=denied` should not affect it. Confirm the throttle rewrite (BF-30) never delays a correct `api-secret` (it should now be faster).
3. **S1** — constants 5760 / 50 / absent. Read-derived: the candidate validator (`cgm-remote-monitor@ef3404fd lib/api/index.js:73-90`) refuses only non-whole-number counts and has no upper cap below 2^53, so 5760 passes. The join should only confirm no per-collection cap exists elsewhere.
4. **S2/S3** — confirm `find[dateString][$gte]` with a `…Z` ISO value is unchanged by the `enforceDateFilter` regex change (`lib/server/query.js`), and `created_at` is not typed as a number/date by `query-coercion.json`.
