# AndroidAPS: Nightscout API and websocket usage (15.0.9 consumer impact)

All claims are **read-derived**: taken from the source with `git grep` / `git show`. Nothing was built or run, and nothing was sent to any server.

| | |
|---|---|
| repo | `externals/AndroidAPS` (remote `origin` = nightscout/AndroidAPS) |
| ref A (shipping) | `origin/master` **598e2eb39c**, 2026-08-02, `git describe` = `3.4.2.6`, `Versions.appVersion = "3.4.2.6"` |
| ref B (next) | `origin/dev` **7e1d537d49**, 2026-09-23, `Versions.appVersion = "4.0.0-dev-c"`. dev is 2929 commits past master; master has 6 commits not on dev |
| local staleness | local HEAD = origin/master (behind=0, dirty=0, per client-corpus.txt). The working tree was not read |
| server refs compared against | cgm-remote-monitor `origin/master` 92d08342 (15.0.8), `ef3404fd` (#8754 bf2/auth-hardening, based on dev), `6d120fa2` (#8758 bf/object-id-crud) |

## The main structural fact: dev removes the v1 (socket.io `authorize`/`dbAdd`) client

- **master** ships **two** Nightscout clients:
  - **NSClient v1**: `plugins/sync/.../nsclient/`, socket.io only. Uploads go through `dbAdd`/`dbUpdate`.
  - **NSClient v3**: `plugins/sync/.../nsclientV3/` plus `core/nssdk`. REST `/api/v3/*` over Retrofit, plus the `/storage` and `/alarm` socket namespaces.
- **dev** keeps only v3. Commit `30fe4591a7` (2026-05-27, "Eliminate NSCv1", 101 files, −4167 lines) removes the v1 plugin. `git merge-base --is-ancestor` confirms it is on dev and **not** on master. On dev:
  - `git grep -n -E 'NSClientService|"dbAdd"|"loadRetro"|"authorize"'` over non-test Kotlin finds no v1 socket code.
  - dev also moves to Kotlin Multiplatform: Retrofit is replaced by Ktor (`NightscoutApi.kt`), and there is an iOS client on `socket.io-client-swift`.
- **Consequence for the join.** Every S6 main-namespace hit below (`authorize`, `dbAdd`, `dbUpdate`, main-namespace `ack`) exists **only in the shipping 3.4.x line**. It stops mattering once users move to the 4.0 line. 3.4.x users who picked NSClient v1 stay on the v1 socket path until they upgrade.

## Positive controls (the same `git grep` method reaches the Nightscout code)

- master REST (v3): `AndroidAPS@598e2eb39c core/nssdk/src/main/kotlin/app/aaps/core/nssdk/networking/NightscoutRemoteService.kt:41` `@GET("v3/entries?sort\$desc=date&type=sgv")`
- master v1 socket: `AndroidAPS@598e2eb39c plugins/sync/src/main/kotlin/app/aaps/plugins/sync/nsclient/services/NSClientService.kt:374` `socket?.emit("authorize", authMessage, ack)`
- dev REST (v3): `AndroidAPS@7e1d537d49 core/nssdk/src/commonMain/kotlin/app/aaps/core/nssdk/networking/NightscoutApi.kt:107-109` (`v3/entries`, `sort$desc`, `type=sgv`)
- dev socket: `AndroidAPS@7e1d537d49 plugins/sync/src/commonMain/kotlin/app/aaps/plugins/sync/nsclientV3/ws/NsConnectHandler.kt:61` `emitWithAck("subscribe", ...)`
- Control for the string literal `"count"`: the pattern does find `"count"` in `GarminPlugin.kt:429` (master) and `:427` (dev). That is AAPS's own local HTTP server for Garmin, not a Nightscout call. So the pattern does reach string literals, and the lack of Nightscout `count` hits is meaningful.

Absence patterns run on both refs over `*.kt *.java *.kts`, excluding test source sets:
`'"/?api/v1|api/v1/|/api/v2/|"count"|[?&]count=|Query\("count|\$exists|find\[|"\$in"|pebble|verifyauth|v2/properties|"api-secret"|API-SECRET|X-Forwarded|notifications/ack|v2/notifications'`.
The only Nightscout hit is `/api/v2/authorization/request/{refreshToken}`. This matches the 2026-09-14 census: AndroidAPS shows zero v1 filter use.

## S1–S15

| S-id | what the client does | anchor (repo@sha path:line) | patterns searched |
|---|---|---|---|
| S1 `count` | **None found** on either ref. Neither client sends a v1 REST request. v1 uses the socket, and v3 uses `limit`. | (absence; controls above) | `"count"`, `[?&]count=`, `Query\("count` |
| S2 `find[...]` | **None found.** No v1 `find` is built anywhere, so nothing from user input and no `$exists`. v3 filters are fixed operator keys: `date$gt=<Long>` on entries and `created_at$gt=<ISO string>` on treatments. | master `NightscoutRemoteService.kt:45,60`; dev `NightscoutApi.kt:116,149` | `find\[`, `\$exists`, `"\$in"`, `pipeline`, `\$expr` |
| S3 numeric comparisons | v3 only: `date$gt=<epoch ms>` with `sort=date` (entries first load), and `created_at$gt=<ISO>` with `sort=created_at` (treatments first load). There is no v1 comparison. The v3 search-input diff from master to ef3404fd adds only a `_id` tiebreaker to sort (`lib/api3/generic/search/input.js`); v3 filter typing is unchanged. The client's `date$gt` paging does not depend on the old tie order. | master `NightscoutRemoteService.kt:44-45,59-60`; dev `NightscoutApi.kt:113-119,146-152` | `\$gt`, `\$gte`, `\$lt`, `created_at`, `date` |
| S4 shared/aggregate reads | **None found** among `/api/v1/count`, `times`, `slice`, `echo`, `v2/properties`, `v2/ddata`, `v1/status`, `verifyauth`. It uses `GET /api/v3/status` and `GET /api/v3/lastModified`. | master `NightscoutRemoteService.kt:31-39`; dev `NightscoutApi.kt:96-100` | `api/v1`, `v2/properties`, `ddata`, `verifyauth`, `status.json`, `/count/`, `/slice/` |
| S5 auth | **v1 (master only):** the socket `authorize` payload is `{client:"Android_AAPS", history:48, status:true, from:<latest ms>, secret:<sha1(API_SECRET)>}`. There is no `token` field, so an access token typed into the secret field would be hashed and would not authenticate. **v3 (both refs):** the access token is exchanged at `GET /api/v2/authorization/request/<token>` for a JWT, which is then sent as `Authorization: Bearer` together with a `Date: <ms>` header. On **401 or 403** the client refreshes once and retries once; a 401/403 on the refresh itself raises `InvalidAccessTokenException`. On dev (`NsAuth.kt:111-127`) the authors note the 403 refresh is "doubtful" and keep it on purpose. No `X-Forwarded-For` is set. The candidate's failed-login delay now waits only on a *failed* attempt (`lib/authorization/index.js@ef3404fd:204-213`), so a correct token is no longer slowed by someone else's failures. **v1 write-permission failure:** `processAuthAck` shows "Authenticated (R…)" and raises an URGENT "no write permission" notification when `write && write_treatment` is false. | master `NSClientService.kt:361-375,228-250`; master `NSAuthInterceptor.kt` (401/403 then refresh); master `NightscoutAuthRefreshService.kt:14`; dev `NsAuth.kt:38-63,111-127` | `api-secret`, `Authorization`, `Bearer`, `token`, `X-Forwarded` |
| S6 websocket | **Library:** `io.socket:socket.io-client` **2.1.2** on both refs (master `gradle/libs.versions.toml:164`, dev `:194`). The 2.x Java client speaks the Socket.IO v3/v4 protocol (**EIO=4**); that comes from the library's compatibility line and is not verified in-repo. The server is `socket.io ~4.8.3` with `allowEIO3: true` on every ref, so EIO3 support is not needed. **v1 main namespace (master only):** connect, then `authorize` (above), then listen for `dataUpdate`, `announcement`, `alarm`, `urgent_alarm`, `clear_alarm`. It emits `dbAdd`, `dbUpdate`, and `ack(level, group, silenceTime)` **on the main namespace**. It **never** sends `loadRetro` or `dbRemove`. **On an authorize failure:** the server (every ref) calls `socket.disconnect()` without replying, so AAPS receives only a disconnect event. The watchdog pauses NSClient for 15 min and raises an URGENT "NS malfunction" notification after 5 connects in 2 min. A `dbAdd` reply of `{result:"Not…"}` triggers `EventNSClientRestart` (reconnect). **With AUTH_DEFAULT_ROLES=denied:** a correct API_SECRET gets admin rights, so `dataUpdate` and writes are unaffected. The candidate's new gate (loadRetro refused for sockets without read) does not touch AAPS, because AAPS never calls loadRetro. **v1 main-namespace `ack` has no server handler on any ref:** `git grep "'ack'"` over `lib/server/websocket.js` finds nothing on 92d08342 or ef3404fd; the only `ack` handlers are in `lib/api3/alarmSocket.js`. So AAPS v1 alarm silencing is already a no-op on 15.0.8. This is pre-existing, not a regression. **v3 (both refs):** two sockets, `<url>/storage` and `<url>/alarm`. Each sends `subscribe {accessToken}`, and storage also sends `collections:["devicestatus","entries","profile","treatments","foods","settings"]`. `foods` is not the server collection name `food`; that is pre-existing. On `/alarm` it emits `ack(level, group, ms)`. **Candidate change that reaches v3:** `/alarm` now sends alarms only to sockets in `AlarmReceivers`, which requires `api:*:read`. `ack` is now silently dropped unless the token has `notifications:*:ack` (`alarmSocket.js@ef3404fd:141-153`). A token with a role lacking either permission would stop receiving Nightscout alarms in AAPS, or its snooze would stop reaching Nightscout, with no error returned. The subscribe reply is still `success:true`. | master `NSClientService.kt:277-291,308-314,316-339,595-632`; master `acks/NSAddAck.kt:43-51`; master `NSClientV3Service.kt:116-199,343-346`; dev `NsConnectHandler.kt:53-89,126`; dev `SocketIoNsSocket.kt:47-53`; dev `NSClientV3Service.kt:219` | `socket\.io`, `io\.socket`, `"authorize"`, `"dbAdd"`, `"dbUpdate"`, `"dbRemove"`, `"loadRetro"`, `"subscribe"`, `"ack"`, `emitAlarmAck` |
| S7 own `_id` | **v1 (master):** the add path runs only when `ids.nightscoutId == null`, so no `_id` is sent on `dbAdd` in practice. `toJson(isAdd=true)` would add `_id` only if one were set. It **reads `_id` from the `dbAdd` ack** (`responseArray[0]._id`) and stores it as `nightscoutId`. `dbUpdate` sends `{collection, _id: nightscoutId, data}`. On import, `nightscoutId = identifier ?: _id`. A record created through v3 has a UUID `identifier`, so a later v1 `dbUpdate` would carry a non-hex `_id`. The server matches that as the exact string on every ref (`idMatch` non-hex branch at 6d120fa2). This is pre-existing and unchanged. **#8758 effect:** a `dbUpdate` with a 24-hex `_id` now matches both the ObjectId and the string form and uses `updateMany`, so a record stored with a string `_id` by an earlier release is now found. **v3 (both refs):** it never sends `_id`. It stores `identifier` from the POST response (201/200) as `nightscoutId`, and PATCH/DELETE go to `/api/v3/<coll>/<identifier>`. Devicestatus is created once per record; it relies on v3 identifier dedup. | master `nsclient/extensions/BolusExtension.kt:26,38-40`; master `DataSyncSelectorV1.kt:185-203`; master `acks/NSAddAck.kt:31-36`; master `NSClientService.kt:595-613`; master `NSClientV3Plugin.kt:659-714`; dev `NightscoutApi.kt:132-173` | `"_id"`, `identifier`, `nightscoutId` |
| S8 treatment edits | **v1 (master):** an edit is `dbUpdate` `$set` of the whole record. A delete is a soft delete: `dbUpdate` with `isValid:false`; there is no `dbRemove`. **v3:** an edit is `PATCH /v3/treatments/{identifier}`. A delete (`isValid == false`) is `DELETE /v3/treatments/{identifier}` without `permanent` (soft). 404 is treated as OK. `created_at` is ISO from `dateUtil.toISOString`. On **dev**, a 404 on a real UPDATE is retried up to 5 times per nsId, and a 400 is deliberately not retried (`NSClientV3Plugin.kt@7e1d537d49:700-712`). **dev only:** `/api/v3/settings` gets PUT (upsert), PATCH, and DELETE, optionally with `permanent=true` (client control). | master `BolusExtension.kt:21`; master `NSAndroidClientImpl.kt:330-345`; dev `NightscoutApi.kt:165-173,264-284` | `isValid`, `deleteTreatment`, `updateTreatment`, `permanent` |
| S9 food | v3 `GET /v3/food?limit=1000` and `POST /v3/food`. Food PATCH/DELETE: on master the Retrofit declarations lack `{identifier}` in the path, so the call throws locally and no request is sent. On dev, `updateFood` and `deleteFood` are deliberate `error(...)` stubs, again with no request. v1 `dbAdd`/`dbUpdate` to `food` exist on master. No `quickpick`, `hideafteruse`, or `position` handling was found. | master `NightscoutRemoteService.kt:80-94`; master `LoadFoodsWorker.kt:37`; dev `NightscoutApi.kt:189-218`; dev `LoadFoodsRunner.kt:35`; master `DataSyncSelectorV1.kt:385,391` | `food`, `quickpick`, `hideafteruse` |
| S10 profile | v1 (master): `dbAdd("profile", <whole profile store JSON>)`, with `date` added if missing. No `_id` was seen; ProfilePlugin builds the JSON, but this was not fully traced. v3: `POST /v3/profile`, `GET /v3/profile?sort$desc=date&limit=1`, and `GET /v3/profile/history/{from}?limit=10`. Profile switches are uploaded as treatments. `defaultProfile` and `startDate` are not specially handled in the sync layer. | master `DataSyncSelectorV1.kt:786-805`; master `NightscoutRemoteService.kt:96-103`; dev `NightscoutApi.kt:224-240` | `"profile"`, `startDate`, `defaultProfile` |
| S11 COB/IOB | Writes devicestatus `openaps.{enacted,suggested,iob}`, plus `pump`, `uploaderBattery`, `isCharging`, and `configuration`. COB and IOB travel inside `suggested`/`enacted`, as oref produces them. It reads other uploaders' devicestatus from `dataUpdate` or v3. It does **not** read Nightscout-computed COB/IOB (`v2/properties` or pebble were not found). | master `nsclient/extensions/DeviceStatusExtension.kt:8-22` | `openaps`, `suggested`, `enacted`, `iob`, `properties`, `pebble` |
| S12 query shape | **No v1 query strings.** v3 query keys are sent with a literal `$` (Retrofit `encoded=true`; Ktor `encodedParameters`). There is no `$in`, and no array parameter exists; the maximum array size is **0**. | dev `NightscoutApi.kt:33-45,108-117` | `\$in`, `find\[`, `encoded` |
| S13 API v3 | `limit` values are all constants: **500** (`RECORDS_TO_LOAD`, entries and treatments, first load and history), **1000** (food), **10** (profile history), **1** (last profile), and **100** on dev (settings search and history). None is 0. All are ≤ the default `API3_MAX_LIMIT` of 1000. The master→candidate change to `parseLimit` (digits only, `1..max`) accepts every one of them. `getFoods(1000)` would already get 400 on 15.0.8 if an operator lowered `API3_MAX_LIMIT` below 1000; that is not new. Paging uses `/history/{srvModified}` or `date$gt`/`created_at$gt`, never `skip` or `fields`. It uses `identifier`, never `_id`. | master `NSClientV3Plugin.kt:144`; master `LoadBgWorker.kt:52-54`; dev `NSClientV3Plugin.kt:184`; dev `ClientControlReceiver.kt:252`; server `lib/api3/generic/collection.js@ef3404fd:76-96` | `limit`, `RECORDS_TO_LOAD`, `skip`, `fields`, `history` |
| S14 other | **None found:** no `/api/v2/notifications/loop`, `/api/v1/notifications/ack`, Alexa, or Google Home. Alarm acks go over the socket (S6). | (absence) | `notifications`, `v2/notifications`, `alexa`, `googlehome` |
| S15 errors | **v1 (master):** a `dbAdd` with no ack waits 60 s; then `confirmed=false`, the queue stops, and it retries on the next resend. It is not marked uploaded. A reply of `{result:"Not…"}` triggers a restart. **v3 treatments/entries** (both refs): **400 means log `◄ FAIL` and `return true`**, so the record counts as handled, the cursor advances, the record is **never uploaded and never retried**, and the user sees only a log line. A 400 whose body is "Bad or missing utcOffset field" is retried once with `utcOffset=0`. Other 4xx/5xx return `config.ignoreNightscoutV3Errors()`, which is false unless the file `ignore_nightscout_v3_errors` exists; the result is retry and a stall. **v3 devicestatus and profile:** 400 falls into `else`, gives `ignoreNightscoutV3Errors()`, and **stalls and retries**. **Reads:** a 4xx throws `InvalidParameterNightscoutException`, the worker gives `Result.failure`, and the next loop retries. An empty list ends the load (`RCV BG END`); no workaround for empty results was found. | master `DataSyncSelectorV1.kt:192-196`; master `acks/NSAddAck.kt:43-51`; master `NSClientV3Plugin.kt:484-512,647-658`; master `NSAndroidClientImpl.kt:293-328`; master `ConfigImpl.kt:55`; master `LoadBgWorker.kt:62-90`; dev `NSClientV3Plugin.kt:700-712,833` | `400`, `isSuccessful`, `catch`, `ignoreNightscoutV3Errors`, `confirmed` |

## Things not analysed, and why

- **Whether a released 3.4.x build and master 598e2eb39c differ on these paths.** Only `origin/master` (= tag 3.4.2.6) was read; older 3.x tags were not diffed.
- **The EIO version** comes from the library's major version, not from resolving the dependency tree. No build was run.
- **iOS and desktop (dev):** only noted, not analysed. They use the same `NsConnectHandler` payloads through `socket.io-client-swift` and Ktor.
